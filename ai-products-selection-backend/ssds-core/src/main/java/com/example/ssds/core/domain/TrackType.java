package com.example.ssds.core.domain;

/**
 * 品項雙軌別（規格書 §4 雙軌說明、§7.2 既有資料表變更）。
 *
 * <p>A 軌已有供應商與成本，跑完整因子評分；
 * B 軌是「先看到熱度、還沒找到貨」的探索標的，不產生選品分數，
 * 改以時效落差排序（§5.8）。找到供應商後由 B 轉 A，熱度資料一併帶入。
 */
public enum TrackType {
    /** 已有供應商，套用完整因子評分 */
    A,
    /** 尋源探索中，只產生尋源優先序 */
    B
}
