import { ScoreGradePipe } from './score-grade.pipe';

describe('ScoreGradePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreGradePipe();
    expect(pipe).toBeTruthy();
  });
});
