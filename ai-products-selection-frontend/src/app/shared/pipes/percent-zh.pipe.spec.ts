import { PercentZhPipe } from './percent-zh.pipe';

describe('PercentZhPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentZhPipe();
    expect(pipe).toBeTruthy();
  });
});
