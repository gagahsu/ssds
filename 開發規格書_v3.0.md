# 團購產業 AI 採購選品策略系統
## 開發規格書 v3.0

| 項目 | 內容 |
|---|------|
| 專案名稱 | 團購產業 AI 採購選品策略系統（Smart Sourcing Decision System, SSDS） |
| 客戶 | 莊敬生活百貨 |
| 文件版本 | v3.0 |
| 撰寫日期 | 2026-08 |
| 技術棧 | Angular 19+ / Angular Material / Java 17 或 21 / Spring Boot 4.1 + Gradle（Groovy DSL） / MySQL 8 / opencode + OpenRouter |
| 對應簡報 | 《智慧零售與物流 AI 系統建置計畫》第一階段專題 |

### v3.0 修訂說明

v3.0 不新增功能範圍，是對 v2.0 的**一致性修訂**。v2.0 在增修過程中留下三類問題：規格內部互相矛盾（同一件事有兩種說法）、關鍵定義缺漏（因子算不出來）、以及章節未同步（前面改了後面沒改）。v3.0 逐項裁決並統一。

主要裁決結果：

| 項目 | v2.0 狀態 | v3.0 裁決 |
|---|---|---|
| 加分小計計算 | 加權和之後再做一次未定義的換算 | **取消二次換算**，加權和即為加分小計（§5.5） |
| 加分因子數 | 定義 7 個，權重組只有 5 欄 | **權重組改為 6 因子**，納入價格帶適配度；熱度量級改為門檻條件（§5.2） |
| 分組排行 | 三榜，但權重組有四組 | **四榜**，與四組情境權重組一對一（§FR-04） |
| B 軌資料模型 | product.track_type 與 sourcing_candidate.keyword_id 並存 | **一律以 product 為主體**，sourcing_candidate 綁 product_id（§7.2） |
| 結案回填 | decision_feedback 與 campaign_result 兩張表 | **廢除 decision_feedback**，僅保留 campaign_result（§7.2） |
| 評論情感欄位 | 「移除加分用情感分數欄位」 | **保留 sentiment 欄位**，僅取消其加分用途（§5.2.2） |
| 角色階層 | 使用 `BUYER+` / `DATA_ADMIN+` 記法 | **廢除 `+` 記法**，全部明列角色（§2.1） |
| 效能指標 | 「500 品項評分 < 60 秒」與「512 次 LLM 請求 ≈ 26 分」並存 | **拆為兩條指標**：評分計算與 AI 分析批次（§10） |
| opencode 角色 | 架構圖示為全流量經 opencode | **A 軌 REST 直連，B 軌經 opencode + MCP**（§3.1） |
| 風險編號 | §13 用 R-1～R-8，§13.1 用 R-08～R-19，編號撞號 | **合併為單一風險表 R-01～R-19**（§13） |

完整修訂對照見 §15。

---

# 1. 專案概述

## 1.1 背景

莊敬生活百貨的團購採購作業目前高度依賴採購人員的個人經驗，簡報中已診斷出三項痛點：

| 痛點 | 現況 | 造成的結果 |
|---|---|---|
| P-1 決策憑感覺 | 採購依個人經驗判斷是否引進商品 | 引進不符在地客群的商品，資金與庫存積壓 |
| P-2 趨勢捕捉慢 | 人工收集網路評價與競品價格 | 社群爆款生命週期短，發現時商機已過 |
| P-3 客群未對齊 | 網路熱潮未與既有主力客群交叉比對 | 實際轉化率偏低 |

## 1.2 系統定位

**這是決策輔助系統，不是全自動採購系統。**

```
資料整合  →  量化評分  →  建議報告  →  人工決策
(銷售/趨勢/  (多因子加權  (排序＋可解釋  (採購人員擁有
 評論/價格)   選品分數)     的理由)       最終決定權)
```

系統不會自動下單、不會自動決定進貨量。所有 AI 產出皆為「建議」，並且必須附帶可追溯的理由與資料來源。此定位是本專案的**核心設計約束**，貫穿所有功能設計。

## 1.3 專案目標與可量化指標

| 編號 | 目標 | 驗收指標 |
|---|---|---|
| G-1 | 選品分數化 | 每個候選品項皆能產出 0–100 分與 A/B/C 分級，並可展開因子組成 |
| G-2 | 縮短選品作業時間 | 單一品項評估時間由人工約 30 分鐘降至 3 分鐘以內。量測方式：自品項建檔完成起算，至畫面顯示分數、分級與 AI 建議為止的牆鐘時間，取 UAT 情境中 10 次的中位數 |
| G-3 | 自動化趨勢監控 | 每日自動更新趨勢熱度；熱度異常（判定規則見 §FR-10-2）於偵測後 24 小時內產生示警 |
| G-4 | AI 賣點與風險摘要 | 每個品項自動彙整 ≥2 條賣點、≥2 條風險，並註明來源評論筆數 |
| G-5 | 決策可追溯 | 每次評分皆保存權重版本與因子快照，可重現當時的分數 |
| G-6 | 決策準確度可驗證 | 能以歷史開團紀錄執行回測，比較不同權重版本的預測表現（§FR-15）；累積樣本達門檻後可產出準確度指標（§FR-11-3） |

> G-6 為 v3.0 新增。v2.0 的 §4 總覽表已把 FR-15 的成果標為「越用越準」，但 §1.3 未列對應目標，此處補齊。

## 1.4 專案範圍

### In Scope（第一階段交付）

**A 軌：既有品項評分**

- 候選品項的建檔、匯入與生命週期管理（含 A／B 軌別）
- 多因子選品分數計算引擎、加分／扣分分離與 A/B/C 分級
- 情境權重組（四組）與版本管理、AI 情境判定與人工覆寫
- 商品評論的 AI 賣點/風險萃取、情緒分佈整理、進貨建議生成
- 高風險品項自動示警
- 採購決策紀錄與回饋閉環（實際銷售 vs 系統預測）

**B 軌：商機探索**

- 尋源探索與探索報告（FR-16-1）
- 尋源優先序與時效落差模型（FR-16-2）
- 成案轉軌（B → A）

**共用**

- 多來源熱度資料擷取與趨勢分析（Threads、Google Trends、Instagram、人工熱度標記）
- 熱度來源管理與降級處理（FR-14-2）
- 節慶檔期時間窗與季節氣候基準（FR-17）
- 權重校準閉環：統計迴歸、AI 解讀、人工核准、歷史回測（FR-15）
- 歷史銷售資料匯入（CSV/Excel）
- 使用者、角色權限、稽核紀錄
- 報表匯出（Excel / PDF）

> v2.0 的 In Scope 清單未隨新增模組更新（缺 FR-14-1、FR-15、FR-16、FR-17），v3.0 補齊。各項的必做／次要分級見 §12.1。

### Out of Scope（本階段不做，列為未來擴充）

- 自動下單、自動補貨、與 ERP/POS 的即時雙向串接
- 選址分析、物流排車（簡報中提及的後續題目）
- 供應商議價、合約管理
- 前台消費者介面（本系統為內部後台）
- 即時爬取受反爬保護的電商平台與社群平台（改以人工/半自動匯入，理由見附錄 C）
- 供應商營運異常監控（無可用資料源，見 §FR-10-1）

## 1.5 名詞定義

| 名詞 | 定義 |
|---|---|
| 候選品項 (Candidate Product) | 尚未決定是否引進、進入系統評估的商品。A 軌與 B 軌品項皆為候選品項 |
| A 軌 (Track A) | 已知商品，有供應商與報價，可套用完整因子評分 |
| B 軌 (Track B) | 只有概念或關鍵字，尚未找到貨源，僅有熱度類資料 |
| 選品分數 (Selection Score) | 多因子加權後的 0–100 分綜合評分，等於加分小計減扣分小計 |
| 加分因子 (Bonus Factor) | 參與情境權重加權的因子，共 6 項（§5.2.1） |
| 扣分因子 (Penalty Factor) | 不參與權重、固定生效的風險扣分，共 3 項（§5.2.2） |
| 情境權重組 (Weight Profile) | 一組具名的加分因子權重，共四組：VIRAL / FESTIVAL / REPLENISHMENT / SEASONAL |
| 權重版本 (Weight Version) | 四組情境權重的具名快照，評分結果永遠綁定某個版本 |
| 評分快照 (Score Snapshot) | 某次評分當下的所有因子值與最終分數，不可變更 |
| 主情境 (Primary Scene) | 一個品項在同一期可產生多筆評分（每個適用情境一筆），其中一筆標記為主情境，供品項詳情與 KPI 去重使用 |
| 時效落差 (Time Gap) | B 軌專用：預估熱度剩餘壽命減去預估尋源前置期 |
| AI 洞察 (AI Insight) | 由 LLM 產出的賣點、風險、進貨建議三類文字結果 |
| 決策紀錄 (Decision) | 採購人員對某品項做出的採納/不採納決定與理由 |
| 結案 (Campaign Close) | 一次開團的銷售期結束，由採購人員在系統中標記結案日期，作為回填提醒的起算點 |
| 回饋閉環 (Feedback Loop) | 將實際銷售結果回填，用以校正權重與驗證模型準確度 |

---

# 2. 使用者角色與權限

| 角色 | 代碼 | 說明 | 主要使用畫面 |
|---|---|---|---|
| 採購專員 | `BUYER` | 日常評估品項、記錄決策、發起尋源探索 | 儀表板、品項管理、品項詳情、風險中心、尋源探索、人工熱度標記 |
| 採購主管 | `BUYER_LEAD` | 覆核決策、調整與核准權重、核准校準建議、看報表 | 採購專員全部＋權重設定、權重校準審核、報表、稽核紀錄 |
| 資料管理員 | `DATA_ADMIN` | 匯入銷售/評論資料、維護關鍵字與節慶檔期 | 資料匯入、趨勢分析、AI 任務中心、節慶檔期維護 |
| 系統管理員 | `SYS_ADMIN` | 使用者、模型、風險門檻、熱度來源、系統參數 | 系統設定、熱度來源狀態、稽核紀錄 |
| 唯讀觀察者 | `VIEWER` | 只能瀏覽，不能修改 | 儀表板、報表 |

**角色之間沒有階層關係。** `DATA_ADMIN` 不是 `BUYER` 的超集（前者不能記錄採購決策，後者不能匯入資料）。因此本文件**不使用 `BUYER+`、`DATA_ADMIN+` 這類記法**，所有權限一律明列角色代碼。

## 2.1 權限矩陣

本表為權限的**唯一事實來源**。§8 端點清單與 §9.2 路由表只引用本表，不另行定義。

| # | 功能 | BUYER | BUYER_LEAD | DATA_ADMIN | SYS_ADMIN | VIEWER |
|---|---|:---:|:---:|:---:|:---:|:---:|
| 1 | 檢視儀表板 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 2 | 檢視排行、品項詳情、趨勢 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 3 | 檢視與匯出報表 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 4 | 新增／編輯品項 | ✓ | ✓ | ✓ | ✓ | – |
| 5 | 刪除品項（軟刪除） | – | ✓ | – | ✓ | – |
| 6 | 觸發 AI 分析 | ✓ | ✓ | ✓ | ✓ | – |
| 7 | 建立人工熱度標記 | ✓ | ✓ | ✓ | ✓ | – |
| 8 | 發起尋源探索（B 軌） | ✓ | ✓ | – | ✓ | – |
| 9 | 維護尋源優先序（覆寫前置期、成案轉軌） | ✓ | ✓ | – | ✓ | – |
| 10 | 覆寫情境權重組 | ✓ | ✓ | – | ✓ | – |
| 11 | 記錄採購決策 | ✓ | ✓ | – | ✓ | – |
| 12 | 標記結案與回填實際結果 | ✓ | ✓ | ✓ | ✓ | – |
| 13 | 覆核決策 | – | ✓ | – | ✓ | – |
| 14 | 處理／忽略風險示警 | ✓ | ✓ | – | ✓ | – |
| 15 | 建立／編輯權重版本草稿 | – | ✓ | – | – | – |
| 16 | 核准權重版本生效 | – | ✓ | – | – | – |
| 17 | 核准權重校準建議 | – | ✓ | – | – | – |
| 18 | 調整風險扣分門檻 | – | – | – | ✓ | – |
| 19 | 調整熱度來源合成權重與啟用狀態 | – | – | – | ✓ | – |
| 20 | 維護節慶檔期與品類前置天數 | – | ✓ | ✓ | ✓ | – |
| 21 | 維護品項×節慶關聯度 | ✓ | ✓ | ✓ | ✓ | – |
| 22 | 匯入資料 | – | ✓ | ✓ | ✓ | – |
| 23 | 管理使用者與角色 | – | – | – | ✓ | – |
| 24 | 設定 AI 模型、預算池、快取 | – | – | – | ✓ | – |
| 25 | 檢視稽核紀錄 | – | ✓ | – | ✓ | – |

**v3.0 對權限的三項修正**

1. **廢除 `+` 記法**（原 §8.2、§9.2 的 `BUYER+`、`DATA_ADMIN+`）。角色非全序，`+` 無法翻譯為 `hasAnyRole()`。
2. **拆分「調整評分權重」**。v2.0 將權重與風險門檻併為一列，導致 §FR-08「SYS_ADMIN 可調風險門檻」與矩陣的「SYS_ADMIN 不可調權重」互相矛盾。v3.0 拆為第 15／16／18／19 四列。
3. **稽核紀錄脫離系統設定模組**。v2.0 中 §2.1 給 BUYER_LEAD 檢視權，但 AC-13-3 又要求「僅 SYS_ADMIN 可存取本模組」。v3.0 將稽核紀錄獨立為 `/audit-logs` 路由（§9.2），系統設定模組（`/admin`）維持 SYS_ADMIN 專屬。

權限實作：後端以 Spring Security `@PreAuthorize("hasAnyRole(...)")` 為準，前端僅做選單與按鈕的顯示控制（不可作為安全邊界）。頁面級守衛（§9.2）與動作級守衛（本表）分開實作——例如 `/products/:id` 對所有已登入者開放，但頁內的「建立決策」按鈕依第 11 列控制。

---

# 3. 系統架構

## 3.1 整體架構

```
┌─────────────────────────────────────────────────────────────┐
│                    瀏覽器 (Chrome / Edge)                    │
│   Angular 19+ SPA + Angular Material + ngx-echarts          │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTPS / REST (JSON) + JWT
┌───────────────────────────▼─────────────────────────────────┐
│              Spring Boot 4.1 (Gradle multi-module)          │
│                                                             │
│  ssds-api          REST Controller / DTO / 例外處理          │
│  ssds-core         領域模型、評分引擎、風險規則                │
│  ssds-ai           AI Orchestrator、Agent、PromptSanitizer   │
│  ssds-ingest       熱度 Adapter、CSV 匯入、評論匯入           │
│  ssds-calibration  統計迴歸與回測                            │
│  ssds-infra        JPA Repository、Flyway、快取、排程         │
└──┬─────────────────┬────────────────┬───────────────────────┘
   │                 │                │
   │            A 軌 │           B 軌 │
   │         REST 直連│      opencode  │
   │                 │                │
┌──▼──────────┐  ┌───▼────────────────▼───┐   ┌───────────────┐
│  MySQL 8    │  │      OpenRouter        │   │  外部資料來源  │
│  主資料庫    │  │      (LLM Gateway)     │   │  Threads API  │
│  Flyway 版控 │  └───────────▲────────────┘   │  Google Trends│
└─────────────┘              │                │  IG Graph API │
                  ┌──────────┴──────────┐     │  評論 CSV     │
                  │  opencode server    │     └───────────────┘
                  │  （僅服務 B 軌）     │
                  │  + MCP client       │
                  └─────────────────────┘
```

**opencode 的適用範圍（v3.0 釐清）**

v2.0 的架構圖把 OpenRouter 畫在 opencode 之下，代表所有 LLM 流量都經過 opencode；但 §6.2 又寫「A 軌 REST 直連」。兩者互斥。v3.0 裁決如下：

| 軌別 | 路徑 | 理由 |
|---|---|---|
| A 軌（6 個 Agent） | Spring Boot → OpenRouter REST | 批次 ETL 需要可重試、可監控、schema 穩定；不需要工具呼叫 |
| B 軌（SourcingScoutAgent） | Spring Boot → opencode server → MCP 工具 + OpenRouter | 需要多步驟調查與工具呼叫，opencode 提供編排能力 |

opencode 不可用時，僅 B 軌尋源探索停用，A 軌完全不受影響（§6.7.4）。

## 3.2 技術棧與版本

### 前端

| 項目 | 選型 | 版本 | 用途 |
|---|---|---|---|
| 框架 | **Angular** | **≥ 19，建議 21**，standalone components + signals | SPA 主體 |
| UI 元件 | **Angular Material** | 與 Angular 主版本一致 | 表格、對話框、表單 |
| 圖表 | ngx-echarts + ECharts | — | 趨勢折線、雷達圖、情緒堆疊條 |
| HTTP | Angular HttpClient + Interceptor | 內建 | JWT 注入、錯誤攔截 |
| 狀態 | Angular Signals + Service | 內建 | 輕量狀態，不引入 NgRx |
| 表單 | Reactive Forms | 內建 | 全站表單一律使用 |
| 建置 | Angular CLI | 與 Angular 主版本一致 | — |
| 單元測試 | **Vitest**（或 Web Test Runner） | 與 Angular 主版本一致 | 見下方說明 |
| API client | 由後端 OpenAPI 產生 TypeScript client | — | 見 §3.3 契約管理 |

> **版本表述修正（v3.0）**：v2.0 寫「19 或 21」，跳過 20 且未說明理由，實作團隊無從判斷 20 是否可接受。v3.0 改為「≥ 19」並建議 21。其餘前端套件不在規格書硬指定版本，以 `package.json` 鎖定為準。

> **測試框架修正（v3.0）**：v2.0 指定 Jasmine + Karma。Karma 已於 Angular 16 起標記為 deprecated，Angular 19 之後新專案不再產生 Karma 設定。v3.0 改為 Vitest；若專案初始化時 Vitest 支援尚不穩定，可退回 Web Test Runner，並於 W1 技術驗證時決定，結果記入 `docs/frontend-setup.md`。

### 後端

| 項目 | 選型 | 版本 | 用途 |
|---|---|---|---|
| 語言 | **Java** | **17 或 21 (LTS)**，以 Spring Boot 4.1 官方相容矩陣為準 | 見下方待驗證說明 |
| 框架 | **Spring Boot** | **4.1** | 見下方待驗證說明 |
| 建置 | **Gradle** | 相容 Spring Boot 4.1 之版本 | multi-module；建置腳本一律採 Groovy DSL（`.gradle`），不使用 Kotlin DSL（`.gradle.kts`） |
| 資料存取 | Spring Data JPA + Hibernate | — | — |
| 資料庫遷移 | Flyway | — | schema 版控 |
| 安全 | Spring Security + JWT | — | — |
| 排程 | Spring Scheduling + ShedLock | — | 避免多節點重複執行 |
| 限流 | Resilience4j RateLimiter | — | **LLM 呼叫限流，見 §6.7** |
| 快取 | Caffeine（本機） | — | LLM 結果與趨勢資料快取 |
| API 文件 | springdoc-openapi | — | Swagger UI + OpenAPI 契約產出 |
| 資料驗證 | Jakarta Validation | — | — |
| 農曆換算 | 具農曆／國曆轉換能力之 Java 函式庫 | — | 供 §FR-17-1，選型於 W1 決定 |
| 統計迴歸 | Apache Commons Math 或同等函式庫 | — | 供 `ssds-calibration` |
| 測試 | JUnit 5 + Mockito + Testcontainers + WireMock | — | — |

> **待驗證（列入附錄 A 第 11 項）**：Spring Boot 4.1 的 GA 時程，以及其對 JDK 最低版本的實際要求。v2.0 假設「部署環境至少需支援 17」，此假設須於 W1 以官方相容矩陣確認。若 4.1 要求 Java 21，則部署環境需求須同步上調。

> 其餘相依套件不在規格書硬指定版本，以 Gradle version catalog（`gradle/libs.versions.toml`）鎖定為準。

### AI 層

| 項目 | 選型 | 用途 |
|---|---|---|
| Agent 執行環境 | opencode（server 模式常駐）| **僅 B 軌**：多步驟任務編排、MCP 工具呼叫 |
| LLM 閘道 | OpenRouter | 統一 API 存取多家模型；A 軌由 Spring Boot 直連 |
| 模型策略 | **一律使用免費模型（`:free`）** | 專題無 LLM 預算 |
| 輸出格式 | 強制 JSON（JSON Schema 驗證） | 免費模型的格式遵循度較不穩定，驗證與重試為必要機制 |
| 模型設定方式 | **邏輯別名 → 實際 model ID，設定檔對應** | 免費模型隨時可能下架，換模型不改程式碼 |

模型分派、配額限制與資料外送原則詳見 §6.7 與 §6.8。

### 資料庫

- MySQL 8，字元集 `utf8mb4` / `utf8mb4_unicode_ci`
- **時間欄位一律使用 `DATETIME` 並存 UTC**，應用層以 `Asia/Taipei` 呈現。JPA 端統一設定 `spring.jpa.properties.hibernate.jdbc.time_zone=UTC`，JDBC URL 加 `serverTimezone=UTC`
- **ISO 週（`period` 欄位）一律以 `Asia/Taipei` 判定**。跨週邊界的評分依台北時間歸週，避免同一批評分被拆到兩個 period
- 連線池 HikariCP，預設 max-pool-size 20

> v2.0 只寫「資料庫存 UTC，應用層轉換」，但未指定欄位型別，也未說明 ISO 週的時區基準——跨週邊界的評分會歸錯週。v3.0 明確化。

## 3.3 版本庫結構（前後端分離）

專案採前後端分離架構，**前後端分屬不同 Git repository**，各自獨立版控、建置與部署。

| Repository | 內容 | 技術 |
|---|---|---|
| `ssds-backend` | Spring Boot 多模組專案 | Java 17/21 + Spring Boot 4.1 + Gradle（Groovy DSL） |
| `ssds-frontend` | Angular SPA | Angular 19+ + Angular Material |

### 為什麼分離

| 理由 | 說明 |
|---|---|
| 建置週期不同 | 前端改樣式不需重建後端 jar，反之亦然 |
| 部署節奏不同 | 前端為靜態檔，可獨立發版；後端需重啟服務 |
| 權限與分工 | 兩組人各自 review 各自的 PR，commit 歷史不互相干擾 |
| 技術棧隔離 | Node 與 JVM 的相依樹完全分開，CI 快取互不影響 |

### 契約管理

前後端之間以 **OpenAPI 契約**為唯一介面，不共用程式碼。

```
ssds-backend                          ssds-frontend
  springdoc-openapi                     openapi-generator
       │                                      ↑
       └── 產出 openapi.json ─────────────────┘
           （CI 產出後發佈為 artifact）
```

| 項目 | 規則 |
|---|---|
| 契約來源 | 後端為單一事實來源（single source of truth） |
| 產出方式 | 後端 CI 建置時自動產出 `openapi.json` 並發佈為 artifact |
| 前端消費 | 前端 CI 拉取該 artifact，以 openapi-generator 產生 TypeScript client 與型別 |
| 破壞性變更 | 後端不得在未告知前端的情況下移除或改名既有欄位；新增欄位視為相容變更 |
| 版本標示 | API 路徑統一前綴 `/api/v1`，重大不相容時升為 `/api/v2` 並保留舊版一個迭代 |

> 若專題期間 CI 尚未建置完成，退而求其次：後端於每次 API 變更後手動匯出 `openapi.json`，提交至前端 repo 的 `contract/` 目錄，並於 commit message 註明對應的後端 commit。

### 跨來源設定

| 環境 | 前端來源 | 後端 CORS 允許 |
|---|---|---|
| 開發 | `http://localhost:4200` | 明列該來源，允許 credentials |
| 展示 | 由 Nginx 同源代理 | 同源，不需 CORS |

前端 API base URL 以環境檔管理（`environment.ts` / `environment.prod.ts`），不得硬編碼。

### ssds-backend 模組結構

```
ssds-backend/
├── settings.gradle
├── build.gradle                     # 共用版本、Java toolchain
├── gradle/libs.versions.toml
├── ssds-core/                       # 領域模型、ScoringEngine、RiskRuleEngine
│   ├── scoring/factor/              # 六個加分因子與三個扣分規則
│   ├── scoring/weight/              # 情境權重組、版本管理
│   ├── scoring/temporal/            # 節慶時間窗、氣候基準
│   └── port/                        # Repository 介面（由 infra 實作）
├── ssds-infra/                      # Entity、Repository 實作、Flyway migration、排程、快取
├── ssds-ai/                         # AiOrchestrator、Agent、PromptTemplate、PromptSanitizer
│   ├── agent/
│   ├── schema/
│   ├── prompt/
│   ├── budget/
│   └── mcp/                         # MCP client（僅 B 軌尋源探索使用）
├── ssds-ingest/                     # Adapter 架構
│   ├── adapter/threads/
│   ├── adapter/trends/
│   ├── adapter/instagram/
│   ├── adapter/manual/              # 人工標記來源
│   ├── composite/                   # 多來源合成與斜率計算
│   └── importer/                    # CSV／XLSX 匯入（銷售、評論、會員輪廓、品項主檔）
├── ssds-calibration/                # 統計迴歸、回測、校準報告
└── ssds-api/                        # Controller、DTO、Security 設定、例外處理
```

> 前端不置於本 repo。原 v1.0 的 `frontend/` 目錄移至獨立的 `ssds-frontend` repository。
>
> v3.0 補充：`ssds-ingest` 明確涵蓋 CSV／評論匯入（v2.0 的 §3.1 架構圖有列，但 §3.3 模組結構只有熱度 adapter）；`ssds-core/port` 為 v3.0 新增，用以說明依賴方向。

**建置腳本約定**

| 項目 | 規則 |
|---|---|
| DSL | Gradle Groovy DSL，副檔名一律 `.gradle`，不使用 `.gradle.kts` |
| 相依版本管理 | `gradle/libs.versions.toml`（version catalog 與 DSL 無關，Groovy 專案同樣適用） |
| 共用設定 | 根 `build.gradle` 以 `subprojects { }` 統一 Java toolchain、編碼、測試設定 |
| 各模組 | 每個模組自有 `build.gradle`，只宣告該模組的相依 |

**依賴方向（單向，禁止循環）**

`ssds-core` 是依賴的匯聚點，不依賴任何其他模組。它以 `port/` 下的介面宣告自己需要的資料存取能力，由 `ssds-infra` 提供實作（依賴反轉）。

```
                    ssds-api
                       │
        ┌──────────────┼──────────────┬──────────────┐
        ▼              ▼              ▼              ▼
   ssds-infra      ssds-ai      ssds-ingest   ssds-calibration
        │              │              │              │
        └──────────────┴──────┬───────┴──────────────┘
                              ▼
                          ssds-core
                  （port/ 介面由 ssds-infra 實作，
                    於 ssds-api 的 Spring context 組裝）
```

| 模組 | 依賴 | 說明 |
|---|---|---|
| `ssds-core` | 無 | 純領域邏輯，可獨立單元測試 |
| `ssds-infra` | `ssds-core` | 實作 core 的 port 介面 |
| `ssds-ai` | `ssds-core` | 讀寫 ai_insight 等資料經 core 的 port |
| `ssds-ingest` | `ssds-core` | 寫入 heat_reading 等資料經 core 的 port |
| `ssds-calibration` | `ssds-core` | 同上 |
| `ssds-api` | 全部 | 組裝層，負責 Spring 的 Bean 綁定 |

> v2.0 的依賴圖有 `ssds-api → ssds-core` 與 `ssds-api → ssds-infra` 兩條線，但未說明 `ssds-ai` 與 `ssds-ingest` 如何存取資料庫，且 core 與 infra 的箭頭方向會形成事實上的循環。v3.0 以 port/adapter 模式釐清。

**v3.0 模組變更說明**（相對於 v2.0）

| 模組 | 變更 |
|---|---|
| `ssds-core` | 新增 `port/` 目錄；`scoring/factor/` 明確化為六個加分因子 |
| `ssds-ingest` | 新增 `composite/`（合成與斜率）與 `importer/`（CSV 匯入） |
| 其餘 | 無結構變更，僅釐清依賴方向 |

## 3.4 部署架構

| 環境 | 說明 |
|---|---|
| 開發 | 兩個 repo 各自啟動。後端：Docker Compose（MySQL + opencode server），`:8080`；前端：`ng serve` 於 `:4200`，以 proxy 設定轉發 `/api` 至 `:8080` |
| 展示 | 單台伺服器。Angular 建置產出靜態檔由 Nginx 提供；`/api/**` 反向代理至 Spring Boot。兩者部署流程獨立，可分別更新 |
| 建置產出 | 後端：可執行 jar；前端：`dist/` 靜態檔壓縮包 |

**資料備份**

| 層級 | 頻率 | 保留 | 說明 |
|---|---|---|---|
| 每日 | 每日 03:00 `mysqldump` | 14 份 | 日常還原 |
| 每週 | 每週日 | 8 份 | 涵蓋約兩個月 |
| 每月 | 每月 1 日 | 13 份 | 涵蓋逾一年，與稽核紀錄保留期（1 年）對齊 |

> v2.0 只有「每日 14 份」，與稽核紀錄保留 1 年不對稱——第 15 天起的稽核資料沒有任何備份可還原。v3.0 增加週／月層級。

**部署順序約定**：API 有破壞性變更時，先部署後端（保留舊欄位），再部署前端，最後於下一迭代移除舊欄位。避免前端已上線但後端尚未支援的空窗。

---

# 4. 功能需求

功能編號規則：`FR-模組編號`。每項功能對應「畫面功能示意圖 v3.0」中下表所列的畫面編號（並非一律同號，例如 FR-03 對應 S-03 與 S-04）。

## 功能模組總覽

| 編號 | 模組 | 對應畫面 | 軌別 | 對應痛點/成果 |
|---|---|---|---|---|
| FR-01 | 登入與權限 | S-01 | 共用 | — |
| FR-02 | 選品儀表板 | S-02 | 共用 | 成果：選品分數化 |
| FR-03 | 品項管理 | S-03 / S-04 | A、B | P-1 |
| FR-04 | 選品分數排行 | S-05 | A | P-1、成果：分數化 |
| FR-05 | 品項詳情與 AI 分析 | S-06 | A | P-1、成果：AI 賣點與風險摘要 |
| FR-06 | 趨勢分析 | S-07 | 共用 | P-2、成果：自動化趨勢監控 |
| FR-07 | AI 任務中心 | S-08 | 共用 | P-2 |
| FR-08 | 情境權重組設定 | S-09 | 共用 | P-3 |
| FR-09 | 資料匯入 | S-10 | 共用 | P-3 |
| FR-10 | 風險示警中心 | S-11 | A | 成果：自動化趨勢監控（G-3） |
| FR-11 | 採購決策與回饋閉環 | S-12 | A | P-1、G-5 決策可追溯 |
| FR-12 | 報表與匯出 | S-13 | 共用 | — |
| FR-13 | 系統設定 | S-14 | 共用 | — |
| FR-14 | 熱度資料層 | S-15 / S-16 | 共用 | P-2、成果：趨勢監控 |
| FR-15 | 權重校準閉環 | S-19 | 共用 | P-3、G-6 決策準確度可驗證 |
| FR-16 | 尋源探索 | S-17 / S-18 | B | P-2、P-3 |
| FR-17 | 節慶檔期與氣候基準 | S-20 | 共用 | P-3 |
| FR-18 | 稽核紀錄 | S-21 | 共用 | — |

> **FR-18 為 v3.0 新增**（畫面 S-21）。v2.0 將稽核紀錄放在 §FR-13 系統設定之下，造成 §2.1（BUYER_LEAD 可看）與 AC-13-3（僅 SYS_ADMIN 可進本模組）互相矛盾。v3.0 拆為獨立模組。

### 雙軌說明

決策模型分為兩軌，因為兩者問的不是同一個問題：

| | A 軌：既有品項評分 | B 軌：商機探索 |
|---|---|---|
| 決策問題 | 要不要進？進多少？ | 值不值得去找貨源？ |
| 輸入 | 已知商品（有報價、有規格） | 一個概念或關鍵字 |
| 可用因子 | 全部六項 | 僅熱度斜率、節慶時間窗、氣候適配 |
| 輸出 | 選品推薦分數 + 進貨建議 | 尋源優先序 + 時效落差評估 |
| 主要風險 | 庫存積壓 | 尋源太慢，熱度已過 |
| 執行頻率 | 每週批次 | 隨需、對話式 |

B 軌沒有毛利率、歷史轉換率、價格帶適配與自家評論。硬套 A 軌公式會讓所有 B 軌品項因資料缺失而永遠排在後段，等同功能失效。

**兩軌共用同一個 `product` 實體**（v3.0 裁決，見 §7.2）。B 軌品項也是一筆 `product`，只是 `track_type = 'B'`、成本與售價可為空。成案轉軌只需改 `track_type`，探索期間累積的熱度資料自然保留。

---

## FR-01 登入與權限

**畫面**：S-01 登入頁

| 項目 | 內容 |
|---|---|
| 輸入 | 帳號（email）、密碼 |
| 處理 | 密碼以 BCrypt（cost 10）驗證；連續失敗 5 次鎖定 15 分鐘；成功後發 JWT（access 2 小時 / refresh 7 天） |
| 輸出 | access token、refresh token、使用者基本資料與角色清單 |
| 例外 | 帳號停用 → 提示「此帳號已停用，請聯絡系統管理員」；密碼錯誤 → 不透露是帳號錯還是密碼錯 |

**Token 換發流程（v3.0 明確化）**

```
API 回 401 TOKEN_EXPIRED
      → 前端以 refresh token 呼叫 POST /auth/refresh
          → 成功：取得新 access token，自動重送原請求
          → 失敗（refresh 亦過期或已撤銷）：清除本機 token，導回登入頁
```

> v2.0 的 AC-01-2 寫「JWT 過期後呼叫 API 回 401，前端自動導回登入頁」，直接跳過了 refresh 機制，使 7 天的 refresh token 形同虛設。v3.0 修正。

**驗收準則**
- AC-01-1 正確帳密可登入並導向儀表板
- AC-01-2 access token 過期時，前端自動以 refresh token 換發並重送原請求；refresh token 亦失效時才導回登入頁
- AC-01-3 VIEWER 角色登入後，所有編輯類按鈕不可見，且直接呼叫寫入類 API 回 403
- AC-01-4 非 BUYER_LEAD 呼叫權重核准 API（§2.1 第 16 列）回 403
- AC-01-5 連續登入失敗 5 次後，第 6 次即使密碼正確亦回 423 並提示鎖定剩餘時間

---

## FR-02 選品儀表板

**畫面**：S-02 儀表板

**設計要點**：不設單一總榜。不同情境權重組算出的分數分母不同，放在同一張排行榜上比較大小不具統計意義。

**顯示內容**

| 區塊 | 內容 |
|---|---|
| 軌別切換 | A 軌（有供應商）／ B 軌（尋源中），預設 A 軌 |
| 熱度來源狀態列 | 各來源名稱、最後更新時間、可用性燈號、額度用量；點擊進入 S-16 |
| KPI 卡片 × 4 | 候選品項總數、A 級主推品項數（四榜去重）、高風險示警數、結案未回填數 |
| 分組排行分頁 | 話題爆款榜 / 節慶檔期榜 / 常態補貨榜 / 季節導向榜，各顯示 Top 5；欄位含情境判定標籤與人工覆寫標記 |
| 待回填結案 | 結案逾 7 天未回填之開團案，依逾期天數降冪排序 |
| B 軌摘要 | 尋源中候選 Top 3，**依時效落差升冪排序**（落差最小者最急迫），顯示時效落差天數；已淘汰者灰底顯示 |
| 快速標記入口 | 常駐「＋ 我看到一個」按鈕，導向 FR-14-1 |
| 週期切換 | 本週 / 上週 / 自訂區間，預設當前 ISO 週（以 Asia/Taipei 判定） |

**處理**

- KPI 與排行讀取 `product_score` 中該週期的最新有效快照，不即時重算
- 排行分頁依 `scene_type` 篩選，各榜獨立套用該榜的分級門檻（§5.6）
- **「A 級主推品項數」以 `product_id` 去重計算**。同一品項可同時出現在多張榜（§FR-04），若直接加總四榜的 A 級筆數會重複計數
- 熱度來源狀態即時查詢 `heat_source.availability`，不快取（該欄位由健康檢查排程寫入，見 §FR-14-2）
- 若該週期尚未執行評分，顯示空狀態：「本週尚未執行評分，前往 AI 任務中心啟動」＋ 按鈕

**驗收準則**
- AC-02-1 在 500 筆候選品項的資料量下，頁面首屏 2 秒內完成
- AC-02-2 點擊排行任一列可進入該品項詳情
- AC-02-3 切換週期後所有區塊同步更新
- AC-02-4 任一熱度來源不可用時，狀態列顯示黃燈且排行仍正常產出
- AC-02-5 結案逾 7 天未回填者必定出現在待回填區，且逾期天數計算正確
- AC-02-6 「A 級主推品項數」不因同品項上多榜而重複計數
- AC-02-7 B 軌摘要依時效落差升冪排序，非依熱度排序

---

## FR-03 品項管理

**畫面**：S-03 品項清單、S-04 品項新增/編輯

### FR-03-1 品項清單

| 項目 | 內容 |
|---|---|
| 顯示 | 品項名稱、類別、軌別、供應商、成本、售價、毛利率、最新分數、分級、狀態 |
| 篩選 | 類別、供應商、**軌別（A／B）**、**尋源狀態**、狀態、分數區間、是否有風險標記 |
| 排序 | 任一欄位可排序，預設依最新分數降冪 |
| 分頁 | 每頁 20／50／100 筆 |
| 批次操作 | 批次停用、批次指定類別、批次加入評分佇列 |

**軌別相關欄位**

| 欄位 | 值 | 說明 |
|---|---|---|
| `track_type` | `A` / `B` | A = 已有供應商與報價；B = 尋源中 |
| `sourcing_status` | `PENDING` 待評估 / `SOURCING` 尋源中 / `URGENT` 需加速 / `PROMOTED` 已成案 / `REJECTED` 已淘汰 | 僅 B 軌品項有值；A 軌為 NULL |

> v2.0 的 FR-03-1 只列四個狀態，漏了 `URGENT`（需加速），與 §FR-16-2 的五個狀態不一致。v3.0 補齊，並統一以英文 code 為準。

**B 軌品項的顯示規則**

- 不顯示成本、售價、毛利率（該三欄顯示「—」）
- 不顯示分數與分級（B 軌不產生選品分數，見 §5.8），改顯示時效落差天數
- 篩選「分數區間」對 B 軌無效，選取 B 軌時該篩選器自動停用

### FR-03-2 品項新增/編輯

| 項目 | 內容 |
|---|---|
| 必填（A 軌） | 品項名稱、類別、成本、建議售價 |
| 必填（B 軌） | 品項名稱、類別、關聯關鍵字 |
| 選填 | 供應商、規格、最小訂購量、效期天數、物流條件（常溫／冷藏／冷凍）、關聯關鍵字、適溫區間、節慶關聯度 |
| 自動計算 | 毛利率 =（售價 − 成本）／售價 |
| 處理 | 儲存後標記為「待評分」，於下次批次評分納入 |
| 例外 | A 軌成本 ≥ 售價 → 阻擋儲存並提示；同類別同名品項 → 警告但允許儲存 |

**供評分使用的欄位**

| 欄位 | 供哪個因子／規則使用 |
|---|---|
| 物流條件（常溫／冷藏／冷凍／易碎／易融化） | 物流風險扣分（§5.2.2） |
| 效期天數 | 庫存風險扣分（§5.2.2） |
| 最小訂購量 | 庫存風險扣分（§5.2.2） |
| 適溫區間（`ideal_temp_min` / `ideal_temp_max`） | 季節氣候適配因子（§5.2.1、§FR-17-2） |
| 建議售價 | 價格帶適配度因子（§5.2.1） |
| 節慶關聯度 | 節慶時間窗因子（§FR-17-1） |

> **適溫區間為 v3.0 新增欄位。** v2.0 的氣候因子原始值範例是「8 月均溫 29.4°C」，但沒有任何欄位記錄「這個品項適合什麼溫度」，因子無法計算。若品項未填適溫區間，則沿用所屬品類的預設值（`category_climate_profile`）；兩者皆無則該因子標為無資料。

**刪除**：一律為軟刪除（寫入 `deleted_at` 與 `deleted_by`）。軟刪除的品項不出現在清單、排行、評分批次與報表中，但既有的 `product_score`、`decision_record` 與稽核紀錄保留，可由稽核紀錄回溯。

**驗收準則**
- AC-03-1 清單篩選與排序組合皆可正常運作，含軌別與尋源狀態
- AC-03-2 新增品項後於下一次評分批次中出現
- AC-03-3 毛利率隨成本／售價輸入即時更新
- AC-03-4 軌別為 B 的品項不顯示成本、售價、毛利率、分數與分級，改顯示時效落差
- AC-03-5 刪除品項後不出現在任何清單與報表，但其歷史評分與決策紀錄仍可經稽核紀錄查得
- AC-03-6 B 軌品項可不填成本與售價即完成儲存

---

## FR-04 選品分數排行

**畫面**：S-05 選品分數排行

**設計要點**：加分項與扣分項分離顯示；排行依情境權重組分四榜；資料不足明確標示。

**顯示內容**

| 欄位 | 說明 |
|---|---|
| 加分 | 依情境權重加權後的加分小計，範圍 0–100 |
| 扣分 | 風險扣分小計，範圍 0–40。**內部一律以正值儲存**，UI 顯示時加負號（例：`−4`）；為 0 時顯示「—」 |
| 總分 | 加分小計 − 扣分小計，下限 0 |
| 分級 | A／B／C，各榜獨立門檻 |
| 因子組成 | 迷你長條，依序為熱度斜率、毛利、轉換率、價格帶適配、節慶窗、氣候；無資料者顯示灰底空條 |
| 情境判定 | 該品項套用的權重組；經人工覆寫者附標記 |
| 信心度 | 0–100，低於門檻時附警示標記（§5.9） |

> **扣分正負號約定（v3.0）**：v2.0 在四個地方用了三種寫法（「0 至 −40」「上限 40」「加分 − 扣分」「扣分小計 −4」），若扣分存負值則 `加分 − (−40)` 會變成加分。v3.0 統一：**`penalty_subtotal` 一律存 0 至 40 的正值**，公式為 `final_score = max(0, bonus_subtotal − penalty_subtotal)`，負號只出現在 UI。

**分頁**：話題爆款榜 / 節慶檔期榜 / 常態補貨榜 / **季節導向榜** / 高風險示警

> **四榜（v3.0 修正）**：v2.0 只有三榜，但 `scene_type` 有四個列舉值，被判定為 `SEASONAL`（季節導向型）的品項沒有任何一張榜可以顯示，也不會被儀表板 KPI 計入。v3.0 補上第四榜，四榜與四組情境權重一對一。

**處理**

- **同一品項可同時出現在多張榜**（例：月餅在七月同時符合節慶檔期與常態補貨）。實作方式見下方「多情境評分」
- 扣分明細以獨立卡片呈現，視覺上與加分區分離，強調不參與權重調整
- 支援以其他**權重版本**試算（不寫入資料庫），試算會套用該版本的完整四組權重與門檻

**多情境評分（v3.0 明確化）**

v2.0 一方面說「同一品項可同時出現在多張榜」，一方面 `product_score` 每列只有一個 `scene_type`、SceneClassifierAgent 也只輸出單一值，兩者互斥。v3.0 裁決如下：

| 項目 | 規則 |
|---|---|
| 評分粒度 | 一個品項在一個 period 內，**每個適用情境各產生一筆 `product_score`** |
| 適用情境的判定 | SceneClassifierAgent 輸出 `sceneType`（主情境）與 `alternativeScene`（次要情境，可為 null）。兩者皆視為適用情境 |
| 主情境標記 | 主情境的那筆 `product_score.is_primary = true`，其餘為 false |
| 唯一鍵 | `uk_score(product_id, period, scene_type)` |
| 品項詳情顯示 | §FR-05 顯示主情境那筆；可切換檢視次要情境的分數 |
| KPI 與去重 | 儀表板的品項數一律以 `product_id` 去重 |
| 決策綁定 | 決策綁定的是主情境那筆 score（§FR-11-1） |
| 人工覆寫 | 覆寫後，被指定的情境成為主情境；原主情境降為次要，不刪除 |

**硬規則**

- **扣分達 20 分（含）以上者，分級最高只給 B**，且強制進入 FR-10 風險示警清單
  - 設計理由：避免高分掩蓋致命風險。評論風險單項上限即為 20 分，因此「負評率超過品類門檻且集中於品質／食安／物流破損」的品項必然被壓級——這是刻意設計，食安類問題不應因社群熱度高而被蓋過
- 關鍵因子無資料時標示灰底，**不產生扣分**。新品項與 B 軌品項天生缺資料，若以扣分處理會使其永遠無法進入排行前段

**驗收準則**
- AC-04-1 加分與扣分分欄顯示，總分等於加分小計減扣分小計
- AC-04-2 扣分 ≥ 20 者分級不得為 A
- AC-04-3 無資料因子顯示灰底，且**不產生扣分**（該因子的權重依 §5.7 分攤給其餘因子，此為刻意的分數影響，非缺陷）
- AC-04-4 切換權重版本試算不寫入資料庫，且可還原至現行版本
- AC-04-5 四榜可各自獨立設定 A／B 分級門檻，設定值綁定權重版本
- AC-04-6 同一品項在同一 period 內若有多個適用情境，四榜可同時出現該品項，且各榜顯示的分數為該情境權重下的分數
- AC-04-7 `penalty_subtotal` 於資料庫中為正值，於 UI 顯示為負號

---

## FR-05 品項詳情與 AI 分析

**畫面**：S-06 品項詳情與 AI 分析

系統核心畫面。情境判定是 AI 唯一會影響分數的位置，因此必須顯示理由，且必須可由人工覆寫。

### 區塊 A：情境判定

| 項目 | 內容 |
|---|---|
| 顯示 | 主情境的權重組名稱、信心值、判定理由、依據訊號清單；若有次要情境亦一併列出 |
| 操作 | 「改用其他權重組」按鈕，開啟選單（四組擇一） |
| 覆寫處理 | 覆寫時理由為必填；紀錄寫入 `scene_classification_log`；立即以新權重組重算分數並改為主情境 |
| 例外 | 信心值 < 0.5 或 JSON Schema 驗證失敗 → 退回「常態補貨型」並標示「AI 判定失敗，使用預設權重」 |

**兩個信心門檻的用途（v3.0 釐清）**

| 門檻 | 用途 |
|---|---|
| `confidence < 0.5` | **採用門檻**。低於此值不採信 AI 判定，改用預設權重組 `REPLENISHMENT` |
| `confidence < 0.7` | **計分門檻**。判定仍被採用，但該筆分數的信心度扣 10 分（§5.9） |

兩者用途不同，並非衝突。0.5–0.7 之間的判定會被採用但標示為低信心。

### 區塊 B：品項基本資料

名稱、類別、軌別、供應商、成本、建議售價、毛利率、規格、物流條件、效期天數、適溫區間。

### 區塊 C：加分因子

每項顯示因子名稱、原始值、同品類百分位、長條圖。以下為 §5.5 計算範例（日式抹茶夾心餅乾，話題爆款型，權重版本 v3，評分日 2026-07-27）的實際值：

| 因子 | 原始值 | 同品類百分位 | 權重 | 貢獻 |
|---|---|---|---|---|
| 社群熱度斜率 | +340% / 7d | 96 | 50% | 48.00 |
| 毛利率 | 38% | 88 | 10% | 8.80 |
| 歷史轉換率 | 4.2% | 90 | 8% | 7.20 |
| 價格帶適配度 | 0.68（售價 149 元 vs 零食類客群價格帶組成，§5.2.4） | 82 | 7% | 5.74 |
| 節慶時間窗 | 0.60（中秋前 60 天 × 關聯度 0.60 × 窗權重 1.0） | 85 | 15% | 12.75 |
| 季節氣候適配 | 0.717（8 月均溫 29.4°C vs 適溫 18–26°C） | 44 | 10% | 4.40 |
| **加分小計** | | | **100%** | **86.89** |

> **本範例為全文的黃金案例**，§5.5、§8.3、§11 單元測試皆使用同一組數字。v2.0 在這三處給了三組互相矛盾的數字（91／90／68.2），且 §8.3 的權重加總只有 0.90，違反自己的 AC-08-1。

### 區塊 D：風險扣分（獨立區塊）

以紅色系與加分區視覺分離。**即使扣分為 0 仍需列出該項**，讓使用者確認系統確實檢查過。

| 扣分項 | 顯示內容 | 本例判定 | 扣分 |
|---|---|---|---|
| 評論風險 | 負評率、品類門檻、命中之負評主題 | 負評率 10%，未達零食類門檻 15% | 0 |
| 物流風險 | 命中之物流條件 | 夏季高溫、易融化 | 4 |
| 庫存風險 | 效期／季節性／MOQ 判定結果 | 效期 180 天、MOQ 200，皆未觸發 | 0 |
| **扣分小計** | | | **4.00** |

### 區塊 E：分數組成

以公式形式呈現，並標示權重版本與情境原型：

```
加分 86.9  −  扣分 4.0  =  總分 82.9      分級 B（話題爆款榜門檻 A ≥ 85）
權重版本 v3「2026 夏季｜基準」 · 情境 話題爆款型（AI 信心 0.82）· 分數信心度 86
```

畫面同時提供一行對照說明，讓使用者理解時間窗與氣候因子的影響：

> 若同一品項於 11 月評估（氣候適配百分位 82、無夏季物流扣分），加分小計為 90.69、扣分 0、總分 90.7 → **A 級**。季節與檔期會直接改變分數，這是刻意設計。

### 區塊 F：AI 分析

| 項目 | 內容 |
|---|---|
| 賣點 | 條列 2–4 點，每點附引用評論則數 |
| 潛在風險 | 條列 2–4 點；已計入扣分者標示「已計入扣分」 |
| 進貨建議 | 一段可執行文字，結合分數、情境與檔期 |
| 評論情緒分佈 | 正面／中立／負面百分比 + 負評主題拆解 + 門檻判定結果 |
| 標示 | 固定顯示「AI 產出僅供參考，最終決策由採購人員負責」 |

> **賣點與潛在風險兩者皆由 `ProductInsightAgent` 產出**（§6.3 Agent 3）。v2.0 移除了 RiskAgent，而 ReviewRiskAgent 只輸出主題分類與比率、不產生文字，導致「潛在風險條列 2–4 點」與目標 G-4 沒有任何 Agent 負責。v3.0 將賣點與風險文字合併由同一個 Agent 產出（同一批評論輸入，一次呼叫產兩種輸出，同時省下配額）。

### 區塊 G：決策操作

採納 / 觀察 / 淘汰 ＋ 建議首批數量 ＋ 理由文字（詳見 FR-11）。依 §2.1 第 11 列控制可見性。

**驗收準則**
- AC-05-1 情境判定須顯示信心值與至少一項依據訊號
- AC-05-2 人工覆寫情境時理由為必填，且紀錄可查
- AC-05-3 每個加分因子皆顯示原始值與同品類百分位，共六項
- AC-05-4 風險扣分區即使全為 0 仍完整列出三個項目
- AC-05-5 分數組成以加分減扣分的形式顯示，不直接呈現單一數字
- AC-05-6 AI 區塊與系統計算區塊視覺上明確分離
- AC-05-7 有次要情境時可切換檢視該情境下的分數，切換不寫入資料庫

---

## FR-06 趨勢分析

**畫面**：S-07 趨勢分析

**設計要點**：多來源合成；以斜率為主，非絕對熱度值。

**設計理由**：爆款偵測本質是抓一階導數。等絕對熱度值衝高時，通常已進入大眾期，團購想吃的上升期已過。

**顯示內容**

| 區塊 | 內容 |
|---|---|
| 曲線階段判定 | 上升期（RISING）／高原期（PLATEAU）／衰退期（DECLINING），附階段週數與預估剩餘壽命 |
| 熱度曲線 | 近 90 天合成熱度，階段以底色區隔 |
| 各來源明細 | 來源名稱、7 日斜率、30 日斜率、同源百分位、合成權重、可用性狀態 |
| 合成邏輯說明 | 顯示本次實際採用的合成比例（降級後會與設定值不同） |
| 雙窗口說明 | 7 日與 30 日背離時的判讀方式 |

**熱度階段的統一詞彙（v3.0）**

v2.0 有三套詞彙：`trend_keyword.lifecycle` 五值、`sourcing_candidate.heat_stage` 三值、以及 FR-06 內文的三段中文。v3.0 統一為三值，並廢除 `trend_keyword.lifecycle` 欄位（其資訊由 `heat_composite_daily.stage` 提供）。

| Code | 中文 | 判定 |
|---|---|---|
| `RISING` | 上升期 | 連續 3 週合成熱度成長，且 `slope_30d > 0` |
| `PLATEAU` | 高原期 | `slope_30d` 落在 ±10% 內 |
| `DECLINING` | 衰退期 | `slope_30d < −10%` |

**處理**

1. 各來源量級不可比（Threads 的貼文則數 vs Trends 的相對指數），因此**先在各來源內做百分位正規化，再依合成權重加總**
2. 斜率取 7 日與 30 日雙窗口。兩者背離（7 日轉負、30 日仍正）時標記為「可能見頂」
3. 任一來源不可用時，其合成權重歸零，其餘來源按比例重新正規化
4. 每日合成結果寫入 `heat_composite_daily`（v3.0 新增表），含合成值、雙窗口斜率、**本次實際採用的合成權重**與階段判定
5. 曲線階段與剩餘壽命由 TrendInterpreterAgent 判定，作為 FR-16 時效落差計算的輸入

> **`heat_composite_daily` 為 v3.0 新增。** v2.0 只有各來源的 `heat_reading`，合成後的值沒有任何儲存位置，導致「近 90 天合成熱度曲線」「7 日／30 日斜率」「本次實際採用的合成比例」三項顯示需求都無資料來源。

**資料來源界定**

| 來源 | 取得方式 | 頻率 | 粒度 | 備註 |
|---|---|---|---|---|
| Threads | 官方 API `/keyword_search` | 每日 | 關鍵字級 | 需通過 Meta 審核；額度 2,200 查詢／24 小時 |
| Google Trends | 非官方 API | 每日 | 關鍵字級 | 穩定性列為待確認事項（附錄 A 第 2 項） |
| Instagram | Graph API `ig_hashtag_search` | 每週 | **品類級** | 每帳號 7 日限 30 個標籤 |
| 人工標記 | 系統內部（FR-14-1） | 隨需 | 關鍵字級或品項級 | 涵蓋 FB／TikTok／小紅書 |

**品類級來源的處理（v3.0 補充）**

Instagram 因額度限制只能做品類級（每週 30 個標籤，無法覆蓋數百個關鍵字）。v2.0 未說明品類級資料如何落入以 `keyword_id` 為維度的 `heat_reading`。v3.0 規則：

- `heat_reading` 增加 `category_id` 欄位，與 `keyword_id` **二擇一非空**
- 品類級讀值在合成時，套用給該品類下的所有關鍵字，但**合成權重乘以 0.5 的粒度折扣**（品類級訊號對單一品項的指向性較弱）
- UI 於各來源明細中標示「品類級」，讓使用者知道該數值不是針對此關鍵字量測的

**明確排除**：系統不建置網路爬蟲，理由見附錄 C。此排除同時適用於 B 軌的 MCP 工具（§6.3 Agent 6 與 §FR-16-1 的白名單）。

**驗收準則**
- AC-06-1 各來源分列顯示斜率與百分位，不合併為單一黑盒數值
- AC-06-2 任一來源降級時，合成比例自動重算並於畫面顯示實際採用值
- AC-06-3 曲線階段判定須輸出階段名稱、階段週數與預估剩餘壽命
- AC-06-4 7 日與 30 日斜率背離時顯示「可能見頂」標記
- AC-06-5 品類級來源於各來源明細中明確標示，且其合成權重已套用粒度折扣

---

## FR-07 AI 任務中心

**畫面**：S-08 AI 任務中心

| 項目 | 內容 |
|---|---|
| 任務類型 | 情境判定、評論風險分析、賣點與風險萃取、進貨建議、趨勢解讀、尋源探索、權重校準 |
| 顯示 | 任務名稱、目標範圍、狀態、進度、**已用請求數／配額百分比**、快取命中數、錯誤筆數 |
| 操作 | 手動啟動、取消、重跑失敗項 |
| 排程 | 見 §5.10 |

**度量單位：請求數，不是金額（v3.0 修正）**

本專案一律使用免費模型（§6.7.1），金額永遠是 0。v2.0 卻在 FR-07、`ai_insight.cost_usd`、`ai_task.total_cost_usd`、`GET /ai/usage` 都以金額為主要度量，畫面上會是一整排的 `$0.00000`，無法用於任何判斷。

v3.0 將主要度量改為**請求數與配額百分比**。金額欄位保留但降為次要顯示（未來若改用付費模型可直接啟用），並於資料表中維持 `cost_usd` 欄位。

**預算池（以每日請求數計）**

| 預算池 | 用途 | 每日配額比例 | 達 100% 的行為 |
|---|---|---|---|
| A 軌批次 | 情境判定、評論風險分析、賣點與風險萃取、進貨建議、趨勢解讀 | 70% | 停止送出，未完成品項列入待重跑清單，隔日續跑；畫面標示「本輪部分品項沿用上期結果」 |
| B 軌探索 | 尋源探索（經 opencode + MCP） | 20% | 停用探索功能，並提示額度重置時間；不影響 A 軌 |
| 重試與臨時任務 | 429 退避重試、單品項手動重算、季度校準 | 10% | 停止送出，提示稍後再試 |

> **超額行為統一（v3.0）**：v2.0 的 §FR-07 說 A 軌超額時「改用上期快取結果並標示」，§6.7.3 說「停止送出，保留待重跑清單，隔日續跑」，兩者不同。v3.0 合併為同一行為：停止送出 + 待重跑清單 + 畫面標示沿用上期結果。
>
> **校準預算歸屬統一（v3.0）**：v2.0 的 §FR-07 說校準「獨立計費，單獨列示」（第四個池），§6.7.3 說「不另設配額」，附錄 B 的環境變數只有三個池。v3.0 統一為三池，校準併入「重試與臨時任務」池——季度執行一次、量小，不值得獨立配額；但需避開全量評分時段（§5.10）。

**額度重置時間的取得**

OpenRouter 於 429 回應中提供的 rate limit 標頭（如 `X-RateLimit-Reset`）為主要來源。若該標頭不存在，退而以「當日 UTC 00:00」推估並於畫面標示為估計值。W2 基準測試須確認實際標頭格式（§6.7.5）。

**驗收準則**
- AC-07-1 任務執行中可即時查看進度、已用請求數與配額百分比
- AC-07-2 B 軌預算耗盡不得影響 A 軌批次評分
- AC-07-3 相同請求在快取有效期內命中快取，不佔用配額；命中數於任務詳情顯示
- AC-07-4 A 軌配額耗盡時，未完成品項進入待重跑清單，且隔日排程自動續跑
- AC-07-5 畫面主要度量為請求數，金額為次要顯示

---

## FR-08 情境權重組設定

**畫面**：S-09 情境權重組

**設計要點**：四組具名權重 + 版本管理。AI 判斷情境後選擇其中一組，但不得自行填寫權重數值。

**設計理由**：若允許 AI 自由設定權重，它會傾向給「該品項表現好的因子」較高權重，導致所有品項分數偏高、鑑別力歸零。且連續數值無法人工審核、不可重現。將 AI 的輸出限制為離散選擇，才能同時保有情境適應性與可稽核性。

**權重組定義（v3.0：六因子）**

| 權重組 | Code | 熱度斜率 | 毛利率 | 轉換率 | 價格帶適配 | 節慶窗 | 氣候 | 合計 | 適用情境 |
|---|---|---|---|---|---|---|---|---|---|
| 話題爆款型 | `VIRAL` | 50% | 10% | 8% | 7% | 15% | 10% | 100% | 社群竄升、生命週期短 |
| 節慶檔期型 | `FESTIVAL` | 18% | 18% | 17% | 7% | 30% | 10% | 100% | 明確對應檔期 |
| 常態補貨型 | `REPLENISHMENT` | 13% | 30% | 30% | 12% | 5% | 10% | 100% | 穩定需求、重複開團 |
| 季節導向型 | `SEASONAL` | 18% | 22% | 18% | 7% | 5% | 30% | 100% | 強季節性商品 |

> **納入價格帶適配度（v3.0 修正）**。v2.0 的 §5.2.1 定義了 7 個加分因子，但四組權重只有 5 欄，`price_fit`（價格帶適配度）與 `heat_volume`（熱度絕對量級）都進不了評分路徑。
>
> 這對 `price_fit` 尤其嚴重：它是全系統**唯一**對應痛點 P-3「客群未對齊」的因子，而 §4 總覽表卻把 FR-08 標為對應 P-3。不納入權重，等於 P-3 在評分引擎中完全沒有被處理。v3.0 將其正式納入，並重新分配各組權重至 100%。
>
> `heat_volume` 則改為**門檻條件而非加權因子**（§5.2.1）：熱度量級未達下限時，熱度斜率因子不計分。這才是它原本的用意（「防止 3 則變 9 則被誤判為爆款」），做成加權因子反而會讓大品類永遠贏過小品類。

> 上表為初始建議值，需與客戶共同校準（附錄 A 第 7 項）。

**各榜分級門檻**

門檻依**榜（情境）**設定，不依品類設定。

| 榜 | A 級門檻 | B 級門檻 |
|---|---|---|
| 話題爆款榜 | ≥ 85 | 70 – 84 |
| 節慶檔期榜 | ≥ 85 | 70 – 84 |
| 常態補貨榜 | ≥ 80 | 65 – 79 |
| 季節導向榜 | ≥ 82 | 68 – 81 |

> **門檻維度統一為「榜」（v3.0 裁決）**。v2.0 的 §FR-13 說「各**類別**分級門檻」，AC-04-5 說「各**榜**可獨立設定」，兩者並存需要 category × scene 的二維矩陣，而且沒有任何資料表承載門檻值。v3.0 選定單一維度（榜），並新增 `grade_threshold` 表，**綁定 `weight_version_id`**——門檻與權重同屬評分規則，一起版本化才能滿足 G-5 的可重現性。
>
> 初始門檻值列為待客戶確認（附錄 A 第 3 項）。

**版本管理**

| 項目 | 內容 |
|---|---|
| 建立 | BUYER_LEAD 可建立草稿版本，編輯四組權重與四榜門檻 |
| 核准 | 僅 BUYER_LEAD 可核准生效，需指定生效日期 |
| 不可變 | 生效後的版本不可修改，只能建立新版本 |
| 追溯 | 每筆評分紀錄 `weight_version_id`，可還原當時的權重設定與門檻 |
| 生效切換 | 核准時將舊版本標為 `RETIRED`、`is_current = false`，新版本 `is_current = true`，並觸發全量重新評分 |

**風險扣分規則**（不隸屬任何權重組，固定生效，不隨權重版本變動）

| 規則 | 最大扣分 | 可調整者 |
|---|---|---|
| 評論風險 | 20 | SYS_ADMIN 可調門檻，不可停用 |
| 物流風險 | 10 | 同上 |
| 庫存風險 | 10 | 同上 |
| **合計上限** | **40** | |

**驗收準則**
- AC-08-1 四組權重各自加總須為 100%（六個因子），否則不可儲存
- AC-08-2 生效中的版本不可編輯
- AC-08-3 非 BUYER_LEAD 不可核准版本
- AC-08-4 每筆評分紀錄可回溯其使用的權重版本與該版本的分級門檻
- AC-08-5 風險扣分規則在任一權重組下皆生效，且不隨權重版本變動
- AC-08-6 四榜門檻隨權重版本一併儲存與核准

---

## FR-09 資料匯入

**畫面**：S-10 資料匯入

| 項目 | 內容 |
|---|---|
| 支援格式 | CSV、XLSX |
| 資料類型 | 歷史銷售紀錄、商品評論、**會員輪廓（客群區隔）**、品項主檔 |
| 流程 | 上傳 → 欄位對應 → 預覽前 20 筆 → 驗證 → 匯入 → 產出結果報告 |
| 驗證 | 必填欄位檢查、型別檢查、重複值檢查、參照完整性檢查 |
| 錯誤處理 | 逐列記錄錯誤原因，可下載錯誤清單；部分成功時仍寫入正確列 |

**檔案大小規則（v3.0 明確化）**

| 項目 | 值 | 行為 |
|---|---|---|
| 同步處理門檻 | ≤ 2MB 且 ≤ 5,000 列 | 上傳後即時驗證與匯入 |
| 非同步處理 | > 2MB 或 > 5,000 列 | 轉為背景任務，完成後於通知區提示 |
| 硬上限 | 50MB 或 200,000 列 | **超過則拒收**，提示分批上傳 |

> v2.0 寫「上限 單檔 10MB／50,000 列；超過改採非同步處理」，語意矛盾——若超過還能處理，那就不是上限。v3.0 拆為三段：同步門檻、非同步門檻、硬上限。

**匯入完成後的重算範圍（v3.0 明確化）**

匯入觸發的重算**只重算因子與扣分，不重跑任何 Agent**。情境判定沿用該品項最近一次的判定結果；若從未判定過，使用預設 `REPLENISHMENT` 並標示。

> 理由：大量匯入可在幾分鐘內耗盡當日 LLM 配額（§6.7.3）。因子與扣分皆為純計算，不需 LLM，可即時完成。

**驗收準則**
- AC-09-1 欄位對應設定可儲存為範本重複使用
- AC-09-2 匯入失敗列可下載並修正後重新上傳
- AC-09-3 匯入完成後自動觸發相關品項重新計算因子與扣分，**且不消耗 LLM 配額**
- AC-09-4 超過硬上限的檔案被拒收，並提示可接受的大小
- AC-09-5 超過同步門檻的檔案轉為背景任務，前端可查詢進度

---

## FR-10 風險示警中心

**畫面**：S-11 風險示警中心

| 項目 | 內容 |
|---|---|
| 顯示 | 品項、示警類型、嚴重度、觸發值、觸發時間、處理狀態 |
| 操作 | 標記已處理、忽略（需填理由）、直接跳轉品項詳情 |
| 篩選 | 嚴重度、類型、處理狀態、類別 |

### FR-10-1 示警類型與觸發規則（v3.0 補齊）

v2.0 列了四種示警來源（風險扣分命中、熱度急墜、季節性不匹配、供應商異常），但沒有任何一種定義了觸發式與門檻，`risk_alert.risk_type` 也是自由字串沒有列舉。v3.0 補齊如下。所有門檻皆可由 SYS_ADMIN 於 §FR-13 調整。

| `risk_type` | 中文 | 觸發規則 | 預設嚴重度 |
|---|---|---|---|
| `REVIEW_RISK` | 評論風險 | 評論風險扣分 > 0 時開立；扣分達 20 時升為 HIGH | 扣分 ≥ 20 → HIGH；否則 MEDIUM |
| `LOGISTICS_RISK` | 物流風險 | 物流風險扣分 > 0 | MEDIUM |
| `INVENTORY_RISK` | 庫存風險 | 庫存風險扣分 > 0 | MEDIUM |
| `PENALTY_CAP` | 扣分壓級 | 扣分小計 ≥ 20（觸發 §FR-04 硬規則） | HIGH |
| `HEAT_CRASH` | 熱度急墜 | `slope_7d ≤ −40%` 且該品項狀態為 `ADOPTED` 或 `LISTED` | HIGH |
| `HEAT_SURGE` | 熱度異常竄升 | `slope_7d` 落於同品類當日 `slope_7d` 分佈的 P95 以上，且熱度量級達下限（§5.2.1） | MEDIUM |
| `SEASON_MISMATCH` | 季節不匹配 | 季節氣候適配百分位 < 20，且該品項狀態為 `ADOPTED` 或 `LISTED` | MEDIUM |
| `FESTIVAL_WINDOW_CLOSING` | 檔期窗即將關閉 | 節慶時間窗權重將於 7 日內由 1.0 降為 0.5 或 0，且該品項尚未建立決策 | LOW |
| `LOW_CONFIDENCE` | 低信心分數 | 信心度 < 50（§5.9） | LOW |
| `DATA_INSUFFICIENT`（v3.0 新增） | 資料不足無法評分 | `product.last_scoring_status = 'INSUFFICIENT_DATA'`（§5.7：六項加分因子缺 4 項以上） | MEDIUM |

**「供應商異常」移出本階段範圍。** v2.0 將其列為示警來源，但 `supplier` 表沒有任何可判定「異常」的欄位（只有名稱、聯絡人、電話、備註），也沒有任何資料源提供交期、到貨率或品質退件資訊。此項移入 §1.4 Out of Scope，待 ERP／進貨系統串接後再納入。

### FR-10-2 熱度異常示警（對應 G-3）

目標 G-3 承諾「熱度異常上升／下降於 24 小時內示警」。實作對應如下：

| 項目 | 規則 |
|---|---|
| 偵測時機 | 每日 06:30，緊接熱度採集排程（06:00）之後執行 |
| 偵測範圍 | 所有 `enabled` 的關鍵字所關聯的品項 |
| 上升判定 | `HEAT_SURGE`（見上表） |
| 下降判定 | `HEAT_CRASH`（見上表） |
| 時效保證 | 熱度資料於每日 06:00 更新，示警於 06:30 產生，故任何異常自資料更新起 30 分鐘內、自實際發生起 24 小時內必被偵測 |
| 去重 | 同一品項同一 `risk_type` 於 7 日內已有 `OPEN` 狀態的示警時，不重複開立，改更新其 `trigger_value` 與 `detected_at` |

### FR-10-3 處理與門檻

**門檻變更的生效方式（v3.0 統一）**

v2.0 對此有三種說法：§5.10「全量重新計算扣分」、AC-10-3「立即生效」、AC-13-2「下次評分生效」。v3.0 統一為：

> SYS_ADMIN 變更風險門檻後，**立即觸發一個背景的全量扣分重算任務**。重算期間排行與詳情繼續顯示上一版分數，並於頁首標示「風險門檻已變更，分數重算中（進度 x%）」。重算完成後畫面自動更新，並重跑風險規則。此重算不消耗 LLM 配額（扣分為純計算）。

**驗收準則**
- AC-10-1 每次評分後自動重跑風險規則
- AC-10-2 已忽略的風險不再出現在預設清單，但可用篩選查看
- AC-10-3 風險規則門檻由 SYS_ADMIN 變更後，立即觸發背景全量扣分重算，重算期間畫面標示狀態，完成後自動更新
- AC-10-4 扣分 ≥ 20 之品項必定出現於本清單（`PENALTY_CAP`）
- AC-10-5 熱度急墜與異常竄升於每日 06:30 排程中被偵測並開立示警
- AC-10-6 同一品項同一類型的示警在 7 日內不重複開立

---

## FR-11 採購決策與回饋閉環

**畫面**：S-12 決策與回饋閉環

整套「越用越準」的基礎在此模組。沒有結案回填就沒有標籤資料，FR-15 的權重校準完全不成立。

### FR-11-1 建立決策

| 項目 | 內容 |
|---|---|
| 輸入 | 決策類型（採納 `ADOPT`／觀察 `WATCH`／淘汰 `REJECT`）、建議首批數量、預計上架日、理由文字 |
| 處理 | 綁定該品項該 period 的**主情境評分**（`is_primary = true` 那筆）；自動產生決策快照 |
| 例外 | 無評分快照時不可建立決策；未採納 AI 建議時理由為必填 |

**AI 建議與決策的列舉對齊（v3.0 修正）**

v2.0 中 AI 的 `recommendation.action` 是 `TRIAL` 等值，決策是 `ADOPT/WATCH/REJECT`，兩套列舉沒有映射規則，`decision_record.followed_ai` 無法判定。v3.0 讓 AI 直接輸出與決策相同的三值：

| AI 輸出 `action` | 意義 | `followed_ai` 判定 |
|---|---|---|
| `ADOPT` | 建議採納 | 決策 = `ADOPT` 時為 true |
| `WATCH` | 建議先觀察 | 決策 = `WATCH` 時為 true |
| `REJECT` | 建議不引進 | 決策 = `REJECT` 時為 true |

數量建議同時修正：AI 輸出 `qtyMin` 與 `qtyMax` 兩個整數（`quantityText` 另存自由文字供顯示），使其可與 `first_order_qty` 直接比對。v2.0 的 `"首批 200–300 件"` 是自由字串，無法比對也無法帶入表單。

**決策快照內容**（自動產生，不需人工填寫）

| 欄位 | 說明 |
|---|---|
| `score_id` | 綁定的 `product_score`，其因子明細可由 `score_factor` 完整還原 |
| `weight_version_id` | 當時生效的權重版本（含四榜門檻） |
| `scene_type` + `ai_confidence` | AI 判定的情境原型 |
| `scene_overridden_by` / `override_reason` | 若經人工覆寫，記錄覆寫者與理由 |
| `source_availability` | **當時各熱度來源的狀態與實際採用的合成權重**（JSON） |
| `applied_thresholds` | 當時該榜的 A／B 門檻（JSON） |
| 決策人、決策時間 | |

> **`campaign_snapshot` 只存無法從既有資料還原的欄位（v3.0 修正）。** v2.0 的 `campaign_snapshot` 把分數、分級、加減分小計、因子值全部再存一份 JSON，與 `product_score` + `score_factor` 完全重複——而 §5.10 已保證 score 永不覆寫、決策也已用 `score_id` 綁定。v3.0 精簡為只存三類無法還原的資訊：來源可用性、實際合成權重、當時的分級門檻。其餘一律由 `score_id` join 取得。

快照於建立當下鎖定，之後權重改版不影響已建立的快照。

### FR-11-2 結案與回填

**結案（v3.0 新增流程步驟）**

回填提醒需要一個起算點。v2.0 的 §FR-02 與 AC-11-3 都以「結案後 7 日」為判準，但 `decision_record` 只有 `expected_list_date`，沒有任何欄位承載結案時間，逾期天數算不出來。

v3.0 補上明確的結案動作：

| 項目 | 內容 |
|---|---|
| 觸發者 | 採購專員或資料管理員（§2.1 第 12 列） |
| 動作 | 在決策詳情頁按「標記結案」，填入結案日期（預設今日，可回填過去日期） |
| 寫入 | `decision_record.campaign_end_date` |
| 前提 | 決策類型為 `ADOPT`（`WATCH` 與 `REJECT` 不開團，無結案概念） |
| 提醒 | 結案日 + 7 日仍未回填者，進入 §FR-02 待回填區，逾期天數 = 今日 − 結案日 − 7 |

**回填表單。設計原則：欄位壓在 5 個以內。** 欄位一多就沒人填，沒人填整個校準機制就是空的。

| 欄位 | 型態 | 必填 | 說明 |
|---|---|---|---|
| 實際銷量 | 整數 | ✓ | 對照當初的 `first_order_qty` |
| 售罄狀況 | 單選 | ✓ | `EARLY_SELLOUT` 提前售罄／`ON_TIME` 如期完售／`BELOW_TARGET` 未達標／`SLOW` 滯銷 |
| 退貨／客訴率 | 比率（0–1，四位小數） | — | 可由訂單系統帶入則自動化 |
| 實現毛利率 | 比率（0–1，四位小數） | ✓ | 對照預估毛利率 |
| 事後註記 | 單選 + 選填文字 | — | `FASTER_THAN_EXPECTED` 爆得比預期快／`HEAT_PASSED` 熱度已過／`QUALITY_ISSUE` 品質問題／`LOGISTICS_ISSUE` 物流出狀況／`OTHER` |

> **`realized_margin` 明確為「率」（v3.0）**。v2.0 的欄位名與型別 `DECIMAL(5,2)` 未說明是金額還是率；若是金額則上限 999.99 明顯不足。v3.0 更名為 `realized_margin_rate`，型別統一為 `DECIMAL(5,4)`，與 `return_rate` 一致。

**流程強制點**：結案後 7 日未回填，於 FR-02 儀表板待辦區顯示提醒。不硬鎖功能，但需有摩擦力。

> 這是營運端的承諾，非技術問題。建議於合約或專案章程中明列為客戶責任事項（附錄 A 第 9 項、風險 R-10）。

### FR-11-3 準確度分析

| 指標 | 定義 |
|---|---|
| 分數與銷量相關係數 | 選品分數與實際銷量的皮爾森相關（僅計主情境評分） |
| A 級品項達標率 | A 級品項中售罄狀況為 `EARLY_SELLOUT` 或 `ON_TIME` 者之比例 |
| 已回填樣本數 | 作為統計效度的前提指標，須顯著呈現 |
| 情境判定覆寫率 | 人工覆寫比例，過高代表判定規則需調整（風險 R-13） |
| AI 建議採納率 | `followed_ai = true` 的比例 |

**必要揭露**：樣本數未達 `CALIBRATION_MIN_SAMPLE`（預設 200）筆時，畫面須顯示「樣本數不足，本數據僅供觀察趨勢，不足以支持權重調整」，且該警示不可關閉。

**驗收準則**
- AC-11-1 決策必須綁定一筆主情境評分快照，無快照時不可建立決策
- AC-11-2 未採納 AI 建議時，理由為必填
- AC-11-3 結案滿 7 天未回填者，於儀表板待辦區提示，逾期天數計算正確
- AC-11-4 準確度報表可依時間區間、類別、決策者篩選
- AC-11-5 樣本數 < 200 時必須顯示效度警示，且無法關閉
- AC-11-6 決策快照建立後不因權重改版而變動
- AC-11-7 `followed_ai` 可由 AI 的 `action` 與實際決策直接比對得出
- AC-11-8 決策類型為 `WATCH` 或 `REJECT` 時，不提供結案與回填入口

---

## FR-12 報表與匯出

**畫面**：S-13 報表中心

| 報表 | 內容 | 格式 |
|---|---|---|
| 週選品建議 | **四榜各 Top 10**、AI 建議摘要、風險示警清單 | PDF |
| 品項評分明細 | 全品項六因子原始值、百分位與加減分明細 | XLSX |
| 決策準確度 | FR-11-3 的五項指標與趨勢圖 | PDF |
| 尋源優先序 | B 軌候選清單與時效落差 | XLSX |
| 權重校準紀錄 | 歷次校準建議與核准結果 | PDF |

報表產生為背景任務，任務狀態存於 `report_job`（§7.2）。

**驗收準則**
- AC-12-1 PDF 中文字型正確顯示（內嵌 Noto Sans TC）
- AC-12-2 匯出檔名含日期與篩選條件摘要
- AC-12-3 超過 5,000 列採非同步產生，完成後於通知區提供下載
- AC-12-4 週選品建議報表須分四榜呈現，不合併為單一排行
- AC-12-5 所有匯出動作寫入 `audit_log`

---

## FR-13 系統設定

**畫面**：S-14 系統設定

僅 SYS_ADMIN 可存取（§2.1 第 23／24 列）。

| 分頁 | 內容 | 對應權限列 |
|---|---|---|
| 使用者管理 | 帳號建立、停用、角色指派 | 23 |
| 類別管理 | 品類樹狀結構、品類前置天數入口、品類氣候基準入口 | 20 |
| AI 設定 | 模型別名對應、三個預算池上限、快取天數 | 24 |
| 風險規則 | 三類扣分規則的門檻與扣分上限、FR-10-1 各示警類型的門檻 | 18 |
| 熱度來源 | 導向 FR-14-2 | 19 |
| 節慶檔期 | 導向 FR-17 | 20 |

> **稽核紀錄已移出本模組**，獨立為 FR-18（畫面 S-21），BUYER_LEAD 與 SYS_ADMIN 皆可存取。v2.0 將其放在本模組之下，造成 §2.1（BUYER_LEAD ✓）與 AC-13-3（僅 SYS_ADMIN）互相矛盾。
>
> **分級門檻已移出本模組**，改由 §FR-08 隨權重版本管理（BUYER_LEAD 核准）。v2.0 把「各類別分級門檻」放在系統設定，與 AC-04-5 的「各榜獨立門檻」維度不同。

**驗收準則**
- AC-13-1 所有設定變更寫入 `audit_log`，含變更前後值
- AC-13-2 風險規則門檻變更後，依 §FR-10-3 立即觸發背景全量扣分重算
- AC-13-3 僅 SYS_ADMIN 可存取本模組（`/admin` 路由）
- AC-13-4 AI 模型別名對應可於執行期變更，不需重啟服務

---

## FR-14 熱度資料層

**畫面**：S-15 人工熱度標記、S-16 熱度來源狀態

### FR-14-1 人工熱度標記

Facebook、TikTok、小紅書均無合法的程式化資料管道（見附錄 C），本功能為其替代方案。採購人員本就在瀏覽這些平台，將其觀察結構化即可取得早期訊號，且無法律風險、無維運成本。

| 欄位 | 型態 | 必填 | 說明 |
|---|---|---|---|
| 來源連結 | URL | ✓ | 貼上後自動解析平台別 |
| 平台 | 自動判定 | — | 由 URL 解析，可手動覆寫 |
| 熱度等級 | 1–5 單選 | ✓ | 附文字錨點：1「零星討論」→ 5「洗版等級」 |
| 關聯品項／關鍵字 | 自動完成 | ✓ | 可綁既有品項（A 軌）或新建關鍵字（同時建立一筆 `track_type = 'B'` 的品項） |
| 觀察時間 | 自動帶入 | — | 系統時間，可修改 |
| 備註 | 文字 | — | 選填 |

**「關聯品項／關鍵字」為必填**，因此 `manual_heat_tag` 的 `product_id` 與 `keyword_id` 須至少一者非空，資料庫層以 CHECK 約束保證（§7.2）。

### FR-14-1-a 從 1–5 等級到熱度讀值的換算（v3.0 補齊）

v2.0 只說標記是 1–5 的離散等級，也說要做同源百分位與斜率，但沒有任何一步的換算規則。v3.0 定義如下。

**步驟 1：單筆標記轉為基礎值**

| 熱度等級 | 基礎值 `base` |
|---|---|
| 1 零星討論 | 20 |
| 2 | 40 |
| 3 | 60 |
| 4 | 80 |
| 5 洗版等級 | 100 |

**步驟 2：時間衰減**

採**階梯式**，非指數半衰：

```
age = 觀察日至評估日的天數

decay(age) = 1.0   當  age < 14
           = 0.5   當  14 ≤ age < 30
           = 0.0   當  age ≥ 30
```

> 對應環境變數更名為 `HEAT_TAG_HALVE_AFTER_DAYS`（預設 14）與 `HEAT_TAG_EXPIRE_DAYS`（預設 30）。v2.0 的變數名 `HEAT_TAG_DECAY_HALF_DAYS` 暗示指數半衰期，與正文的階梯式描述不符。

**步驟 3：多人標記聚合**

同一標的（品項或關鍵字）在同一評估日的所有未失效標記，以**時間衰減權重**做加權平均：

```
aggregated = Σ( base_i × decay(age_i) ) / Σ( decay(age_i) )
```

**步驟 4：標記人數信心係數**

```
distinct_taggers = 該標的未失效標記的相異標記人數

confidence_factor = 0.6   當 distinct_taggers = 1
                  = 0.8   當 distinct_taggers = 2
                  = 1.0   當 distinct_taggers ≥ 3

raw_value = aggregated × confidence_factor
```

> v2.0 的 §FR-14-1 只列了「單人 0.6、三人以上 1.0」，漏了兩人 0.8（§5.3.2 有列）。v3.0 三段皆明列。

**步驟 5：寫入與斜率**

- 每日排程將上述 `raw_value` 寫入當日的 `heat_reading`（`source_code = 'MANUAL'`），與其他來源一致
- **稀疏期處理**：某日無新標記時，以最近一次有效標記經衰減後的值向前填補，直到衰減歸零為止。這使「標記後熱度自然衰退」本身就構成一條可計算斜率的曲線
- 同源百分位在所有 `MANUAL` 來源讀值間計算。由於基礎值只有 5 檔，並列情況常見，百分位一律採**平均排名法**（tied ranks 取其平均），避免同分品項的排序不穩定

**設計目標**：30 秒內填完。入口常駐於儀表板頁首，不置於次層選單。

### FR-14-2 熱度來源管理

| 項目 | 內容 |
|---|---|
| 顯示 | 來源名稱、Adapter 類型、粒度（關鍵字級／品類級）、合成權重、額度用量、最後更新、可用性狀態 |
| 操作 | 啟用／停用、調整合成權重、測試連線 |
| 狀態 | `AVAILABLE` / `DEGRADED` / `UNAVAILABLE` |
| 不採用來源 | 畫面列出 FB／TikTok／小紅書及其排除理由（附錄 C） |

**可用性的判定與寫入（v3.0 補齊）**

v2.0 要求儀表板「即時查詢 `heat_source.availability`，不快取」，但沒有說誰探測、多久探測一次、如何判定。v3.0 定義：

| 項目 | 規則 |
|---|---|
| 探測排程 | 每 15 分鐘一次健康檢查（`HeatSourceHealthCheckJob`，經 ShedLock 保證單節點執行） |
| 探測方式 | 對每個 `enabled` 的來源送出一次極輕量查詢（人工標記來源改為檢查最近 30 日是否有標記） |
| `AVAILABLE` | 探測成功，且當日額度用量 < 80% |
| `DEGRADED` | 探測成功但額度用量 ≥ 80%，或最近一次採集的資料日期落後 > 2 日 |
| `UNAVAILABLE` | 連續 2 次探測失敗，或當日額度用量 = 100% |
| 寫入 | 每次探測更新 `heat_source.availability`、`last_probed_at`、`quota_used` |
| 狀態變更 | 由 `AVAILABLE` 轉為 `UNAVAILABLE` 時寫入 `audit_log` 並於 AI 任務中心顯示提示 |

**Adapter 介面**

```
HeatSourceAdapter
  ├─ fetch(target, dateRange) → HeatReading[]     // target = 關鍵字或品類
  ├─ probe() → AVAILABLE | DEGRADED | UNAVAILABLE // 健康檢查用，輕量
  └─ getSourceMeta() → { sourceCode, granularity, weight, isManual, latencyDays }
```

**降級行為**

- 該來源合成權重歸零，其餘來源按比例重新正規化
- 品項分數照常產生，UI 標示「本次分數未含 [來源] 資料」
- 本次實際採用的合成權重寫入 `heat_composite_daily.applied_weights`，供事後追溯
- 不阻斷評分流程，不影響排行產出

**合成權重變更的生效**：SYS_ADMIN 調整合成權重後，於下一次每日熱度合成（次日 06:00）生效，並觸發該次合成後的全量重新評分。此變更寫入 `audit_log`。

> §5.10 的計算時機表已補入「熱度來源合成權重變更」一列（v2.0 遺漏，但 AC-14-5 有此要求）。

**驗收準則**
- AC-14-1 貼上連結後可自動判定平台別，判定錯誤時可手動修正
- AC-14-2 標記滿 14 天權重降為 0.5、滿 30 天失效，畫面顯示現行權重
- AC-14-3 同標的多人標記時，合成值反映標記人數之信心係數（1／2／≥3 人分別為 0.6／0.8／1.0）
- AC-14-4 任一來源不可用時，其餘來源權重自動重新正規化，且畫面與 `heat_composite_daily` 皆記錄實際比例
- AC-14-5 僅 SYS_ADMIN 可調整合成權重，變更後於下一輪合成生效並觸發重新評分
- AC-14-6 健康檢查排程每 15 分鐘更新一次可用性狀態
- AC-14-7 標記人數與相異標記者計算正確（同一人多次標記只計一次）

---

## FR-15 權重校準閉環

**畫面**：S-19 權重校準審核

**分工原則：統計決定數字，AI 負責解釋與提出假設，人負責核准。**

**執行流程**

1. **統計模組**（`ssds-calibration`）：以 `campaign_result` 為標籤，對六個加分因子執行迴歸分析，產出各因子與實際銷量的相關係數，以及目前權重的偏離程度
2. **WeightCalibrationAgent**：解讀統計結果，產出可讀報告與調整建議
3. **BUYER_LEAD 審核**：核准／部分採納／駁回
4. **產生新版本**：核准後產生 `weight_version` 草稿（含四組權重與四榜門檻），需再指定生效日期

**顯示內容**

| 區塊 | 內容 |
|---|---|
| 樣本數警示 | 樣本 < `CALIBRATION_MIN_SAMPLE`（預設 200）筆時置頂顯示黃色警示，不可關閉 |
| 因子預測力 | 六因子與銷量的相關係數、相關強度長條、現行權重、建議權重、差異 |
| AI 解讀 | 建議說明、需注意事項（含情境覆寫集中之品類） |
| 回測驗證 | 平權／現行版本／建議版本三者的相關係數與 A 級達標率比較 |
| 審核操作 | 駁回／部分採納（逐項勾選）／核准並建立新版本 |

**必要揭露**

統計校準需累積數百筆帶結果標記的開團案例才具意義。以最低樣本 200 筆計算：若客戶每月開團約 30 團且全數回填，約需 **7 個月**；若回填執行率為五成，則需**逾一年**。

> v2.0 寫「若客戶月開團數為數十團，此為一至兩年的累積」，與 200 筆的算術不符（月 30 團僅需約 7 個月）。v3.0 改以回填執行率說明落差——真正的瓶頸是回填率，不是開團數（對應風險 R-10）。

**專題期間可交付的是機制與回測驗證，不是已完成校準的模型。** 此限制須於畫面與對客戶說明中明確揭露。

**回測作為替代驗證**

在樣本不足期間，以客戶提供的過往開團紀錄執行回測，比較不同權重設定的預測表現，即可在短期內驗證校準機制有效性。此項成立與否取決於附錄 A 第 4 項。

**驗收準則**
- AC-15-1 樣本數 < 200 時必須顯示效度警示，且警示不可關閉
- AC-15-2 建議權重須由統計迴歸產生，AI 僅負責解讀，不可自行產生數值
- AC-15-3 校準建議須經 BUYER_LEAD 核准才產生新權重版本
- AC-15-4 回測功能可比較任意兩個權重版本在同一批歷史資料上的表現
- AC-15-5 部分採納時可逐項勾選要接受的調整
- AC-15-6 產生的新版本為草稿狀態，須另行核准與指定生效日才會生效

---

## FR-16 尋源探索（B 軌）

**畫面**：S-17 尋源探索、S-18 尋源優先序

### FR-16-1 尋源探索

| 項目 | 內容 |
|---|---|
| 輸入 | 關鍵字、品類 |
| 處理 | SourcingScoutAgent 經 opencode 呼叫**白名單內的** MCP 工具，產出探索報告 |
| 輸出 | 熱度階段判定、機會訊號、風險訊號、時效落差評估 |
| 操作 | 存為觀察／加入尋源優先序 |
| 例外 | B 軌預算池耗盡時停用本功能，並提示可用額度重置時間；opencode 或 MCP 不可用時同樣停用，A 軌不受影響 |

**MCP 工具白名單（v3.0 補齊）**

附錄 C 的結論是「系統不建置網路爬蟲」。v2.0 只寫 SourcingScoutAgent「透過 MCP 呼叫社群資料工具」，沒有說明工具從哪來、抓什麼——若該工具實際上去抓 FB 或小紅書，等於用另一條路徑做了附錄 C 明確排除的事，責任歸屬還更模糊。

v3.0 明確限定 MCP 工具的來源，白名單設定於 `MCP_ALLOWED_TOOLS`：

| 允許的工具 | 資料來源 |
|---|---|
| `heat.query` | 系統自有的 `heat_composite_daily` 與 `heat_reading` |
| `heat.manual_tags` | 系統自有的 `manual_heat_tag` |
| `trends.official` | Google Trends（同 A 軌使用的來源） |
| `threads.search` | Threads 官方 API（同 A 軌使用的憑證與額度） |
| `catalog.lookup` | 系統自有的品項主檔與品類樹 |
| `vendor.dataset`（選配） | 付費第三方資料商 API，須經合約確認可用於本用途 |

**明確禁止**：任何以 HTML 抓取、無官方 API 授權、或違反平台服務條款方式取得資料的 MCP 工具，一律不得列入白名單。此規則與附錄 C 一體適用，並於 §14 驗收時以設定檔檢查。

**時效落差模型**

```
時效落差 = 預估熱度剩餘壽命 − 預估尋源前置期
```

| 落差（天） | 判定 | 行為 |
|---|---|---|
| `gap > 14` | 可行 | 正常排序，狀態 `SOURCING` |
| `0 ≤ gap ≤ 14` | 高風險 | 標示「需加速尋源」，狀態 `URGENT` |
| `gap < 0` | 淘汰 | 自動標記 `REJECTED`，不可加入尋源清單 |

> v2.0 的三段寫成「> +14」「0 ～ +14」「< 0」，在 14 天處重疊。v3.0 改用明確不等式。

- **預估熱度剩餘壽命**：由 FR-06 曲線階段判定推估（§5.8）
- **預估尋源前置期**：依品類查 `category_lead_time`（FR-17），採購可覆寫

**設計理由**：時效落差為**否決條件**而非加權因子。若寫成扣分，高熱度仍會將其覆蓋，而「來不及」是不可交易的事實。

### FR-16-2 尋源優先序

| 欄位 | 說明 |
|---|---|
| 品項名稱 | B 軌品項（`track_type = 'B'`） |
| 關聯關鍵字 | |
| 熱度階段 | `RISING` 上升期／`PLATEAU` 高原期（含週數）／`DECLINING` 衰退期 |
| 剩餘壽命 | 天數 |
| 前置期 | 天數，可覆寫 |
| 時效落差 | 以顏色區分三段 |
| 狀態 | `PENDING` 待評估／`SOURCING` 尋源中／`URGENT` 需加速／`PROMOTED` 已成案／`REJECTED` 已淘汰 |

**排序**：以時效落差**升冪**為主排序依據，非熱度。熱度最高但來不及的品項，排在前面不具意義。

**資料模型（v3.0 裁決）**

v2.0 有兩套互斥的 B 軌模型：`product.track_type` 與 `sourcing_status` 暗示 B 軌是 `product`；但 `sourcing_candidate` 綁 `keyword_id`、`manual_heat_tag` 也用 `keyword_id` 代表 B 軌。在後者的模型下，AC-16-5「成案轉軌後熱度資料完整保留」沒有實作路徑——keyword 上的熱度無法掛到新建的 product 上。

v3.0 統一為：

| 項目 | 規則 |
|---|---|
| B 軌主體 | 一律為 `product`，`track_type = 'B'`，`cost` 與 `suggested_price` 可為 NULL |
| `sourcing_candidate` | 綁 `product_id`（非 `keyword_id`），一個 B 軌品項對應一筆 |
| 關鍵字關聯 | 經既有的 `product_keyword` 多對多表，A／B 軌一致 |
| `manual_heat_tag` | 仍保留 `product_id` 與 `keyword_id` 兩欄（可只標關鍵字而不指定品項），但 B 軌品項建立後標記可綁 `product_id` |
| 建立時機 | 使用者在 FR-14-1 新建關鍵字、或在 FR-16-1 發起探索時，系統自動建立對應的 B 軌 `product` |

**成案轉軌**：找到供應商並取得報價後，補填成本與售價，`track_type` 由 `B` 改為 `A`、`sourcing_status` 改為 `PROMOTED`，開始套用完整六因子評分。因為主體從頭到尾都是同一筆 `product`，探索期間累積的熱度資料、人工標記與關鍵字關聯全部自然保留，不需搬移也不需重抓。

**已淘汰保留**：灰底顯示但不移除，供下次同關鍵字出現時參考。

**驗收準則**
- AC-16-1 探索報告須包含熱度階段、剩餘壽命與時效落差
- AC-16-2 B 軌不產生「選品推薦分數」，僅產生尋源優先序
- AC-16-3 B 軌預算耗盡不影響 A 軌評分
- AC-16-4 時效落差 < 0 者自動標記淘汰且不可加入清單
- AC-16-5 成案轉軌後，探索期間的熱度資料、人工標記與關鍵字關聯須完整保留，且不需任何資料搬移
- AC-16-6 尋源優先序依時效落差升冪排序
- AC-16-7 MCP 工具白名單外的工具呼叫一律被拒絕並記錄

---

## FR-17 節慶檔期與氣候基準

**畫面**：S-20 節慶檔期維護

### FR-17-1 節慶時間窗

節慶不是固定的加分欄位，而是時間窗函數。月餅在七月分數應高（備貨期），在十月應歸零。

```
節慶因子(品項, 日期) = max over 所有節慶 f 之 [ affinity(品項, f) × window(日期, f, L) ]

其中
  affinity(品項, f) = 品項×節慶關聯度，0–1
  L                 = 該品項所屬品類的前置天數（category_lead_time）
  d                 = 節慶日 − 評估日（正值代表節慶尚未到）
```

**時間窗函數（v3.0 明確化為不等式）**

```
window(d, L) = 0.0    當  d > L + 30      // 太早，尚未進入備貨期
             = 1.0    當  L ≤ d ≤ L + 30  // 黃金備貨期
             = 0.5    當  0 ≤ d < L       // 來不及正常備貨，但可能有補單機會
             = 0.0    當  d < 0           // 節慶日之後，歸零
```

> v2.0 用「~」表示區間，端點歸屬未定義（`d = L` 與 `d = L + 30` 各落在兩個區間之間）。v3.0 改為明確不等式，四段完全互斥且涵蓋全數線。
>
> v2.0 的 §FR-05 範例「中秋前 62 天，窗權重 0.5」代不進任何一種前置天數：食品國產（L=21）時 62 > 51 應為 0.0；食品進口（L=45）時 62 落在 45–75 應為 1.0。v3.0 的黃金案例改用「中秋前 60 天、進口食品 L=45」→ `45 ≤ 60 ≤ 75` → `window = 1.0`，關聯度 0.60，因子值 0.60。三處（§5.5、§8.3、§FR-05）一致。

**多節慶取最大值**：一個品項可能同時關聯多個節慶（如禮盒類同時關聯中秋與農曆年）。因子值取所有節慶中最高者，並在 UI 標示是哪一個節慶生效。

**離散值的正規化處理**：`window` 只有 0／0.5／1.0 三檔，因子值 = 關聯度 × 窗權重雖為連續值，但並列情況仍多。同品類百分位一律採**平均排名法**（同 §FR-14-1-a 步驟 5）。

**維護內容**

| 資料 | 說明 |
|---|---|
| 節慶檔期 | 農曆年、元宵、清明、端午、父親節、中秋、萬聖、聖誕、跨年、開學季、雙 11／雙 12 |
| 農曆換算 | 中秋、端午、農曆年、元宵等須自動換算國曆日期，不可逐年人工輸入。換算函式庫於 W1 選型（§3.2） |
| 品類前置天數 | 食品（進口）45 天／食品（國產）21 天／日用品 30 天／美妝 35 天。初始值待客戶確認（附錄 A 第 8 項） |
| 品項×節慶關聯度 | 0–1 數值。初期人工建立，累積後由 AI 建議、人審核 |

品類前置天數同時作為 FR-16 時效落差計算的輸入，只維護一份。

### FR-17-2 季節氣候基準

**分兩種用途，不可混用：**

| 用途 | 資料來源 | 進評分 | 說明 |
|---|---|---|---|
| 選品評分 | 歷史同期氣候統計（月均溫、降雨機率） | ✓ | 穩定可用，時序對得上備貨週期 |
| 開團時機提醒 | 短期天氣預報（7–14 天） | ✗ | 僅做通知，不進分數 |

**設計理由**：短期預報僅 7–14 天可信，但團購備貨週期常超過三週。以預報作為評分因子在時序上對不上，會產生「看似有用但永遠來不及反應」的功能。

**氣候適配度的計算（v3.0 補齊）**

v2.0 的因子原始值範例是「8 月均溫 29.4°C」，但完全沒有定義溫度如何轉成「適配度」，`product` 也沒有任何氣候偏好欄位——這個因子在 v2.0 是算不出來的。v3.0 定義：

```
輸入
  T  = 評估月份在該區域的歷史月均溫（climate_normal.avg_temp）
  [Tmin, Tmax] = 品項適溫區間（product.ideal_temp_min / ideal_temp_max）
                 若品項未填，沿用 category_climate_profile 的品類預設
                 兩者皆無 → 該因子標為無資料（不扣分，權重分攤，見 §5.7）

計算
  若 Tmin ≤ T ≤ Tmax          → fit = 1.0
  否則                         → fit = max(0, 1 − distance / TOLERANCE)
       其中 distance = (T < Tmin) ? (Tmin − T) : (T − Tmax)
             TOLERANCE = 12（°C，可由 SYS_ADMIN 調整）

輸出
  raw_value        = fit（0–1）
  normalized_value = fit 在同品類內的百分位
```

**黃金案例驗算**：日式抹茶夾心餅乾，適溫 18–26°C，8 月台北歷史月均溫 29.4°C。
`distance = 29.4 − 26 = 3.4`，`fit = 1 − 3.4/12 = 0.717`。該品項在零食類的同品類百分位為 **44**（夏季多數零食的適溫上限高於 26°C，故此品項相對偏低）。與 §FR-05 區塊 C 一致。

**降雨機率的用途**：目前僅供 UI 顯示與開團時機提醒，不進入 `fit` 計算。若後續要納入，須另行定義品項的「雨天敏感度」欄位。

**開團時機提醒**：獨立通知模組，例如「未來 7 天寒流，庫存中有 3 項暖身相關品項可考慮排檔期」。此通知不寫入 `product_score`，不影響任何分數。

**驗收準則**
- AC-17-1 農曆節慶日期須自動換算，不需逐年人工維護
- AC-17-2 時間窗權重隨當前日期自動變動，於每次評分時重算
- AC-17-3 品類前置天數同時供 FR-16 時效落差使用，資料一致（同一張 `category_lead_time` 表）
- AC-17-4 短期天氣預報不得計入選品分數，僅產生通知
- AC-17-5 品項未填適溫區間時沿用品類預設；兩者皆無時該因子標為無資料且不扣分
- AC-17-6 一品項關聯多個節慶時，因子值取最大者並於 UI 標示生效節慶

---

## FR-18 稽核紀錄（v3.0 新增模組）

**畫面**：S-21 稽核紀錄

自 §FR-13 系統設定中拆出，供 BUYER_LEAD 與 SYS_ADMIN 存取（§2.1 第 25 列）。

| 項目 | 內容 |
|---|---|
| 顯示 | 時間、操作者、動作、實體類型、實體 ID、變更前後值、來源 IP |
| 篩選 | 時間區間、操作者、動作類型、實體類型 |
| 匯出 | XLSX，匯出動作本身亦寫入稽核 |

**須寫入稽核的動作**

| 類別 | 動作 |
|---|---|
| 權限 | 使用者建立／停用、角色指派變更 |
| 評分規則 | 權重版本建立／核准、分級門檻變更、風險門檻變更 |
| 熱度 | 來源啟用／停用、合成權重變更、可用性由 AVAILABLE 轉 UNAVAILABLE |
| 決策 | 決策建立、覆核、結案標記、結案回填 |
| 情境 | 人工覆寫情境權重組（含理由） |
| 風險 | 風險示警忽略（含理由） |
| 校準 | 校準報告核准／部分採納／駁回 |
| 資料 | 資料匯入、報表匯出、品項軟刪除 |
| AI 設定 | 模型別名變更、預算池上限變更 |

**驗收準則**
- AC-18-1 上表所有動作皆產生 `audit_log` 紀錄，含變更前後值
- AC-18-2 BUYER_LEAD 與 SYS_ADMIN 可存取，其餘角色回 403
- AC-18-3 稽核紀錄不可由任何 API 修改或刪除
- AC-18-4 稽核紀錄保留 1 年，且備份策略涵蓋該期間（§3.4）

---

# 5. 選品推薦分數演算法

## 5.1 設計原則

1. **同品類可比**：所有因子在同品類內做百分位正規化，避免跨品類的量級差異影響排序
2. **加分與扣分分離**：加分項參與情境權重調整，扣分項固定生效，不受權重組影響
3. **透明可追溯**：每個因子的原始值、正規化值、權重與扣分明細皆於 UI 揭露
4. **情境適應**：不同商品類型套用不同權重組，但權重組本身是人工定義的離散選項
5. **資料不足不懲罰**：缺資料的因子標示但不扣分
6. **優雅降級**：任一資料來源不可用時，系統仍產生分數並標示缺漏
7. **LLM 不在關鍵路徑上**：所有 LLM 失效時，分數仍必須產生（§6.5）

### 為什麼要分離加分與扣分

客戶明確表示評論影響採購決策的比重不高——社群討論度高的商品即使評價普通仍會開團。但若因此直接調降評論權重，會使「負評集中於食安或物流破損」的品項照樣通過。

分離設計同時滿足兩者：加分項可依情境自由調整（爆款可以不管毛利），扣分項固定生效（但不能不管食安）。

---

## 5.2 因子定義

### 5.2.1 加分因子（六項，參與權重）

| 因子 | `factor_code` | A 軌 | B 軌 | 資料來源 | 定義 |
|---|---|:---:|:---:|---|---|
| 社群熱度斜率 | `TREND` | ✓ | ✓ | FR-06 多來源合成 | 7 日與 30 日雙窗口斜率的加權值（§5.3.3） |
| 毛利率 | `MARGIN` | ✓ | — | 品項主檔 | （售價 − 成本）／售價 |
| 歷史轉換率 | `CVR` | ✓ | — | 銷售紀錄 | 同品類歷史開團表現（§5.2.3） |
| 價格帶適配度 | `PRICE_FIT` | ✓ | — | 會員輪廓 | 建議售價與主力客群價格帶的重疊度（§5.2.4） |
| 節慶時間窗 | `FESTIVAL` | ✓ | ✓ | FR-17-1 | 關聯度 × 時間窗權重，取多節慶最大值 |
| 季節氣候適配 | `CLIMATE` | ✓ | ✓ | FR-17-2 | 適溫區間與歷史月均溫的適配度 |

**代號統一為大寫（v3.0）**。v2.0 有兩套命名：§5.2.1 用 `heat_slope`／`margin`／`conversion`，而 `score_factor.factor_code` 與 API 用 `TREND`／`MARGIN`／`CVR`。v3.0 全文統一為上表的 `factor_code`。

**B 軌只計三個因子**（`TREND`、`FESTIVAL`、`CLIMATE`），且**不產生選品分數**（§5.8）——這三個因子的值仍會計算並儲存，供 B 軌的熱度階段判定與時效落差模型使用，也供成案轉軌後銜接。

### 5.2.1-a 熱度絕對量級：門檻條件，不是加權因子

v2.0 把 `heat_volume`（熱度絕對量級）列為加分因子，說明是「防止『3 則變 9 則』誤判為爆款」，但四組權重都沒有它的欄位。v3.0 依其原本用意改為**門檻條件**：

```
若 heat_volume < HEAT_VOLUME_FLOOR（該品類近 90 日中位數的 20%）
   → TREND 因子標為「量級不足」，normalized_value = 0，且不觸發 HEAT_SURGE 示警
   → 但不視為「無資料」，權重照常套用（此為刻意的低分，不是資料缺漏）
```

**為什麼不做成加權因子**：熱度量級在品類之間差距極大（美妝的討論量本就遠高於乾貨）。做成加權因子會讓大品類的品項永遠贏過小品類，與設計原則 1「同品類可比」直接衝突。做成門檻則只排除雜訊，不影響品類間的公平性。

### 5.2.2 扣分因子（三項，固定生效，不參與權重）

| 因子 | `factor_code` | 最大扣分 | 觸發條件 |
|---|---|---|---|
| 評論風險 | `REVIEW_RISK` | 20 | 負評率超過品類門檻**且**負評集中於品質／食安／物流破損 |
| 物流風險 | `LOGISTICS_RISK` | 10 | 冷鏈需求、易融化、易碎、材積異常 |
| 庫存風險 | `INVENTORY_RISK` | 10 | 效期短、季節性強、最小訂購量偏高 |
| **合計上限** | | **40** | |

扣分一律以 **0 至上限的正值**儲存於 `score_factor`（`is_penalty = true`）與 `product_score.penalty_subtotal`。負號只出現在 UI。

**評論的角色（v3.0 澄清）**

v1.0 將評論情感作為加分因子。v2.0 改為僅在「負面且集中於特定主題」時扣分——這個改變是對的，但 v2.0 的 §7.2「既有資料表變更」把它寫成「移除加分用情感分數欄位」，被誤讀為要刪掉 `review_analysis.sentiment` 欄位。

**v3.0 明確：`sentiment` 欄位保留，只是不再作為加分因子。** 下游有三處必須用它：

| 用途 | 出處 |
|---|---|
| 計算負評率（評論風險扣分的觸發條件之一） | §5.2.2 本表 |
| 顯示正面／中立／負面百分比 | §FR-05 區塊 F |
| API 回應的 `sentimentDistribution` | §8.3 |

刪掉欄位，這三項全部失去資料來源。

**評論風險扣分的計算**

```
negative_rate = 負面評論則數 / 總評論則數
category_threshold = 該品類的負評率門檻（預設 15%，SYS_ADMIN 可調）
risk_topic_share = 負評中屬於「品質／食安／物流破損」三類的比例

若 negative_rate ≤ category_threshold          → 扣 0
若 negative_rate > category_threshold 且 risk_topic_share < 0.3  → 扣 8
若 negative_rate > category_threshold 且 0.3 ≤ risk_topic_share < 0.6 → 扣 14
若 negative_rate > category_threshold 且 risk_topic_share ≥ 0.6  → 扣 20
```

評論則數 < 20 時不計算此扣分（樣本不足），標示「評論樣本不足」。

**物流風險與庫存風險扣分的計算（v3.0 補齊）**

v2.0 只定義了 `REVIEW_RISK` 的完整公式，`LOGISTICS_RISK`／`INVENTORY_RISK` 只給了上限與觸發條件的文字描述，沒有定義各條件的確切扣分點數、多條件同時觸發時如何加總、「效期短」「MOQ 偏高」的數字門檻——這在實作評分引擎時無法算出結果，與 v2.0 遺漏 `PRICE_FIT` 資料表、`HEAT_VOLUME` 門檻定義是同一類問題。

v3.0 定義公式**形狀**（與 `REVIEW_RISK` 的連續值分段判定不同，這兩個因子的觸發條件本質是分類條件，故採逐條件加總）：

```
扣分 = min( Σ(命中條件對應的扣分點數), 該因子上限 )
```

各條件的扣分點數由 `risk_rule.threshold_json` 提供（同 `REVIEW_RISK` 的 `category_threshold`，可依 `risk_rule.category_id` 品類覆寫，由 SYS_ADMIN 調整），**不寫死在程式邏輯中**。初始經驗值（待客戶確認，附錄 A 第 18 項）：

| 因子 | 條件 | 初始點數 |
|---|---|---|
| `LOGISTICS_RISK` | 易融化（`MELTABLE`）且評分月份為夏季（6–9 月） | 4 |
| `LOGISTICS_RISK` | 冷鏈需求（`CHILLED` 或 `FROZEN`） | 4 |
| `LOGISTICS_RISK` | 易碎（`FRAGILE`） | 3 |
| `LOGISTICS_RISK` | 材積異常（`OVERSIZED`） | 3 |
| `INVENTORY_RISK` | 效期短（`shelf_life_days` < 60 天） | 4 |
| `INVENTORY_RISK` | 季節性強（`product.season` ≠ `ALL`） | 3 |
| `INVENTORY_RISK` | 最小訂購量偏高（`moq` > 300） | 3 |

**黃金案例驗算**（§5.5 全文黃金案例區塊 D）：夏季、`MELTABLE` → 命中「易融化+夏季」4 分，其餘物流條件未命中 → `LOGISTICS_RISK` 扣 4，與 §5.5 一致。效期 180 天（≥ 60）、`season=ALL`、MOQ 200（≤ 300）→ 三條件皆未命中 → `INVENTORY_RISK` 扣 0，與 §5.5 一致。

### 5.2.3 歷史轉換率的定義（v3.0 補齊）

```
conversion = Σ(該品項或同品類歷史開團的 qty) / Σ(對應的 impression)
```

| 情況 | 處理 |
|---|---|
| 該品項有自身開團紀錄且 `impression` 非空 | 使用自身資料 |
| 該品項無紀錄，同品類有 ≥ 10 筆 | 使用同品類中位數，`is_imputed = true` |
| `impression` 全為空（客戶未提供曝光數） | 退而以 `qty / 該品類同期平均 qty` 作為相對表現指標，並於 UI 標示計算方式 |
| 同品類 < 10 筆 | 標為無資料（§5.7） |

> 依附錄 A 第 1 項的結果調整。若客戶完全無歷史銷售資料，此因子在專題期間全面標為無資料，權重由其餘五因子分攤（風險 R-01）。

### 5.2.4 價格帶適配度的定義（v3.0 新增）

v2.0 把 `price_fit` 的資料來源標為「會員輪廓」，但 §7.2 完全沒有會員輪廓的資料表，`product.target_audience` 只是一串逗號分隔的代碼，價格敏感區間無處儲存。此因子在 v2.0 是算不出來的——而它是全系統唯一對應痛點 P-3 的因子。

v3.0 補上兩張表（§7.2）與計算式：

```
輸入
  P = 品項建議售價
  該品項所屬品類的客群組成：category_audience_mix(category_id, audience_code, share)
  各客群的價格帶：audience_segment(audience_code, price_min, price_max)

計算
  fit(P, segment) = 1.0                          當 price_min ≤ P ≤ price_max
                  = max(0, 1 − dist / (band × 0.5))   否則
        其中 dist = (P < price_min) ? (price_min − P) : (P − price_max)
              band = price_max − price_min

  price_fit = Σ( share_s × fit(P, s) )   對該品類所有客群 s 加總，Σshare = 1

輸出
  raw_value        = price_fit（0–1）
  normalized_value = price_fit 在同品類內的百分位
```

**黃金案例驗算**：日式抹茶夾心餅乾，售價 149 元。零食類客群組成為主力客群 A（價格帶 120–180，佔 0.6）、價格敏感客群 B（60–120，佔 0.3）、高端客群 C（180–400，佔 0.1）。

- 客群 A：`120 ≤ 149 ≤ 180` → fit = 1.0
- 客群 B：`dist = 149 − 120 = 29`，`band = 60`，fit = `1 − 29/30` = 0.033
- 客群 C：`dist = 180 − 149 = 31`，`band = 220`，fit = `1 − 31/110` = 0.718

`price_fit = 0.6×1.0 + 0.3×0.033 + 0.1×0.718 = 0.6 + 0.010 + 0.072 = 0.682`

四捨五入後 raw_value ≈ 0.68。該值在零食類的同品類百分位為 **82**。（§FR-05 區塊 C 顯示的 0.82 為百分位換算後的顯示值；原始值 0.68 於 hover 時顯示。）

**會員資料的處理**：`audience_segment` 只存去識別化的客群統計（代碼、名稱、價格帶、佔比），不存任何個人資料。此因子的計算完全在後端完成，只有結果百分位可送入 LLM（§6.8）。

---

## 5.3 正規化

### 5.3.1 同品類百分位

各因子在同品類內計算百分位：

```
normalized(x) = percentile_rank(x, same_category_values) × 100
```

**並列處理**：一律採平均排名法（tied ranks 取平均）。節慶時間窗與人工標記熱度都是離散值，並列常見。

**樣本不足的退路（v3.0 修正）**

| 同品類樣本數 | 處理 |
|---|---|
| ≥ 10 | 直接使用同品類百分位 |
| 3 – 9 | **與同一父品類下的兄弟品類合併計算**，信心度扣 20（§5.9） |
| < 3（含父品類合併後仍不足） | 使用全品類百分位，信心度扣 20，並於 UI 明確標示「本因子以全品類基準計算，跨品類量級差異可能影響排序」 |

> v2.0 直接寫「樣本 < 10 退回全品類百分位」，但跨品類量級不可比正是設計原則 1 的立論基礎，此退路直接牴觸該原則且未揭露偏差。v3.0 增加「兄弟品類合併」的中間層，並要求明確標示。

### 5.3.2 熱度多來源合成

各來源量級不可比，因此**先在各來源內百分位化，再依合成權重加總**：

```
heat_composite = Σ( effective_weight_i × percentile_within_source_i ) / Σ( effective_weight_i )

effective_weight_i = configured_weight_i
                   × (source_availability_i == UNAVAILABLE ? 0 : 1)
                   × (granularity_i == CATEGORY ? 0.5 : 1)      // 品類級粒度折扣
```

- 任一來源不可用時其 `effective_weight` 歸零，分母同步縮減，其餘按比例重新正規化
- 品類級來源（目前僅 Instagram）套用 0.5 的粒度折扣（§FR-06）
- 人工標記來源的 `raw_value` 已在寫入時套用時間衰減與標記人數信心係數（§FR-14-1-a），此處不重複套用
- 每日合成結果與 `effective_weight` 的實際值寫入 `heat_composite_daily`

### 5.3.3 斜率計算

```
slope_7d  = (heat_t − heat_{t-7})  / max(heat_{t-7}, ε)      ε = 1.0
slope_30d = (heat_t − heat_{t-30}) / max(heat_{t-30}, ε)
```

`TREND` 因子的原始值為雙窗口加權：

```
trend_raw = 0.7 × slope_7d + 0.3 × slope_30d
```

7 日窗權重較高，因為爆款偵測的目標是抓早期上升；30 日窗提供穩定性，避免單日雜訊。

**背離標記**：`slope_7d < 0` 且 `slope_30d > 0` 時標記為「可能見頂」，於 UI 顯示但**不直接影響分數**（斜率的下降本身已反映在 `trend_raw` 中，再額外扣分會重複計算）。

**歷史資料不足**：新關鍵字的 `heat_composite_daily` 不滿 30 日時，`slope_30d` 以現有最長區間計算並標示；不滿 7 日時整個 `TREND` 因子標為無資料。

---

## 5.4 情境權重組與選組

SceneClassifierAgent 於每次評分時判定情境原型，輸出必須是四組之一：

```json
{
  "sceneType": "VIRAL",
  "confidence": 0.82,
  "reasoning": "近 7 日 Threads 討論量成長 340%，無明確節慶對應，歷史開團 2 次",
  "alternativeScene": "SEASONAL",
  "signals": ["heat_slope_7d: +340%", "history_records: 2", "festival_match: none"]
}
```

**約束條件**

- AI 僅能從已定義的四組中選擇，不得自訂權重數值
- 必須輸出判定理由與依據訊號
- `alternativeScene` 可為 `null`；非 null 時，該情境亦視為適用情境並產生第二筆 `product_score`（§FR-04 多情境評分）
- `confidence < 0.5` 或 Schema 驗證失敗時，退回 `REPLENISHMENT`（常態補貨型）並標示，此時 `alternativeScene` 一律視為 null
- 採購可一鍵覆寫，覆寫紀錄寫入 `scene_classification_log`，被覆寫指定的情境成為主情境

---

## 5.5 加權公式

```
加分小計 = Σ( w_i × normalized_i )        其中 Σw_i = 1，normalized_i ∈ [0, 100]
                                          結果直接落在 [0, 100]，不做二次換算

扣分小計 = Σ( penalty_j )                  正值，上限 40

選品分數 = max(0, 加分小計 − 扣分小計)
```

`w_i` 取自該品項所屬情境權重組於當時生效之 `weight_version`。

> **取消二次正規化（v3.0 裁決）**。v2.0 的範例在加權和 76.3 之後標註「→ 91（同品類百分位換算後）」，這道換算全文沒有定義輸入母體、函數形式或寫入欄位，且使 §5.5、§8.3 與 §FR-05 的數字互相矛盾。加權和本身已是百分位的加權平均，值域即為 0–100，再換算一次沒有增加任何資訊。v3.0 取消。

### 計算範例（全文黃金案例）

**日式抹茶夾心餅乾**｜零食類（進口食品）｜話題爆款型 `VIRAL`｜權重版本 v3｜評分日 2026-07-27

| 因子 | 原始值 | 正規化值 | 權重 | 貢獻 |
|---|---|---|---|---|
| `TREND` 熱度斜率 | +340% / 7d | 96 | 50% | 48.00 |
| `MARGIN` 毛利率 | 0.38 | 88 | 10% | 8.80 |
| `CVR` 歷史轉換率 | 0.042 | 90 | 8% | 7.20 |
| `PRICE_FIT` 價格帶適配 | 0.68 | 82 | 7% | 5.74 |
| `FESTIVAL` 節慶時間窗 | 0.60 | 85 | 15% | 12.75 |
| `CLIMATE` 季節氣候適配 | 0.717 | 44 | 10% | 4.40 |
| **加分小計** | | | **100%** | **86.89** |

| 扣分項 | 判定 | 扣分 |
|---|---|---|
| `REVIEW_RISK` 評論風險 | 負評率 10%，未達零食類門檻 15% | 0.00 |
| `LOGISTICS_RISK` 物流風險 | 夏季高溫、易融化 | 4.00 |
| `INVENTORY_RISK` 庫存風險 | 效期 180 天、MOQ 200，皆未觸發 | 0.00 |
| **扣分小計** | | **4.00** |

```
最終分數 = 86.89 − 4.00 = 82.89  →  82.9
分級     = B（話題爆款榜門檻 A ≥ 85、B ≥ 70）
信心度   = 86（見 §5.9 驗算）
```

**對照：同一品項於 11 月評估**

`CLIMATE` 百分位由 44 升至 82（`fit` = 1.0，落在適溫區間內），且不觸發夏季物流扣分：

```
加分小計 = 86.89 − 4.40 + 8.20 = 90.69  →  90.7
扣分小計 = 0.00
最終分數 = 90.7  →  A 級
```

節慶與氣候會直接改變分數，這是刻意設計——同一個商品在不同時點本來就不是同一個採購決策。

> 本範例的所有數字於 §FR-05 區塊 C／D／E、§8.3 API 回應範例、以及 §11 的評分引擎單元測試中完全一致，作為迴歸測試的黃金案例。

---

## 5.6 分級規則

分級門檻依**榜（情境）**設定，並隨 `weight_version` 一併版本化（§FR-08）。預設值：

| 榜 | A 級 | B 級 | C 級 |
|---|---|---|---|
| 話題爆款榜 `VIRAL` | ≥ 85 | 70 – 84 | < 70 |
| 節慶檔期榜 `FESTIVAL` | ≥ 85 | 70 – 84 | < 70 |
| 常態補貨榜 `REPLENISHMENT` | ≥ 80 | 65 – 79 | < 65 |
| 季節導向榜 `SEASONAL` | ≥ 82 | 68 – 81 | < 68 |

門檻校準列為待客戶確認事項（附錄 A 第 3 項）。

**硬規則**

- 扣分小計 ≥ 20 者，分級最高只給 B，且強制進入風險示警清單（`PENALTY_CAP`）
- 目的：避免高分掩蓋致命風險。評論風險單項上限即為 20，因此食安類負評必然壓級——這是刻意設計

---

## 5.7 資料不足處理

| 情況 | 處理 |
|---|---|
| 單一因子無資料 | 該因子標示灰底，`score_factor.data_available = false`；其權重按比例分攤給其餘有資料的因子，**不產生扣分** |
| 因子有資料但為推估值 | `is_imputed = true`（例：同品類中位數填補的 `CVR`），正常參與加權，信心度另行扣分 |
| 熱度來源部分不可用 | 見 §5.3.2，其餘來源重新正規化 |
| 同品類樣本 < 10 | 見 §5.3.1 的三段退路 |
| **加分因子缺 4 項以上**（六項中過半） | 不產生分數，不寫入 `product_score`；改寫 `product.last_scoring_status = 'INSUFFICIENT_DATA'` 並產生 `DATA_INSUFFICIENT` 風險示警（v3.0 補齊落地機制，見本節後段） |

**`data_available` 與 `is_imputed` 的分工（v3.0 釐清）**

v2.0 同時存在「權重分攤」與「以類別中位數填補」兩種互斥策略，以及 `is_imputed` 與 `data_available` 兩個語意重疊的布林欄位。v3.0 明確分工：

| 欄位 | 意義 | 是否參與加權 |
|---|---|---|
| `data_available = false` | 完全沒有資料，也無法推估 | 否，權重分攤給其他因子 |
| `is_imputed = true` | 有推估值（同品類中位數等） | 是，正常加權，但信心度扣分 |

兩者互斥：`data_available = false` 時 `is_imputed` 必為 false。

**權重分攤公式**

```
available = 有資料的因子集合
w'_i = w_i / Σ(w_k for k in available)      使 Σw'_i = 1
```

**AC-04-3 的措辭**：分攤本身會改變分數（其餘因子的有效權重變大），因此驗收準則寫的是「不產生**扣分**」，而非「不影響分數」。v2.0 寫成「不影響分數」在字面上不成立。

**設計理由**：新品項與 B 軌品項天生缺乏歷史資料。若以扣分處理，這些品項將永遠無法進入排行前段，功能等同失效。

**「資料不足，無法評分」的落地機制（v3.0 補齊）**

v2.0 只寫了「狀態標示」四個字，沒定義標示要寫進哪個欄位。這會有兩個實作上走不通的地方：`product_score.grade`／`final_score` 是 NOT NULL（§7.2.6），不能靠寫一筆「空評分」來代表無法評分；而 §FR-04 四榜的排行查詢天生是從 `product_score` 撈資料，這種品項不產生任何列，等於在四榜裡直接消失，不會有任何「明確標示」（與 FR-04「資料不足明確標示」的設計要點矛盾）。

v3.0 明確：**不寫入 `product_score`**，改用兩處既有機制承接：

1. `product` 新增 `last_scoring_status ENUM('SCORED', 'INSUFFICIENT_DATA') NULL`、`last_scoring_attempted_at DATETIME NULL` 兩欄（§7.2.2）。每次評分嘗試（含全量重評與單筆評分）結束時寫入，供品項清單（§FR-03）與品項詳情（§FR-05）查詢並標示，不需要 join `product_score`。
2. 產生一筆 `risk_alert`，`risk_type = 'DATA_INSUFFICIENT'`（§7.2.8、§FR-10-1 新增列），把「資料不足」與其他扣分達 20 的 `PENALTY_CAP` 情況一樣**主動推進風險示警清單**，而非被動等使用者發現品項從排行榜消失才回頭查——兩者同屬「系統要主動叫人去處理」的情境，用同一套機制。

---

## 5.8 B 軌時效落差模型

B 軌不產生選品分數，改以時效落差排序：

```
時效落差 = 預估熱度剩餘壽命 − 預估尋源前置期
```

| 落差（天） | 判定 | 狀態 |
|---|---|---|
| `gap > 14` | 可行 | `SOURCING` |
| `0 ≤ gap ≤ 14` | 需加速尋源 | `URGENT` |
| `gap < 0` | 淘汰（否決條件） | `REJECTED` |

**預估熱度剩餘壽命**由 TrendInterpreterAgent 依曲線階段推估：

| 階段 | 初始推估值 |
|---|---|
| `RISING` 上升期 | 56 天 |
| `PLATEAU` 高原期第 1–2 週 | 42 天 |
| `PLATEAU` 高原期第 3 週以上 | 35 天 |
| `DECLINING` 衰退期 | 17 天 |

> 上述為初始經驗值，累積回饋資料後由 FR-15 校準。

**Agent 失效時的降級**：TrendInterpreterAgent 不可用時，以規則式判定階段（§6.3 Agent 5 的降級行為），仍可查表得出剩餘壽命，時效落差照常計算。B 軌的排序功能不依賴 LLM。

---

## 5.9 信心度

每筆分數附帶信心度，用於提示使用者該分數的可靠程度。

```
confidence = clamp(100 − Σ(扣分項), 0, 100)
```

| 扣分項目 | 影響 |
|---|---|
| 同品類樣本 < 10（含兄弟品類與全品類退路） | −20 |
| 熱度來源降級或不可用 | 每個 −10 |
| 加分因子 `data_available = false` | 每個 −8 |
| 加分因子 `is_imputed = true` | 每個 −4 |
| 情境判定 `ai_confidence < 0.7` | −10 |
| 人工標記來源僅單人標記 | −5 |
| 熱度歷史不滿 30 日（`slope_30d` 以短區間推估） | −5 |

**起算值為 100，下限為 0。** v2.0 未說明起算值也未說明是否有下限，多項疊加後可能為負。

**黃金案例驗算**

| 檢查項 | 本案狀況 | 扣分 |
|---|---|---|
| 同品類樣本 | 零食類 68 筆，充足 | 0 |
| 熱度來源 | Instagram 當日降級 | −10 |
| 因子無資料 | 六項皆有資料 | 0 |
| 因子為推估值 | `CVR` 為新品，採同品類中位數填補 | −4 |
| 情境判定信心 | 0.82 ≥ 0.7 | 0 |
| 人工標記人數 | 兩人 | 0 |
| 熱度歷史長度 | 90 日完整 | 0 |
| **信心度** | `100 − 10 − 4` | **86** |

§FR-05 區塊 E 與 §8.3 API 回應皆採用此值。

**信心度 < 50 時**於排行與詳情頁顯示警示標記。此門檻與 `<ssds-confidence-badge>` 元件的警示門檻一致（§9.3），v2.0 兩處分別寫 50 與 60，v3.0 統一為 **50**。

---

## 5.10 計算時機

| 觸發 | 範圍 | 是否呼叫 LLM |
|---|---|---|
| 每日 06:00 | 熱度採集與合成，寫入 `heat_composite_daily`；不重算分數 | 否 |
| 每日 06:30 | 熱度異常示警偵測（§FR-10-2） | 否 |
| 每週一 07:00 | 全量重新評分（含情境判定） | **是** |
| 每季首日 08:00 | 權重校準（避開週一 07:00 的全量評分；若首日為週一則順延至 09:00） | 是 |
| 每 15 分鐘 | 熱度來源健康檢查（§FR-14-2） | 否 |
| 品項新增或編輯 | 該品項單筆評分 | 是（單筆，計入「重試與臨時任務」池） |
| 權重版本生效 | 全量重新評分 | 否（沿用最近一次情境判定） |
| 分級門檻變更（隨權重版本） | 全量重新分級，不重算分數 | 否 |
| **熱度來源合成權重變更** | 次日 06:00 合成後全量重新評分 | 否（沿用最近一次情境判定） |
| 風險規則門檻變更 | 立即背景全量重算扣分並重跑風險規則（§FR-10-3） | 否 |
| 資料匯入完成 | 相關品項重算因子與扣分（§FR-09） | 否 |
| 人工覆寫情境 | 該品項即時重算 | 否（情境已由人指定） |

> **「熱度來源合成權重變更」為 v3.0 補入**。AC-14-5 有此要求，但 v2.0 的計算時機表遺漏。

每次評分產生新的 `product_score` 紀錄，保留歷史，不覆寫。同一 `(product_id, period, scene_type)` 重複評分時，以 `calculated_at` 最新者為有效，舊紀錄保留但 `is_active = false`。

**全量重評的批次順序（v3.0 補齊，實作階段發現的操作性缺口）**

§5.3.1 同品類百分位的母體，是「同一 period 內同品類其他品項這個因子的原始值」。這代表本表「每週一 07:00 全量重新評分」等全量批次**不能逐品項算完就正規化**：批次裡最先被算的品項，此時同品類其他品項的原始值都還沒寫入，母體不完整，算出來的百分位會偏差。

正確順序為兩段式：

1. **第一段**：批次內所有品項先各自算出六項加分因子的原始值（`raw_value`），寫入 `score_factor`（此時 `normalized_value` 可留空）。
2. **第二段**：全部原始值到齊後，才對每個品項、每個因子做同品類百分位正規化（§5.3.1）與後續的加權、扣分、分級。

本表所有「全量重新評分」「重算因子」的觸發都適用此順序；單筆評分（品項新增或編輯、人工覆寫情境）因為母體已由前一次全量批次建立，不受影響，可直接查詢既有母體正規化。

> **第一段「寫入 `score_factor`」與現行 schema 衝突（v3.0 補齊，orchestrator 實作階段發現）。**
> `score_factor.score_id` 是 `NOT NULL REFERENCES product_score(id)`（§7.2.6），而
> `product_score.grade`／`final_score` 也是 `NOT NULL`（§5.7 已有的落地機制同一個限制）。
> 這代表字面上的「批次內所有品項先各自算出原始值、寫入 `score_factor`，此時分數還沒算出來」
> 無法照做——`score_factor` 列必須掛在一筆已存在的 `product_score` 列下，但 `product_score`
> 列必須等六項因子、加權、分級全部跑完才能產生。實作（`ProductScoringOrchestrator`）改為第一段
> 只在應用層記憶體中蒐集整批品項的原始值與母體，不落 DB；母體到齊、每個品項的最終結果算出後，
> 才對 `product_score`／`score_factor` 各寫入一次終局列。兩段式「先蒐集完整母體、再正規化」的
> **順序**本身沒有改變、正確性不受影響，只是「先寫 DB 再正規化」這個字面手段改成「先留在記憶體」。
> 若未來需要真的分兩次交易落 DB（例如批次量大到記憶體放不下、或需要中途可觀測進度），
> 需先讓 `score_factor` 能在 `product_score` 之前存在（例如 `score_id` 改為可空、或改以
> `(product_id, period, factor_code)` 為主鍵，不經 `product_score` 外鍵），屬另一次 schema 變更。

---

# 6. AI Agent 設計（opencode + OpenRouter）

## 6.1 定位

所有 AI 產出均為**輔助建議**，不自動執行任何決策。UI 上須明確標示 AI 產出區塊，並與系統計算結果視覺分離。

AI 在本系統中有三種角色，權責不同：

| 角色 | 說明 | 可影響分數 |
|---|---|---|
| 情境判斷 | 判定商品所屬情境原型，選擇權重組 | ✓（離散選擇，可人工覆寫） |
| 文字生成 | 賣點、風險、進貨建議、探索報告 | ✗ |
| 統計解讀 | 解讀迴歸結果，提出權重調整建議 | ✗（須人核准） |

**AI 不做的事**：不自訂權重數值、不決定扣分、不決定分級門檻、不自動建立決策、不修改資料。

---

## 6.2 架構

```
ssds-ai
 ├─ agent/          各 Agent 實作
 ├─ schema/         JSON Schema 定義與驗證
 ├─ prompt/         Prompt 模板 + PromptSanitizer（欄位白名單）
 ├─ cache/          請求快取
 ├─ budget/         三個預算池的配額管理
 ├─ mcp/            MCP client（僅 B 軌使用，含工具白名單檢查）
 └─ fallback/       降級策略
```

### 存取方式分流

| | A 軌 | B 軌 |
|---|---|---|
| 執行路徑 | Spring Boot → OpenRouter REST | Spring Boot → opencode server → MCP 工具 + OpenRouter |
| 觸發 | 每週批次排程 + 單品項即時 | 隨需，人觸發 |
| 輸出 | 結構化 JSON（Schema 驗證） | 對話式報告 + 結構化摘要 |
| 配額 | A 軌批次池（70%） | B 軌探索池（20%） |
| 依賴 | 只依賴 OpenRouter | 依賴 opencode server 與 MCP server |

**A 軌不走 MCP 的理由**：批次 ETL 的需求是可重試、可監控、schema 穩定。在每日數百次的固定抓取路徑上插入 LLM 工具呼叫，會帶來 token 成本、不確定性與錯誤處理困難；且 MCP server 多為 Node/Go 行程，需在 Spring Boot 之外額外維運一個 runtime。

**opencode 或 MCP 不可用時**：僅 B 軌尋源探索停用，A 軌完全不受影響。§3.1 的架構圖已據此調整（v2.0 的圖把 OpenRouter 畫在 opencode 之下，與本節的「REST 直連」互相矛盾）。

---

## 6.3 Agent 規格

共 7 個 Agent。其中 **4 個為 A 軌逐品項執行**（Agent 1–4），配額估算以此為準；Agent 5 逐關鍵字執行、Agent 6 為 B 軌隨需、Agent 7 每季一次。

### Agent 1：SceneClassifierAgent（情境原型判定）

| 項目 | 內容 |
|---|---|
| 執行粒度 | 每品項每次全量評分一次 |
| 輸入 | 品項名稱、品類、熱度斜率百分位、熱度階段、歷史開團次數、節慶匹配結果 |
| 輸出 | `sceneType`（四選一）、`confidence`、`reasoning`、`alternativeScene`（可 null）、`signals[]` |
| 約束 | 輸出必須是列舉值之一，不得產生權重數值 |
| 降級 | Schema 驗證失敗或 `confidence < 0.5` → `REPLENISHMENT` 並標示，`alternativeScene` 視為 null |
| 邏輯別名 | `MODEL_CLASSIFY` |

### Agent 2：ReviewRiskAgent（評論風險分析）

| 項目 | 內容 |
|---|---|
| 執行粒度 | 每品項每次全量評分一次（該品項評論無新增時直接命中快取） |
| 輸入 | 商品評論文字（最多 200 則，超過取最新），**經 §6.8 去識別化** |
| 輸出 | 每則評論的 `sentiment`、負評主題分類（`QUALITY`／`FOOD_SAFETY`／`SHIPPING_DAMAGE`／`PRICE`／`OTHER`）、各主題佔比、嚴重度 |
| 約束 | 主題分類限定於上述列舉值；**不輸出加分用的情感分數** |
| 降級 | 失敗時評論風險扣分計為 0 並標示「評論分析未完成」，不阻斷評分 |
| 邏輯別名 | `MODEL_LONG_TEXT` |

> `sentiment` 仍是本 Agent 的正式輸出，寫入 `review_analysis.sentiment`，供負評率計算與情緒分佈顯示（§5.2.2）。v2.0 的「移除加分用情感分數欄位」指的是取消其加分用途，不是刪除欄位。

### Agent 3：ProductInsightAgent（賣點與風險文字）★ v3.0 合併

| 項目 | 內容 |
|---|---|
| 執行粒度 | 每品項每次全量評分一次 |
| 輸入 | 商品評論（去識別化）、品項基本資料、**扣分明細的分數與命中主題**（不含成本售價原始值） |
| 輸出 | `sellingPoints[]` 2–4 條（每條附 `supportCount` 引用則數與 `aspect`）；`risks[]` 2–4 條（每條附 `type`、`severity`、`countedInPenalty` 布林） |
| 降級 | 失敗時顯示「賣點與風險分析未完成」，不影響分數 |
| 邏輯別名 | `MODEL_LONG_TEXT` |

> **v3.0 合併說明**：v2.0 移除了 RiskAgent，而 ReviewRiskAgent 只輸出主題分類與比率、不產生文字，導致 §FR-05 區塊 F 的「潛在風險條列 2–4 點」與目標 G-4 的「≥2 條風險」沒有任何 Agent 負責。v3.0 將賣點與風險文字合併由本 Agent 產出——兩者輸入相同（同一批評論），一次呼叫產兩種輸出，同時把每品項的 A 軌呼叫數維持在 4 次。

### Agent 4：RecommendationAgent（進貨建議）

| 項目 | 內容 |
|---|---|
| 執行粒度 | 每品項每次全量評分一次 |
| 輸入 | **六因子的正規化百分位（不含原始值）**、加分小計、扣分小計、分級、情境原型、扣分命中的規則名稱、節慶檔期與剩餘天數 |
| 輸出 | `action`（`ADOPT`／`WATCH`／`REJECT`）、`qtyMin`、`qtyMax`（整數）、`quantityText`、`reasoning` |
| 約束 | `action` 列舉與決策類型一致（§FR-11-1）；不得輸出未在輸入中出現的數字 |
| 降級 | 失敗時顯示規則式預設建議（依分級與扣分產生固定文案） |
| 邏輯別名 | `MODEL_SHORT_GEN` |

> **輸入欄位修正（v3.0）**：v2.0 的輸入寫「因子明細、扣分明細」，而因子明細含 `rawValue`（毛利率 0.38、成本、售價），直接違反 §6.8.2 第 1 條「只送百分位，不送原始值」。v3.0 明確限定為百分位。
>
> **輸出欄位修正（v3.0）**：v2.0 輸出 `action: "TRIAL"` 與 `quantitySuggestion: "首批 200–300 件"`，前者與決策列舉不對齊使 `followed_ai` 無法判定，後者為自由字串無法帶入 `first_order_qty`。v3.0 對齊列舉並拆出整數區間。

### Agent 5：TrendInterpreterAgent（趨勢解讀）

| 項目 | 內容 |
|---|---|
| 執行粒度 | **每關鍵字**，非每品項；每日熱度合成後對有顯著變動的關鍵字執行 |
| 輸入 | 近 90 日合成熱度時序、各來源斜率、來源可用性 |
| 輸出 | `stage`（`RISING`／`PLATEAU`／`DECLINING`）、`stageWeeks`、`estimatedLifespanDays` |
| 降級 | 以規則式判定（§FR-06 的階段判定式）與 §5.8 的查表值，B 軌時效落差照常計算 |
| 邏輯別名 | `MODEL_NUMERIC` |

### Agent 6：SourcingScoutAgent（尋源探索，B 軌）

| 項目 | 內容 |
|---|---|
| 執行粒度 | 隨需，由使用者觸發 |
| 輸入 | 關鍵字、品類 |
| 工具 | 經 opencode 呼叫 MCP 工具，**限於 §FR-16-1 的白名單** |
| 輸出 | 探索報告（自然語言）、機會訊號、風險訊號、熱度階段 |
| 預算 | B 軌探索池（20%） |
| 降級 | 預算耗盡、opencode 不可用或 MCP 不可用時停用本功能，提示額度重置時間；A 軌不受影響 |
| 邏輯別名 | `MODEL_REASONING` |

**工具白名單的執行點**：`ssds-ai/mcp/` 的 client 在送出任何工具呼叫前檢查 `MCP_ALLOWED_TOOLS`，白名單外的呼叫直接拒絕並寫入 `audit_log`。此檢查以單元測試驗證（§11、§14）。

### Agent 7：WeightCalibrationAgent（權重校準解讀）

| 項目 | 內容 |
|---|---|
| 執行粒度 | 每季一次 |
| 輸入 | 統計迴歸結果（相關係數、建議權重）、情境覆寫紀錄統計、回測結果。**不送逐筆銷售紀錄** |
| 輸出 | 可讀報告、權重調整建議的說明、需注意事項 |
| 約束 | **不自行產生權重數值**，僅解讀 `ssds-calibration` 的輸出並轉為人話 |
| 降級 | 失敗時直接顯示統計原始結果表 |
| 邏輯別名 | `MODEL_REASONING` |

### v2.0 → v3.0 的 Agent 變更

| Agent | 變更 |
|---|---|
| `SellingPointAgent` | 更名並擴充為 `ProductInsightAgent`，同時產出賣點與風險文字 |
| `RecommendationAgent` | 輸入改為百分位；輸出 `action` 對齊決策列舉、數量拆為整數區間 |
| `TrendInterpreterAgent` | 明確為「每關鍵字」執行，不計入每品項的 4 次呼叫 |
| `RiskAgent`（v1.0） | 已於 v2.0 移除；其文字產出職責於 v3.0 由 `ProductInsightAgent` 承接 |

---

## 6.4 Prompt 模板規範

每個 Agent 的 Prompt 須包含：

1. **角色設定**：明確說明這是零售採購決策輔助情境
2. **輸入資料**：以結構化格式提供，不夾雜指令；一律經 `PromptSanitizer` 組裝（§6.8.2）
3. **輸出格式**：明確指定 JSON Schema，要求不輸出任何額外文字
4. **限制條款**：
   - 僅根據提供的資料作答，不得引入外部知識
   - 資料不足時明確回覆不足，不得推測
   - 不得產生未在輸入中出現的具體數字
   - 不得對特定品牌或供應商作出評價性斷言

每個模板以 `prompt_version` 版控，寫入 `ai_insight.prompt_version` 與日誌，供 A/B 比較與問題回溯。

---

## 6.5 可靠度設計

| 機制 | 說明 |
|---|---|
| Schema 驗證 | 所有結構化輸出經 JSON Schema 驗證，失敗則重試一次後切備援模型 |
| 重試 | 主模型失敗最多重試 1 次，間隔 2 秒；429 另有指數退避（§6.7.3） |
| 逾時 | 單次呼叫 30 秒（`LLM_TIMEOUT_SECONDS`）；尋源探索 90 秒（`LLM_TIMEOUT_SCOUT_SECONDS`） |
| 快取 | 見下方快取鍵表 |
| 降級 | 每個 Agent 皆定義降級行為，任一 Agent 失敗不得阻斷核心評分 |

### 快取鍵組成與有效期（v3.0 補齊）

v2.0 只寫「相同請求 7 日內直接回傳快取」，但全量評分也是 7 日一輪——兩者同週期意味著第二週的評分幾乎必然命中上週結果，「AI 每週重新判定情境」形同虛設。而且除了 Agent 1 之外，其餘 Agent 的 cache key 組成完全未定義。

v3.0 定義如下。`AI_CACHE_DAYS` 預設由 7 改為 **6**，確保短於評分週期。

| Agent | Cache Key 組成 | 有效期 |
|---|---|---|
| SceneClassifier | `productId` + `stage` + `slope_7d` 分箱（每 10% 一箱）+ `festivalMatch` + `promptVersion` | 6 日 |
| ReviewRisk | `productId` + 評論則數分箱（每 50 則）+ 最新評論日期 + `promptVersion` | 6 日 |
| ProductInsight | 同 ReviewRisk | 6 日 |
| Recommendation | `productId` + `grade` + `sceneType` + 加分小計分箱（每 5 分）+ 扣分小計 + `promptVersion` | 6 日 |
| TrendInterpreter | `keywordId` + `stage` + `slope_30d` 分箱 + `promptVersion` | 3 日 |
| SourcingScout | 正規化關鍵字 + `categoryId` + `promptVersion` | 3 日 |
| WeightCalibration | 不快取（每季一次，量小） | — |

**分箱的用意**：熱度或分數的微小波動不應觸發重新呼叫，但跨過分箱邊界代表狀態實質改變，此時快取自動失效。`promptVersion` 納入 key，模板一改所有快取即失效。

**快取命中不佔配額**，命中數於 §FR-07 任務詳情顯示。

**核心原則**：LLM 失敗時，選品分數仍必須產生。AI 只影響情境選組（有預設值可退回）與文字內容，不影響因子計算與扣分。

---

## 6.6 幻覺防護

| 措施 | 說明 |
|---|---|
| 引用要求 | 賣點與風險須標註引用的評論則數（`supportCount`） |
| 數字白名單 | 輸出中的數字須能對應到輸入資料，否則標示為未驗證；由後端在 Schema 驗證後做一次數字比對 |
| 主題限定 | 評論主題分類限定於預設列舉值 |
| 列舉限定 | `sceneType`、`action`、`stage` 皆為列舉，非列舉值視為 Schema 驗證失敗 |
| 人工抽查 | 報表提供 AI 產出抽查功能，供 BUYER_LEAD 定期檢視；使用者可標記「不準確」（`POST /ai/insights/{id}/feedback`） |

---

## 6.7 模型分派與配額控管

### 6.7.1 模型策略

本專案**一律使用免費模型**。免費模型的限制與付費模型有本質差異，設計上必須正面處理：

| 限制 | 影響 | 對策 |
|---|---|---|
| 每分鐘請求數上限 | 批次評分無法一次打完 | 應用層限流 + 分批排程（§6.7.3） |
| 每日請求數上限 | 全量評分可能耗盡當日額度 | 單輪品項上限 + 快取優先 + 失敗項延後重跑（§6.7.3） |
| 無 SLA、尖峰時段可能不可用 | 任務中途失敗 | 備援模型鏈 + 降級（§6.7.4） |
| 模型隨時可能下架 | 硬編碼 model ID 會直接壞掉 | 邏輯別名 + 設定檔對應 |
| 格式遵循度較不穩定 | JSON 解析失敗率高於付費模型 | Schema 驗證 + 重試 + 降級 |
| 資料政策差異 | 提示詞可能被用於訓練 | 資料外送原則（§6.8） |

### 6.7.2 模型分派

系統以**邏輯別名**指定模型，實際 model ID 寫在設定檔，換模型不需改程式碼。

**下表為候選配置，待 §6.7.5 基準測試驗證後才可定案。**

| 邏輯別名 | 用途特性 | 主模型（候選） | 備援模型（候選） | 驗證狀態 |
|---|---|---|---|---|
| `MODEL_CLASSIFY` | 高頻、短輸入、結構化輸出 | `nemotron-3.5-lightning:free` | `gemma-4-31b:free` | 待驗證 |
| `MODEL_LONG_TEXT` | 長輸入、分類與摘要 | `gemma-4-31b:free` | `nemotron-3-super:free` | 待驗證 |
| `MODEL_SHORT_GEN` | 短文字生成 | `nemotron-3.5-lightning:free` | `gemma-4-26b-a4b:free` | 待驗證 |
| `MODEL_NUMERIC` | 數值判讀、低複雜度 | `nemotron-3-nano-30b-a3b:free` | `nemotron-3.5-lightning:free` | 待驗證 |
| `MODEL_REASONING` | 高複雜度、工具呼叫 | `nemotron-3-ultra:free` | `free-models-router` | 待驗證 |

**Agent 對應**

| Agent | 邏輯別名 | 選用理由 |
|---|---|---|
| SceneClassifierAgent | `MODEL_CLASSIFY` | 每次評分每品項一次，量最大；輸出為四選一的列舉值，不需高階推理 |
| ReviewRiskAgent | `MODEL_LONG_TEXT` | 輸入可達 200 則評論，需較長 context 與穩定的主題分類能力 |
| ProductInsightAgent | `MODEL_LONG_TEXT` | 同上，且需生成通順中文 |
| RecommendationAgent | `MODEL_SHORT_GEN` | 輸出為一段短建議，輸入為已結構化的百分位摘要 |
| TrendInterpreterAgent | `MODEL_NUMERIC` | 輸入為時序數值，判定階段與推估壽命 |
| SourcingScoutAgent | `MODEL_REASONING` | 需透過 MCP 呼叫工具、多步驟調查，為全系統最吃能力的任務 |
| WeightCalibrationAgent | `MODEL_REASONING` | 需解讀迴歸結果；每季一次，量小可用最強模型 |

**未採用的免費模型與理由**

| 模型 | 不採用理由 |
|---|---|
| Nemotron 3.5 Content Safety | 內容安全專用分類器，非通用任務模型 |
| Nemotron 3 Nano Omni、Nemotron Nano 12B 2 VL | 多模態導向，本系統無影像分析需求 |
| North Mini Code | 程式碼導向，與本系統任務不符 |
| LFM2.5-2.6B | 參數量過小，結構化輸出穩定度風險高 |
| Dots3-Note Preview、Hy3、MiMo V2.5、Laguna S / XS 2.1 | 資訊不足，列入 §6.7.5 基準測試候選，測後再決定是否納入 |

### 6.7.3 限流、批次排程與單輪上限

以每分鐘 20 次、每日 1,000 次為設計前提（實際數值以 OpenRouter 當期文件為準，且日上限通常需帳戶曾購買最低額度才會提高）。

**A 軌批次池的每日可用請求數 = 1,000 × 70% = 700。**

#### 單輪 AI 分析品項上限（v3.0 新增）

v2.0 用「128 品項 × 4 Agent = 512 次」驗算配額，但 128 這個數字在全文其他地方都不成立：§10 容量目標是 10,000 品項、§10 效能指標與 M2 里程碑用 500 品項、AC-02-1 也用 500 筆。以 500 計算即為 2,000 次請求，超出全日配額近三倍；10,000 品項則差了兩個數量級。

v3.0 明訂：**單輪全量評分中，實際送入 LLM 分析的品項有上限。**

| 項目 | 規則 |
|---|---|
| 環境變數 | `LLM_BATCH_ITEM_CAP`，預設 **150** |
| 納入條件 | 狀態為 `EVALUATING`／`WATCHING`／`ADOPTED`，且未被軟刪除 |
| 優先序 | ①新品項（從未分析過）→ ②快取已失效且熱度跨過分箱邊界 → ③距上次分析最久者 |
| 超出上限者 | 沿用最近一次的 AI 結果（情境、賣點、風險、建議），UI 標示「沿用 x 日前的分析結果」；並排入待重跑清單，隔日續跑 |
| 分數產出 | **所有品項的分數都會產出**，不因未送 LLM 而缺席——因子與扣分為純計算，情境沿用上次判定 |

#### 請求數估算

```
每品項 A 軌 Agent 呼叫數 = 4（SceneClassifier / ReviewRisk / ProductInsight / Recommendation）
單輪上限 150 品項 × 4 = 600 次請求  ≤  A 軌每日配額 700 ✓
600 ÷ 20 次/分 ≈ 30 分鐘（不含重試與快取命中）
```

| 品項規模 | 全部分析所需請求數 | 每日可用 700 | 完成天數 | 每日牆鐘時間 |
|---|---|---|---|---|
| 150 | 600 | 600 | 1 天 | 約 30 分 |
| 500 | 2,000 | 700／日 | 3 天（每日 150 品項為上限，實際 4 天） | 約 30–35 分 |
| 10,000 | 40,000 | — | 不可行 | — |

> §10 的「品項 10,000」是**儲存與查詢容量**目標，不是單輪 AI 分析量。兩者在 v2.0 被混為一談。

**設計要求**

| 項目 | 規格 |
|---|---|
| 限流器 | Resilience4j RateLimiter，全域每分鐘上限，由 `LLM_RATE_LIMIT_PER_MINUTE` 設定 |
| 退避 | 收到 429 時指數退避重試，最多 `LLM_RETRY_MAX`（預設 3）次；仍失敗則列入待重跑清單 |
| 分批 | 全量評分以品項為單位分批送出，單批完成後才送下一批 |
| 快取優先 | 送出前先查快取，命中則不計入配額 |
| 配額監控 | 記錄當日各池已用請求數，達 80% 於 AI 任務中心警示，達 100% 停止送出並保留待重跑清單 |
| 非同步 | 全量評分為背景任務，前端以 SSE 或輪詢取得進度，不阻塞使用者操作 |

**三個預算池**（與 §FR-07 一致）

| 用途 | 每日配額比例 | 環境變數 | 超額行為 |
|---|---|---|---|
| A 軌批次評分 | 70% | `LLM_QUOTA_SHARE_TRACK_A` | 停止送出，待重跑清單隔日續跑，UI 標示沿用上期結果 |
| B 軌尋源探索 | 20% | `LLM_QUOTA_SHARE_TRACK_B` | 停用探索功能，提示重置時間；不影響 A 軌 |
| 重試與臨時任務（含季度校準） | 10% | `LLM_QUOTA_SHARE_RETRY` | 停止送出，提示稍後再試 |

> 季度校準併入第三池（v3.0 統一）。v2.0 的 §FR-07 說校準「獨立計費，單獨列示」（四池），§6.7.3 說「不另設配額」，附錄 B 只有三個變數——三處說法不一。校準每季一次、量小，不值得獨立配額；但排程須避開全量評分時段（§5.10）。

### 6.7.4 備援與降級鏈

```
主模型 → （失敗或 Schema 驗證不通過）→ 重試 1 次
      → （仍失敗）→ 備援模型
      → （仍失敗）→ Agent 各自的降級行為（§6.3）
      → 分數仍照常產出
```

| 階段 | 行為 |
|---|---|
| 主模型 429 | 退避後重試，不立即切換備援（備援可能共用同一額度池） |
| 主模型 5xx 或逾時 | 立即切換備援模型 |
| 主模型下架（404） | 記錄警示，切換備援，於 AI 任務中心提示需更新設定，並寫入 `audit_log` |
| 備援亦失敗 | 執行 Agent 降級行為；**核心評分不得中斷** |
| opencode / MCP 不可用 | 僅停用 B 軌尋源探索 |

`free-models-router` 作為 `MODEL_REASONING` 的備援，由閘道自動選擇當下可用的免費模型，適合用於容錯而非主要路徑（模型身分不固定，輸出穩定度較低）。

### 6.7.5 模型基準測試（W2 執行）

免費模型的能力差異大，且多數在訓練資料截止後才發布，無法僅憑名稱判斷。因此需於 W2 執行基準測試後再定案分派表。

| 測項 | 方法 | 通過標準 |
|---|---|---|
| JSON Schema 遵循度 | 以 50 筆真實輸入跑 SceneClassifier prompt | 通過率 ≥ 95% |
| 中文輸出品質 | 賣點與風險萃取 20 筆，人工評分 | 可讀且無明顯幻覺 |
| 長輸入處理 | 送入 200 則評論，檢查是否截斷或失焦 | 能正確涵蓋全部輸入 |
| 工具呼叫 | MCP 工具呼叫 10 次 | 成功率 ≥ 80% |
| 實際延遲 | 尖峰與離峰各測 20 次 | P95 < 30 秒 |
| 可用性 | 連續 3 日定時探測 | 無整日不可用 |
| **實際配額上限** | 連續送出直到 429，記錄每分鐘與每日的實際上限 | 量得實際值，回填 §6.7.3 的設計前提 |
| **額度重置標頭** | 檢查 429 回應是否含 `X-RateLimit-Reset` 或等效標頭 | 確認可否用於 §FR-07 的重置時間顯示 |

> 後兩項為 v3.0 新增。§6.7.3 的「每分鐘 20 次、每日 1,000 次」目前是假設值，而整個單輪上限與完成天數的估算都建立在它之上——必須量到真實值。

測試結果記錄於 `docs/model-benchmark.md`，並依結果更新 §6.7.2 的分派表與 §6.7.3 的配額前提。

---

## 6.8 資料外送原則

免費模型端點的資料政策與付費端點不同，部分提供者可能保留或使用提示詞進行模型改進。本系統處理客戶的營運資料，因此需明確界定哪些欄位可以送出。

### 6.8.1 分級

| 等級 | 欄位 | 可否送入 LLM |
|---|---|---|
| **公開** | 品項名稱、品類、關鍵字、社群熱度趨勢、**經去識別化的**消費者評論文字 | ✓ 可送 |
| **內部** | 因子百分位、正規化分數、加分／扣分小計、情境判定訊號、熱度斜率百分比、分級 | ✓ 可送（已脫離原始量值） |
| **機敏** | 成本、售價、毛利率原始值、實際銷量、實現毛利、會員輪廓與客群價格帶、供應商名稱與報價 | **✗ 不得送出** |

### 6.8.2 實作規則

1. **只送百分位，不送原始值**
   RecommendationAgent 需要知道「毛利表現好不好」，但不需要知道毛利率是 38%。送入 `marginPercentile: 88` 即可，不送 `margin: 0.38`。同理，價格帶適配度只送 `priceFitPercentile: 82`，不送售價與客群價格帶。

2. **評論文字須先去識別化**（v3.0 新增）
   評論常含帳號、暱稱、電話、地址、訂單編號。`PromptSanitizer` 在送出前執行以下遮罩：

   | 樣式 | 處理 |
   |---|---|
   | `@帳號`、`#tag` 後接的帳號字串 | 替換為 `@USER` |
   | 台灣手機與市話格式 | 替換為 `[PHONE]` |
   | Email | 替換為 `[EMAIL]` |
   | 訂單編號樣式（連續 8 碼以上英數混合） | 替換為 `[ORDER]` |
   | 地址關鍵詞（路／街／巷／弄／號 前後文） | 替換為 `[ADDRESS]` |

   v2.0 把「消費者評論文字」直接列為公開等級可送，但 §10 又要求「送往 LLM 的內容不含個資」，兩者矛盾。v3.0 以去識別化步驟弭平。

3. **供應商匿名化**
   涉及供應商的欄位以代號取代（`SUP_042`），不送實際名稱。

4. **銷量以統計量表示**
   WeightCalibrationAgent 送入的是迴歸係數與相關性，不送逐筆銷售紀錄，也不送絕對銷量。

5. **會員輪廓不進 LLM**
   價格帶適配度因子在後端計算完成（§5.2.4），只將結果百分位送出。

6. **Prompt 建構層集中控管**
   所有送往 LLM 的資料須經 `ssds-ai/prompt/PromptSanitizer` 組裝，該類別維護欄位白名單，未列於白名單者一律不送。以單元測試驗證機敏欄位不會外流（§11、§14 第 6 項）。

### 6.8.3 對客戶的說明義務

專案文件與 UAT 說明中須載明：本系統使用免費 LLM 服務，送出的資料範圍限於上述「公開」與「內部」等級，機敏營運資料不離開自有資料庫，消費者評論於送出前經去識別化處理。

若客戶要求任何資料皆不得送往外部服務，則 AI 分析類功能（情境判定、賣點、風險摘要、進貨建議、尋源探索）需改為地端模型或停用；**評分引擎本身不受影響**，情境一律使用 `REPLENISHMENT` 預設權重組或由人工指定。

---

# 7. 資料庫設計

**本章的表定義一律為 v3.0 的最終狀態。** v2.0 把同一張表分別寫在「主要資料表」與「既有資料表變更」兩處且內容不一致（`product_score` 的欄位在前者已存在、後者又列為「新增」；`product` 則相反），讀者無從判斷以哪份為準。v3.0 只保留一份定義，migration 說明另置於 §7.5。

## 7.1 實體關聯概觀

```
【權限】
app_user ──< user_role >── role
app_user ──< refresh_token
audit_log ──> app_user

【主檔】
category ──┬──< category_lead_time
           ├──< category_climate_profile
           ├──< category_audience_mix >── audience_segment
           ├──< risk_rule                 （品類級門檻）
           └──< product
supplier ──< product
product ──┬──< product_image
          ├──< product_keyword >── trend_keyword
          ├──< item_festival_affinity >── festival_calendar
          └──< sales_record

【熱度】
heat_source ──< heat_reading >──┬── trend_keyword
                                └── category
trend_keyword ──< heat_composite_daily
manual_heat_tag ──> product / trend_keyword / app_user

【評分】
weight_version ──┬──< weight_profile      （四情境 × 六因子）
                 └──< grade_threshold     （四榜 A/B 門檻）
product ──< product_score >── weight_version
product_score ──< score_factor            （加分 6 + 扣分 3）
product ──< scene_classification_log

【評論與 AI】
product ──< product_review ──< review_analysis
product ──< ai_insight
ai_task ──< ai_task_item ──> product

【風險與決策】
product ──< risk_alert
product ──< decision_record ──┬──< campaign_snapshot  （1:1）
                              └──< campaign_result    （1:1）

【B 軌】
product ──< sourcing_candidate            （僅 track_type = 'B'）

【氣候與校準】
climate_normal        （region × month，無外鍵）
calibration_report    （季度，無外鍵）

【批次】
import_batch ──< import_error
report_job ──> app_user
```

## 7.2 主要資料表

### 7.2.1 權限與稽核

#### app_user

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK AI | — |
| email | VARCHAR(150) UNIQUE | 登入帳號 |
| password_hash | VARCHAR(100) | BCrypt cost 10 |
| display_name | VARCHAR(50) | — |
| status | ENUM('ACTIVE','DISABLED') | — |
| failed_attempts | INT | 連續登入失敗次數 |
| locked_until | DATETIME | UTC |
| created_at / updated_at | DATETIME | UTC |

#### role / user_role

標準 RBAC 兩表。`role.code` 對應 §2 的五個角色代碼，`role.code` 有 UNIQUE 索引。

#### refresh_token（v3.0 新增）

支援 §FR-01 的 token 換發與撤銷。

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| user_id | BIGINT FK | — |
| token_hash | CHAR(64) UNIQUE | SHA-256，不存明文 |
| issued_at / expires_at | DATETIME | UTC |
| revoked_at | DATETIME NULL | 登出或強制撤銷時寫入 |
| user_agent / ip | VARCHAR(255) / VARCHAR(45) | 供稽核 |

#### audit_log

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| user_id | BIGINT FK NULL | 系統自動動作時為 NULL |
| action | VARCHAR(64) | 對應 §FR-18 的動作清單 |
| entity_type / entity_id | VARCHAR(48) / BIGINT | — |
| before_json / after_json | JSON | 變更前後值 |
| ip | VARCHAR(45) | — |
| created_at | DATETIME | UTC |

索引：`idx_audit_time(created_at DESC)`、`idx_audit_entity(entity_type, entity_id)`
保留 1 年（§10），備份策略見 §3.4。

### 7.2.2 主檔

#### category

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| parent_id | BIGINT NULL FK | 支援兩層類別 |
| name | VARCHAR(50) | — |
| sort_order | INT | — |

#### category_lead_time

| 欄位 | 型別 | 說明 |
|---|---|---|
| category_id | BIGINT PK FK | — |
| lead_time_days | INT | 供 §FR-17-1 節慶時間窗與 §FR-16 時效落差**共用同一份** |
| updated_by / updated_at | — | — |

#### category_climate_profile（v3.0 新增）

品類層級的預設適溫區間。品項未填 `ideal_temp_min/max` 時沿用此值（§FR-17-2）。

| 欄位 | 型別 | 說明 |
|---|---|---|
| category_id | BIGINT PK FK | — |
| ideal_temp_min | DECIMAL(4,1) | °C |
| ideal_temp_max | DECIMAL(4,1) | °C |
| tolerance | DECIMAL(4,1) | 預設 12.0，適配度衰減的容忍範圍 |

#### audience_segment（v3.0 新增）

去識別化的客群統計，**不含任何個人資料**。供 `PRICE_FIT` 因子計算（§5.2.4）。

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| audience_code | VARCHAR(24) UNIQUE | 如 `MAIN`、`PRICE_SENSITIVE`、`PREMIUM` |
| name | VARCHAR(50) | — |
| price_min / price_max | DECIMAL(10,2) | 該客群的價格帶 |
| note | VARCHAR(255) | — |

#### category_audience_mix（v3.0 新增）

| 欄位 | 型別 | 說明 |
|---|---|---|
| category_id | BIGINT PK FK | — |
| audience_id | BIGINT PK FK | — |
| share | DECIMAL(4,3) | 佔比；同一 category 的 share 加總須為 1.000 |

> `audience_segment` 與 `category_audience_mix` 為 v3.0 新增。v2.0 的 `PRICE_FIT` 因子標明資料來源為「會員輪廓」，但資料庫完全沒有對應的表，因子無法計算——而它是全系統唯一對應痛點 P-3 的因子。

#### supplier

| 欄位 | 型別 |
|---|---|
| id BIGINT PK / name VARCHAR(100) / contact VARCHAR(50) / phone VARCHAR(30) / note VARCHAR(255) | — |

> **無「異常」判定欄位**。§FR-10-1 已將「供應商異常」示警移出本階段範圍，理由是沒有任何資料源提供交期、到貨率或退件資訊。

#### product

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK AI | — |
| name | VARCHAR(100) | — |
| category_id | BIGINT FK | — |
| supplier_id | BIGINT FK NULL | B 軌可為空 |
| **track_type** | ENUM('A','B') | A = 已有供應商與報價；B = 尋源中 |
| **sourcing_status** | ENUM('PENDING','SOURCING','URGENT','PROMOTED','REJECTED') NULL | 僅 B 軌有值 |
| cost | DECIMAL(10,2) NULL | B 軌可為空 |
| suggested_price | DECIMAL(10,2) NULL | B 軌可為空 |
| margin_rate | DECIMAL(5,4) NULL | 冗餘欄位，寫入時計算 |
| moq | INT NULL | 最小訂購量，供庫存風險扣分 |
| **shelf_life_days** | INT NULL | 效期天數，供庫存風險扣分 |
| **logistics_condition** | SET('NORMAL','CHILLED','FROZEN','FRAGILE','MELTABLE','OVERSIZED') NULL | 供物流風險扣分 |
| **ideal_temp_min / ideal_temp_max** | DECIMAL(4,1) NULL | 適溫區間，供 `CLIMATE` 因子；未填則沿用品類預設 |
| season | ENUM('ALL','SPRING','SUMMER','AUTUMN','WINTER','FESTIVAL') | 供庫存風險的季節性判定 |
| status | ENUM('DRAFT','EVALUATING','WATCHING','ADOPTED','LISTED','REJECTED') | 狀態機見 §7.4 |
| reject_reason | VARCHAR(500) NULL | — |
| listed_at | DATE NULL | — |
| **last_scoring_status**（v3.0 新增） | ENUM('SCORED','INSUFFICIENT_DATA') NULL | 最近一次評分嘗試的技術結果，與 `status` 的採購業務狀態機分開維護（§5.7 落地機制）。`NULL` 表尚未嘗試過評分 |
| **last_scoring_attempted_at**（v3.0 新增） | DATETIME NULL | UTC；供 §FR-03 品項清單、§FR-05 詳情頁查詢是否需標示「資料不足」，不需 join `product_score` |
| **deleted_at** | DATETIME NULL | 軟刪除；非 NULL 者不出現在任何清單、排行、評分批次與報表 |
| **deleted_by** | BIGINT FK NULL | — |
| created_by / created_at / updated_at | — | — |

索引：`idx_product_cat_status(category_id, status, deleted_at)`、`idx_product_track(track_type, sourcing_status)`、`idx_product_name(name)`

> `last_scoring_status`／`last_scoring_attempted_at` 為 v3.0 新增，對應 §5.7「資料不足，無法評分」的落地機制。刻意不疊加進 `status`：後者是採購決策的業務狀態機（§7.4），前者是評分引擎的技術結果，兩者混在一起會分不清「品項被人判定觀察中」與「系統根本沒算出分數」。

> `target_audience` 欄位（v2.0 的逗號分隔客群代碼）**已移除**。客群資訊改由 `category_audience_mix` 於品類層級維護，避免逗號分隔字串無法 join 的問題。

#### product_image

`id / product_id FK / file_path VARCHAR(255) / sort_order INT`

#### trend_keyword

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| keyword | VARCHAR(80) UNIQUE | — |
| geo | VARCHAR(10) | 預設 `TW` |
| last_fetched_at | DATETIME NULL | UTC |
| enabled | BOOLEAN | — |

> **`lifecycle` 欄位已移除（v3.0）**。v2.0 用五值列舉（EMERGING/GROWING/PEAK/DECLINING/STABLE），與 `sourcing_candidate.heat_stage` 的三值、以及 §FR-06 內文的三段中文並存，形成三套詞彙。v3.0 統一為三值，且該資訊改由 `heat_composite_daily.stage` 逐日提供，不需在關鍵字上另存一份可能過期的快照。

#### product_keyword

`product_id BIGINT PK FK / keyword_id BIGINT PK FK`（多對多）。A 軌與 B 軌一致使用。

### 7.2.3 熱度

#### heat_source

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| source_code | VARCHAR(32) UNIQUE | `THREADS` / `GOOGLE_TRENDS` / `INSTAGRAM` / `MANUAL` |
| adapter_type | VARCHAR(32) | `REST` / `MANUAL` |
| granularity | ENUM('KEYWORD','CATEGORY') | 品類級來源於合成時套用 0.5 粒度折扣（§5.3.2） |
| composite_weight | DECIMAL(4,3) | 設定的合成權重 |
| availability | ENUM('AVAILABLE','DEGRADED','UNAVAILABLE') | 由健康檢查排程寫入 |
| consecutive_probe_failures | TINYINT | 連續探測失敗次數，達 2 次轉 UNAVAILABLE |
| quota_used / quota_limit | INT | 額度用量 |
| last_probed_at / last_fetched_at | DATETIME NULL | UTC |
| enabled | BOOLEAN | — |

#### heat_reading

各來源熱度原始讀值（時序）。

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| source_id | BIGINT FK | — |
| keyword_id | BIGINT FK NULL | 關鍵字級來源 |
| **category_id** | BIGINT FK NULL | 品類級來源（如 Instagram） |
| reading_date | DATE | — |
| raw_value | DECIMAL(12,3) | 來源原始值；`MANUAL` 來源為 §FR-14-1-a 步驟 4 的輸出 |
| percentile_within_source | DECIMAL(5,2) | 同源百分位（並列採平均排名法） |

約束：`CHECK (keyword_id IS NOT NULL OR category_id IS NOT NULL)`
索引：`uk_reading(source_id, keyword_id, category_id, reading_date)`

> `category_id` 為 v3.0 新增。v2.0 只有 `keyword_id` 維度，但 §FR-06 明說 Instagram「僅做品類級」，品類級讀值無處落地。

#### heat_composite_daily（v3.0 新增）

多來源合成後的每日熱度，是 §FR-06 曲線、§5.3.3 斜率與 §5.8 階段判定的唯一資料來源。

| 欄位 | 型別 | 說明 |
|---|---|---|
| keyword_id | BIGINT PK FK | — |
| stat_date | DATE PK | — |
| composite_value | DECIMAL(6,2) | 0–100 |
| slope_7d | DECIMAL(8,4) | 比率，可為負 |
| slope_30d | DECIMAL(8,4) | 比率，可為負 |
| stage | ENUM('RISING','PLATEAU','DECLINING') | — |
| stage_weeks | TINYINT | 已處於該階段的週數 |
| estimated_lifespan_days | INT NULL | 由 TrendInterpreterAgent 或規則式推估 |
| applied_weights | JSON | **本次實際採用的各來源合成權重**，供事後追溯 |
| divergence_flag | BOOLEAN | 7 日與 30 日背離（可能見頂） |
| volume_below_floor | BOOLEAN | 熱度量級未達下限（§5.2.1-a） |

> v2.0 完全沒有這張表。各來源的 `heat_reading` 有了，但合成後的值、雙窗口斜率、階段判定與「本次實際採用的合成比例」四項顯示需求都沒有資料來源。原 v1.0 的 `trend_daily` 表（單來源 0–100 熱度）於 v3.0 廢除，其職責由 `heat_reading`（各來源原始值）與本表（合成值）取代。

#### manual_heat_tag

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| source_url | VARCHAR(512) | 來源連結 |
| platform | ENUM('FACEBOOK','TIKTOK','XIAOHONGSHU','THREADS','INSTAGRAM','OTHER') | 由 URL 解析，可手動覆寫 |
| heat_level | TINYINT | 1–5，換算見 §FR-14-1-a |
| product_id | BIGINT FK NULL | 標的為品項時 |
| keyword_id | BIGINT FK NULL | 標的為關鍵字時 |
| observed_at | DATETIME | UTC |
| tagged_by | BIGINT FK | — |
| note | VARCHAR(255) NULL | — |

約束：`CHECK (product_id IS NOT NULL OR keyword_id IS NOT NULL)`
索引：`idx_tag_target(product_id, keyword_id, observed_at DESC)`

**不儲存頁面內容**，僅儲存連結與評級（附錄 C）。

> CHECK 約束為 v3.0 新增。v2.0 兩欄皆可為 NULL，但 §FR-14-1 規定「關聯品項／關鍵字」為必填，規格與 schema 不一致。

### 7.2.4 評論

#### product_review

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| product_id | BIGINT FK | — |
| source | VARCHAR(50) | 來源平台或匯入批次 |
| content | TEXT | 原文（送 LLM 前經 §6.8.2 去識別化，不改動本欄） |
| rating | DECIMAL(2,1) NULL | 原始星等 |
| reviewed_at | DATE | — |
| content_hash | CHAR(64) | 去重用（SHA-256） |

唯一鍵：`uk_review(product_id, content_hash)`

#### review_analysis

| 欄位 | 型別 | 說明 |
|---|---|---|
| review_id | BIGINT PK FK | — |
| **sentiment** | ENUM('POSITIVE','NEUTRAL','NEGATIVE') | **保留**。供負評率計算（§5.2.2）與情緒分佈顯示（§FR-05） |
| risk_topic | ENUM('QUALITY','FOOD_SAFETY','SHIPPING_DAMAGE','PRICE','OTHER') NULL | 僅 NEGATIVE 時有值 |
| key_phrase | VARCHAR(50) NULL | — |
| model | VARCHAR(80) | — |
| prompt_version | VARCHAR(20) | — |
| analyzed_at | DATETIME | UTC |

> **`sentiment` 保留是 v3.0 的明確裁決。** v2.0 的變更說明寫「移除加分用情感分數欄位」，若照字面刪除欄位，負評率、情緒分佈與 API 的 `sentimentDistribution` 三處全部失去資料來源。原意是取消其**加分用途**，不是刪欄位。
>
> `aspects`（v2.0 的逗號分隔字串）改為結構化的 `risk_topic` 列舉，與 §6.3 Agent 2 的輸出對齊。

### 7.2.5 評分規則

#### weight_version

**單一定義**（v2.0 在 §7.2 定義了兩次且欄位互斥）。

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| version_no | VARCHAR(16) UNIQUE | 如 `v1` / `v2` / `v3` |
| name | VARCHAR(80) | 如「2026 夏季｜基準」 |
| status | ENUM('DRAFT','APPROVED','RETIRED') | — |
| effective_from | DATE NULL | 核准時指定 |
| is_current | BOOLEAN | 同一時間僅一筆為 true；`GET /weight-versions/active` 依此查詢 |
| change_note | VARCHAR(512) NULL | 本版變更說明 |
| source_calibration_id | BIGINT FK NULL | 若由 §FR-15 校準產生，指向 `calibration_report` |
| created_by / created_at / approved_by / approved_at | — | — |

> 權重數值與分級門檻不存於本表，分別由 `weight_profile` 與 `grade_threshold` 保存。每筆評分永遠綁定 `weight_version_id`，以確保 G-5 的可追溯性。

#### weight_profile

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| version_id | BIGINT FK | 所屬版本 |
| scene_type | ENUM('VIRAL','FESTIVAL','REPLENISHMENT','SEASONAL') | — |
| factor_code | ENUM('TREND','MARGIN','CVR','PRICE_FIT','FESTIVAL','CLIMATE') | **六個加分因子** |
| weight | DECIMAL(4,3) | 同一 `(version_id, scene_type)` 的權重加總須為 1.000 |

唯一鍵：`uk_profile(version_id, scene_type, factor_code)`
應用層於儲存時驗證每組加總為 1.000（AC-08-1）。

#### grade_threshold（v3.0 新增）

四榜的 A／B 分級門檻，隨權重版本一併版本化。

| 欄位 | 型別 | 說明 |
|---|---|---|
| version_id | BIGINT PK FK | — |
| scene_type | ENUM(...) PK | 四榜 |
| grade_a_min | DECIMAL(5,2) | A 級下限 |
| grade_b_min | DECIMAL(5,2) | B 級下限；低於此值為 C |

> v2.0 完全沒有這張表。§FR-13 說門檻依「類別」設定、AC-04-5 說依「榜」設定，兩種維度並存且都無處儲存。v3.0 選定「榜」為唯一維度，並綁定 `version_id`——門檻與權重同屬評分規則，一起版本化才能重現當時的分級。

#### risk_rule

三類扣分規則與 §FR-10-1 各示警類型的門檻，由 SYS_ADMIN 維護。

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| rule_code | VARCHAR(32) UNIQUE | `REVIEW_RISK` / `LOGISTICS_RISK` / `INVENTORY_RISK` / `HEAT_CRASH` / … |
| category_id | BIGINT FK NULL | 非 NULL 時為該品類的覆寫值；NULL 為全域預設 |
| threshold_json | JSON | 各規則的門檻參數（如負評率 0.15、slope 門檻 −0.40） |
| max_penalty | DECIMAL(4,1) NULL | 扣分類規則的上限 |
| enabled | BOOLEAN | 三類扣分規則不可停用（應用層強制） |
| updated_by / updated_at | — | — |

### 7.2.6 評分結果

#### product_score

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| product_id | BIGINT FK | — |
| weight_version_id | BIGINT FK | — |
| period | **CHAR(7)** | ISO 週，如 `2026W30`，以 Asia/Taipei 判定 |
| scene_type | ENUM('VIRAL','FESTIVAL','REPLENISHMENT','SEASONAL') | 套用的情境權重組 |
| **is_primary** | BOOLEAN | 主情境那筆為 true；次要情境為 false |
| **is_active** | BOOLEAN | 同鍵重複評分時，僅最新一筆為 true |
| bonus_subtotal | DECIMAL(5,2) | 加分小計，0–100 |
| penalty_subtotal | DECIMAL(5,2) | 扣分小計，**0–40 的正值** |
| final_score | DECIMAL(5,2) | `max(0, bonus − penalty)` |
| grade | ENUM('A','B','C') | 依該榜門檻 |
| confidence | TINYINT UNSIGNED | 0–100 |
| calculated_at | DATETIME | UTC |

唯一鍵：`uk_score(product_id, period, scene_type, calculated_at)`
索引：`idx_score_rank(period, scene_type, is_active, grade, final_score DESC)`、`idx_score_product(product_id, period, is_primary)`

> **`period` 由 CHAR(8) 改為 CHAR(7)**：`2026W30` 是 7 個字元，CHAR(8) 會補尾隨空白造成比對失敗。
>
> **`is_primary` 與 `is_active` 為 v3.0 新增**，支撐 §FR-04 的多情境評分與 §5.10 的「保留歷史不覆寫」。v2.0 的索引 `idx_score_period_grade(period, grade, final_score)` 不含 `product_id` 也無唯一鍵，同一品項同期可無限重複評分且無法辨識哪筆有效。

#### score_factor

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| score_id | BIGINT FK | — |
| factor_code | VARCHAR(24) | 加分：`TREND`/`MARGIN`/`CVR`/`PRICE_FIT`/`FESTIVAL`/`CLIMATE`；扣分：`REVIEW_RISK`/`LOGISTICS_RISK`/`INVENTORY_RISK` |
| **is_penalty** | BOOLEAN | 區分加分／扣分 |
| raw_value | DECIMAL(12,4) NULL | 原始值；`data_available = false` 時為 NULL |
| normalized_value | DECIMAL(5,2) NULL | 加分因子 0–100；扣分因子為 NULL |
| penalty_value | DECIMAL(4,1) NULL | 扣分因子的扣分（正值）；加分因子為 NULL |
| weight | DECIMAL(4,3) NULL | 加分因子**分攤後的有效權重**；扣分因子為 NULL |
| **data_available** | BOOLEAN | false = 完全無資料，不參與加權（§5.7） |
| **is_imputed** | BOOLEAN | true = 使用推估值（如同品類中位數），正常參與加權 |
| note | VARCHAR(120) NULL | 如「以全品類基準計算」「量級不足」「評論樣本不足」 |

唯一鍵：`uk_factor(score_id, factor_code)`

> **`data_available` 與 `is_imputed` 互斥**：前者 false 時後者必為 false（§5.7）。v2.0 兩個布林欄位語意重疊、且「權重分攤」與「中位數填補」兩種策略並存未說明分工。
>
> 此表是「因子評分透明化」的資料基礎，畫面上每根長條都直接對應一列。**扣分項即使為 0 也必須寫入**（AC-05-4）。

#### scene_classification_log

情境判定與人工覆寫紀錄，為權重校準的關鍵訊號來源（覆寫率指標）。

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| product_id | BIGINT FK | — |
| period | CHAR(7) | — |
| ai_scene_type | VARCHAR(24) NULL | AI 判定結果；判定失敗時為 NULL |
| ai_alternative_scene | VARCHAR(24) NULL | 次要情境 |
| ai_confidence | DECIMAL(3,2) NULL | 0.00–1.00 |
| ai_reasoning | TEXT NULL | — |
| ai_signals | JSON NULL | 依據訊號清單 |
| final_scene_type | VARCHAR(24) | 實際採用 |
| fallback_reason | VARCHAR(64) NULL | 如 `LOW_CONFIDENCE`、`SCHEMA_INVALID` |
| overridden_by | BIGINT FK NULL | 覆寫者 |
| override_reason | VARCHAR(255) NULL | 覆寫時必填（應用層強制，AC-05-2） |
| created_at | DATETIME | UTC |

### 7.2.7 AI

#### ai_insight

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| product_id | BIGINT FK | — |
| insight_type | ENUM('SELLING_POINT','RISK','RECOMMENDATION','TREND') | — |
| content_json | JSON | 結構化結果 |
| model | VARCHAR(80) | 實際使用的 model ID |
| model_alias | VARCHAR(32) | 邏輯別名 |
| prompt_version | VARCHAR(20) | — |
| source_review_count | INT NULL | — |
| prompt_tokens / completion_tokens | INT | — |
| **request_count** | SMALLINT | 本次產出實際消耗的請求數（含重試與備援），**主要度量** |
| cost_usd | DECIMAL(8,5) | 次要度量；免費模型為 0（§FR-07） |
| from_cache | BOOLEAN | 命中快取時為 true，不計入配額 |
| generated_at | DATETIME | UTC |
| is_current | BOOLEAN | 同 `(product_id, insight_type)` 僅一筆 true，其餘為歷史版本 |

#### ai_task / ai_task_item

| ai_task 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| task_type | VARCHAR(32) | `FULL_ANALYSIS` / `SCENE_ONLY` / `SCOUT` / `CALIBRATION` … |
| budget_pool | ENUM('TRACK_A','TRACK_B','RETRY') | 計入哪個預算池 |
| status | ENUM('PENDING','RUNNING','SUCCEEDED','PARTIAL','FAILED','CANCELLED') | — |
| total_count / success_count / fail_count / cache_hit_count | INT | — |
| **request_count** | INT | 已消耗請求數，主要度量 |
| total_cost_usd | DECIMAL(10,5) | 次要 |
| created_by / started_at / finished_at | — | UTC |

| ai_task_item 欄位 | 型別 | 說明 |
|---|---|---|
| id / task_id FK / product_id FK NULL / keyword_id FK NULL | — | 依任務類型擇一 |
| status | ENUM('PENDING','SUCCEEDED','FAILED','SKIPPED_CACHE','SKIPPED_QUOTA') | — |
| error_message | VARCHAR(500) NULL | — |
| duration_ms | INT NULL | — |

> `raw_response` 欄位（v2.0）**已移除**。LLM 的原始回應可能含經模型改寫的評論片段，長期保存於資料庫會擴大機敏資料的暴露面，且無明確用途。除錯所需的資訊改記於應用日誌（§10），保留期依日誌政策，不進資料庫。

### 7.2.8 風險與決策

#### risk_alert

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| product_id | BIGINT FK | — |
| risk_type | ENUM('REVIEW_RISK','LOGISTICS_RISK','INVENTORY_RISK','PENALTY_CAP','HEAT_CRASH','HEAT_SURGE','SEASON_MISMATCH','FESTIVAL_WINDOW_CLOSING','LOW_CONFIDENCE','DATA_INSUFFICIENT') | **完整列舉**，對應 §FR-10-1。`DATA_INSUFFICIENT` 為 v3.0 新增，見 §5.7 |
| severity | ENUM('HIGH','MEDIUM','LOW') | — |
| trigger_value | VARCHAR(100) | 觸發當下的數值描述 |
| status | ENUM('OPEN','ACKNOWLEDGED','IGNORED') | — |
| ignore_reason | VARCHAR(300) NULL | 忽略時必填 |
| detected_at / handled_at / handled_by | — | UTC |

索引：`idx_alert_dedup(product_id, risk_type, status, detected_at DESC)`（供 §FR-10-2 的 7 日去重）

> v2.0 的 `risk_type VARCHAR(30)` 是自由字串，四種示警來源都沒有定義觸發規則，`supplier` 也沒有可判定「異常」的欄位。v3.0 改為完整列舉並在 §FR-10-1 逐一定義規則。

#### decision_record

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| product_id | BIGINT FK | — |
| score_id | BIGINT FK | 綁定的**主情境**評分快照（AC-11-1） |
| decision | ENUM('ADOPT','WATCH','REJECT') | — |
| ai_insight_id | BIGINT FK NULL | 當時的 AI 建議 |
| ai_action | ENUM('ADOPT','WATCH','REJECT') NULL | AI 建議的動作，與 `decision` 同列舉 |
| followed_ai | BOOLEAN | `decision == ai_action` |
| first_order_qty | INT NULL | — |
| ai_qty_min / ai_qty_max | INT NULL | AI 建議的數量區間 |
| expected_list_date | DATE NULL | — |
| **campaign_end_date** | DATE NULL | **結案日期**，回填提醒的起算點（§FR-11-2） |
| reason | VARCHAR(500) NULL | 未採納 AI 建議時必填（應用層強制，AC-11-2） |
| reviewed_by / reviewed_at | — | 覆核者 |
| decided_by / decided_at | — | UTC |

索引：`idx_decision_pending(campaign_end_date, decision)`（供待回填查詢）

> **`campaign_end_date` 為 v3.0 新增。** v2.0 的 §FR-02 與 AC-11-3 都以「結案後 7 日」為判準、要顯示「逾期天數」，但沒有任何欄位承載結案時間——這個計算在 v2.0 是做不出來的。
>
> **`ai_action` / `ai_qty_min` / `ai_qty_max` 為 v3.0 新增**，對應 §6.3 Agent 4 的輸出修正，使 `followed_ai` 可直接比對得出。

#### campaign_snapshot

**只存無法從 `product_score` + `score_factor` 還原的資訊。**

| 欄位 | 型別 | 說明 |
|---|---|---|
| decision_id | BIGINT PK FK | 1:1 |
| source_availability | JSON | 決策當下各熱度來源的狀態 |
| applied_composite_weights | JSON | 當下實際採用的合成權重 |
| applied_thresholds | JSON | 當下該榜的 A／B 門檻 |
| scene_overridden | BOOLEAN | 是否經人工覆寫 |
| created_at | DATETIME | UTC |

> v2.0 的 `campaign_snapshot` 把分數、分級、加減分小計、因子值全部再存一份 JSON，與 `product_score` + `score_factor` 完全重複——而 §5.10 已保證 score 永不覆寫、`decision_record.score_id` 也已綁定。v3.0 精簡為三類無法還原的資訊，其餘一律 join 取得。

#### campaign_result

結案回填（§FR-11-2）。**v2.0 的 `decision_feedback` 已廢除**，功能完全由本表取代。

| 欄位 | 型別 | 說明 |
|---|---|---|
| decision_id | BIGINT PK FK | 1:1 |
| actual_qty | INT | 實際銷量（必填） |
| sellout_status | ENUM('EARLY_SELLOUT','ON_TIME','BELOW_TARGET','SLOW') | 售罄狀況（必填） |
| return_rate | **DECIMAL(5,4)** NULL | 退貨／客訴率，0–1 |
| **realized_margin_rate** | **DECIMAL(5,4)** | 實現毛利率，0–1（必填） |
| post_note_code | ENUM('FASTER_THAN_EXPECTED','HEAT_PASSED','QUALITY_ISSUE','LOGISTICS_ISSUE','OTHER') NULL | — |
| post_note_text | VARCHAR(255) NULL | — |
| filled_by / filled_at | — | UTC |

> **精度與命名修正（v3.0）**：v2.0 的 `return_rate` 在 `decision_feedback` 是 `DECIMAL(5,4)`、在 `campaign_result` 是 `DECIMAL(5,2)`，兩張表不一致；`realized_margin DECIMAL(5,2)` 未說明是金額還是率，若為金額則上限 999.99 明顯不足。v3.0 統一所有比率欄位為 `DECIMAL(5,4)`，並將欄位更名為 `realized_margin_rate` 以消除歧義。

### 7.2.9 B 軌

#### sourcing_candidate

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| **product_id** | BIGINT FK UNIQUE | **綁品項，非關鍵字**（v3.0 裁決） |
| heat_stage | ENUM('RISING','PLATEAU','DECLINING') NULL | — |
| stage_weeks | TINYINT NULL | — |
| estimated_lifespan_days | INT NULL | 由 §5.8 查表或 Agent 推估 |
| lead_time_days | INT | 預設取自 `category_lead_time`，採購可覆寫 |
| lead_time_overridden_by | BIGINT FK NULL | 覆寫者 |
| time_gap_days | INT NULL | 時效落差 = 剩餘壽命 − 前置期 |
| scout_report | TEXT NULL | SourcingScoutAgent 的探索報告 |
| scouted_at | DATETIME NULL | UTC |

索引：`idx_sourcing_gap(time_gap_days ASC)`（供 §FR-16-2 的升冪排序）

> **綁 `product_id` 是 v3.0 的資料模型裁決。** v2.0 綁 `keyword_id`，與 `product.track_type` 的模型互斥，導致 AC-16-5「成案轉軌後熱度資料完整保留」沒有實作路徑。改綁品項後，轉軌只需改 `track_type`，熱度、標記與關鍵字關聯全部自然保留。
>
> 狀態欄位不重複於本表，一律以 `product.sourcing_status` 為準（五值，含 `URGENT`）。

### 7.2.10 節慶與氣候

#### festival_calendar

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| festival_code | VARCHAR(24) | `MID_AUTUMN` / `DRAGON_BOAT` / `LUNAR_NEW_YEAR` / … |
| festival_name | VARCHAR(50) | — |
| calendar_type | ENUM('LUNAR','SOLAR') | — |
| year | SMALLINT | — |
| festival_date | DATE | 農曆節慶於每年初由排程自動換算寫入 |

唯一鍵：`uk_festival(festival_code, year)`

#### item_festival_affinity

| 欄位 | 型別 | 說明 |
|---|---|---|
| product_id | BIGINT PK FK | — |
| festival_code | VARCHAR(24) PK | — |
| affinity | DECIMAL(3,2) | 0.00–1.00 |
| set_by | ENUM('MANUAL','AI_SUGGESTED') | AI 建議者需人工確認後才生效 |
| confirmed_by / confirmed_at | — | — |

#### climate_normal

歷史同期氣候統計。

| 欄位 | 型別 | 說明 |
|---|---|---|
| region_code | VARCHAR(16) PK | 預設 `TW_TPE` |
| month | TINYINT PK | 1–12 |
| avg_temp | DECIMAL(4,1) | °C |
| rain_probability | DECIMAL(4,1) | %，僅供 UI 顯示與開團時機提醒，不進 `CLIMATE` 因子（§FR-17-2） |

### 7.2.11 校準與批次

#### calibration_report

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| quarter | VARCHAR(8) UNIQUE | 如 `2026Q3` |
| sample_size | INT | 已回填的 `campaign_result` 筆數 |
| regression_result | JSON | 六因子的相關係數與建議權重 |
| ai_interpretation | TEXT NULL | WeightCalibrationAgent 的解讀 |
| backtest_result | JSON NULL | 平權／現行／建議三者的比較 |
| status | ENUM('PENDING','APPROVED','PARTIAL','REJECTED') | — |
| accepted_items | JSON NULL | 部分採納時，逐項勾選的結果 |
| reviewed_by / reviewed_at | — | UTC |

#### sales_record

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| order_date | DATE | — |
| product_id | BIGINT FK NULL | 比對不到時為 NULL，保留原名 |
| product_name_raw | VARCHAR(150) | — |
| category_id | BIGINT FK NULL | — |
| price | DECIMAL(10,2) | — |
| qty | INT | — |
| impression | INT NULL | 曝光數，供 `CVR` 因子；為空時走 §5.2.3 的退路 |
| audience_code | VARCHAR(24) NULL | 對應 `audience_segment` |
| import_batch_id | BIGINT FK | — |

索引：`idx_sales_date_cat(order_date, category_id)`、`idx_sales_product(product_id, order_date)`

#### import_batch / import_error

| import_batch | `id / data_type / file_name / file_size / total_rows / success_rows / fail_rows / status / is_async / created_by / created_at / finished_at` |
|---|---|
| import_error | `id / batch_id FK / row_number / column_name / error_message / raw_row` |

`data_type` 列舉：`SALES` / `REVIEW` / `AUDIENCE` / `PRODUCT`

#### report_job（v3.0 新增）

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | — |
| report_type | ENUM('WEEKLY_PICK','SCORE_DETAIL','ACCURACY','SOURCING_QUEUE','CALIBRATION') | 對應 §FR-12 五種報表 |
| format | ENUM('PDF','XLSX') | — |
| params_json | JSON | 篩選條件 |
| status | ENUM('PENDING','RUNNING','SUCCEEDED','FAILED') | — |
| file_path | VARCHAR(255) NULL | 產出檔案位置 |
| row_count | INT NULL | — |
| requested_by / requested_at / finished_at | — | UTC |

> v2.0 有 `/reports/generate`、`/reports/{id}/download`、`/reports` 三個端點，但沒有任何資料表承載任務狀態與檔案位置。

## 7.3 效能考量

| 議題 | 對策 |
|---|---|
| 排行查詢 | `product_score` 建立 `idx_score_rank(period, scene_type, is_active, grade, final_score DESC)`；每週期資料量約為品項數 × 適用情境數（1–2） |
| 評論表成長 | `product_review` 依 `product_id` 分區；超過 2 年的評論可歸檔 |
| 熱度時序查詢 | `heat_composite_daily` 主鍵 `(keyword_id, stat_date)` 即為查詢鍵，90 日區間查詢為索引範圍掃描 |
| 待回填查詢 | `decision_record` 建立 `idx_decision_pending(campaign_end_date, decision)`，避免全表掃描 |
| N+1 | JPA 查詢一律使用 `@EntityGraph` 或 DTO projection，禁止在 Controller 層觸發 lazy loading |
| 大量匯入 | 使用 JDBC batch insert（batch size 500），關閉 Hibernate 一級快取累積 |
| 軟刪除 | 所有品項查詢一律帶 `deleted_at IS NULL`，以 Hibernate `@SQLRestriction` 統一套用，避免遺漏 |

## 7.4 品項狀態機（v3.0 新增）

v2.0 定義了 `product.status` 的六個值與 `decision` 的三個值，錯誤碼有 `INVALID_STATE_TRANSITION`、§11 測試計畫也要測「狀態機」，但規格書從未定義任何轉換規則。

| 來源狀態 | 事件 | 目標狀態 | 允許角色（§2.1 列） |
|---|---|---|---|
| — | 建立品項 | `DRAFT` | 4 |
| `DRAFT` | 完成必填並儲存 | `EVALUATING` | 4 |
| `EVALUATING` | 建立決策 `WATCH` | `WATCHING` | 11 |
| `EVALUATING` | 建立決策 `ADOPT` | `ADOPTED` | 11 |
| `EVALUATING` | 建立決策 `REJECT` | `REJECTED` | 11 |
| `WATCHING` | 建立決策 `ADOPT` | `ADOPTED` | 11 |
| `WATCHING` | 建立決策 `REJECT` | `REJECTED` | 11 |
| `ADOPTED` | 標記上架 | `LISTED` | 4 |
| `LISTED` | 標記結案 | `LISTED`（狀態不變，寫入 `campaign_end_date`） | 12 |
| `REJECTED` | 重新評估 | `EVALUATING` | 13 |
| 任一狀態 | 軟刪除 | 狀態不變，`deleted_at` 寫入 | 5 |

**B 軌的並行狀態**：`track_type = 'B'` 時，`status` 固定為 `EVALUATING`，實際進度由 `sourcing_status` 表示。成案（`PROMOTED`）時 `track_type` 改為 `A`，此後才進入上表的一般流程。

不在上表的轉換一律回 `409 INVALID_STATE_TRANSITION`。

## 7.5 相對於 v2.0 的 Schema 變更

供 Flyway migration 規劃使用。v2.0 已建立的 V2–V6 migration 需以新的 V7 起續補。

### 新增資料表（8 張）

| 資料表 | 用途 | 對應修正 |
|---|---|---|
| `refresh_token` | Token 換發與撤銷 | §FR-01 |
| `audience_segment` | 客群價格帶 | `PRICE_FIT` 因子（§5.2.4） |
| `category_audience_mix` | 品類客群組成 | 同上 |
| `category_climate_profile` | 品類預設適溫區間 | `CLIMATE` 因子（§FR-17-2） |
| `heat_composite_daily` | 合成熱度時序 | §FR-06 曲線與斜率 |
| `grade_threshold` | 四榜分級門檻 | §FR-08、AC-04-5 |
| `risk_rule` | 風險門檻設定 | §FR-10-1、§FR-13 |
| `report_job` | 報表任務 | §FR-12 |

### 廢除資料表（2 張）

| 資料表 | 處理 |
|---|---|
| `decision_feedback` | 資料遷移至 `campaign_result` 後 drop。兩表功能完全重疊，且 v2.0 未說明何者作廢 |
| `trend_daily` | 資料遷移至 `heat_reading`（`source_code = 'GOOGLE_TRENDS'`）後 drop。多來源架構下單一來源的專用表已無意義 |

### 既有資料表變更

| 資料表 | 變更 |
|---|---|
| `product` | 新增 `ideal_temp_min`、`ideal_temp_max`、`deleted_at`、`deleted_by`；`logistics_condition` 改為 SET 型別；移除 `target_audience` |
| `product_score` | `period` 由 CHAR(8) 改 CHAR(7)；新增 `is_primary`、`is_active`；新增唯一鍵 `uk_score`；索引改為 `idx_score_rank` |
| `score_factor` | 新增 `penalty_value`、`note`；`factor_code` 值域擴充為六加分＋三扣分；`data_available` 與 `is_imputed` 語意明確化 |
| `weight_profile` | `factor_code` 新增 `PRICE_FIT`；既有四組權重需重新分配至加總 1.000 |
| `weight_version` | 合併 v2.0 的兩份互斥定義；新增 `source_calibration_id` |
| `trend_keyword` | 移除 `lifecycle` |
| `review_analysis` | **保留 `sentiment`**；`aspects` 改為 `risk_topic` 列舉；新增 `prompt_version` |
| `heat_reading` | 新增 `category_id` 與 CHECK 約束 |
| `heat_source` | 新增 `granularity`、`consecutive_probe_failures`、`last_probed_at` |
| `manual_heat_tag` | `platform` 改為 ENUM；新增 CHECK 約束 |
| `sourcing_candidate` | `keyword_id` 改為 `product_id`；移除 `status`（改用 `product.sourcing_status`） |
| `decision_record` | 新增 `campaign_end_date`、`ai_action`、`ai_qty_min`、`ai_qty_max`、`reviewed_by`、`reviewed_at` |
| `campaign_snapshot` | 大幅精簡，只留 `source_availability`、`applied_composite_weights`、`applied_thresholds`、`scene_overridden` |
| `campaign_result` | `return_rate` 統一為 DECIMAL(5,4)；`realized_margin` 更名為 `realized_margin_rate` 並改為 DECIMAL(5,4) |
| `ai_insight` / `ai_task` | 新增 `request_count`、`from_cache`、`budget_pool`、`cache_hit_count`；`ai_task_item` 移除 `raw_response` |
| `risk_alert` | `risk_type` 由 VARCHAR 改為完整 ENUM |
| `scene_classification_log` | 新增 `period`、`ai_alternative_scene`、`ai_signals`、`fallback_reason` |
| `sales_record` | `audience_tag` 更名為 `audience_code` 並對應 `audience_segment` |

---

# 8. API 規格

## 8.1 通則

| 項目 | 規範 |
|---|---|
| Base path | `/api/v1` |
| 認證 | `Authorization: Bearer <access token>`（除 `/auth/login` 與 `/auth/refresh`） |
| 內容型別 | `application/json; charset=UTF-8` |
| 分頁參數 | `page`（0 起）、`size`、`sort=field,asc\|desc` |
| 日期格式 | ISO-8601（`2026-08-12`、`2026-08-12T10:30:00+08:00`）；回應一律以 `+08:00` 呈現 |
| 命名 | 路徑 kebab-case，JSON 欄位 camelCase |
| 權限 | 一律引用 §2.1 權限矩陣的列號，不在本章另行定義。**不使用 `BUYER+` 這類記法** |

**統一回應格式**

成功：
```json
{ "success": true, "data": { }, "timestamp": "2026-08-12T10:30:00+08:00" }
```

分頁：
```json
{
  "success": true,
  "data": { "content": [], "page": 0, "size": 20, "totalElements": 128, "totalPages": 7 }
}
```

錯誤：
```json
{
  "success": false,
  "error": {
    "code": "PRODUCT_NOT_FOUND",
    "message": "找不到指定的品項",
    "fieldErrors": [{ "field": "suggestedPrice", "message": "建議售價需大於進貨成本" }]
  },
  "timestamp": "2026-08-12T10:30:00+08:00"
}
```

**錯誤代碼**

| HTTP | code | 情境 |
|---|---|---|
| 400 | `VALIDATION_FAILED` | 欄位驗證失敗 |
| 401 | `UNAUTHORIZED` / `TOKEN_EXPIRED` | 未登入或 access token 過期（前端應觸發 refresh，見 §FR-01） |
| 401 | `REFRESH_TOKEN_INVALID` | refresh token 過期或已撤銷，須重新登入 |
| 403 | `FORBIDDEN` | 權限不足 |
| 404 | `RESOURCE_NOT_FOUND` | — |
| 409 | `DUPLICATE_RESOURCE` / `INVALID_STATE_TRANSITION` | 重複，或不在 §7.4 狀態機中的轉換 |
| 409 | `WEIGHT_SUM_INVALID` | 權重組加總不等於 1.000（AC-08-1） |
| 422 | `AI_SCHEMA_INVALID` | LLM 回應無法解析（重試與備援皆失敗後） |
| 423 | `ACCOUNT_LOCKED` | 連續登入失敗達上限 |
| 429 | `RATE_LIMITED` / `QUOTA_EXCEEDED` | 超過頻率或預算池配額上限 |
| 500 | `INTERNAL_ERROR` | — |
| 503 | `AI_SERVICE_UNAVAILABLE` | OpenRouter 無法連線 |
| 503 | `SCOUT_SERVICE_UNAVAILABLE` | opencode 或 MCP 無法連線（僅影響 B 軌） |

## 8.2 端點清單

**本章為完整清單，每個端點只出現一次。** v2.0 把 v2 新增的端點另列一段，導致 `POST /weight-versions`、`POST /weight-versions/{id}/approve`、`GET /scores/ranking`、`GET /decisions/accuracy` 各出現兩次，且 `PUT /decisions/{id}/feedback` 與 `POST /decisions/{id}/result` 功能重複。

「權限」欄的數字對應 §2.1 的列號。

### 認證

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| POST | `/auth/login` | 登入，回傳 access + refresh token | — |
| POST | `/auth/refresh` | 以 refresh token 換發 access token | 持有有效 refresh token |
| POST | `/auth/logout` | 登出，撤銷 refresh token | 已登入 |
| GET | `/auth/me` | 取得當前使用者與角色清單 | 已登入 |

### 儀表板

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/dashboard/summary?period=2026W30&track=A` | KPI 四項（A 級主推數已去重） | 1 |
| GET | `/dashboard/rankings?period=&track=&scene=&limit=5` | **分軌分組排行**。`scene` 省略時回傳四榜各 limit 筆 | 1 |
| GET | `/dashboard/sourcing-summary?limit=3` | B 軌摘要，依時效落差升冪 | 1 |
| GET | `/dashboard/todos` | 待辦提示（含待回填結案） | 1 |
| GET | `/dashboard/heat-sources` | 熱度來源狀態摘要（不快取） | 1 |

> `GET /dashboard/top-products`（v2.0）**已廢除**。它回傳單一總榜，與 §FR-02「不設單一總榜」的核心設計直接衝突。`/dashboard/trend-movers` 併入 `/trends/movers`。

### 品項

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/products` | 清單（篩選、分頁） | 2 |
| GET | `/products/{id}` | 詳情 | 2 |
| POST | `/products` | 新增 | 4 |
| PUT | `/products/{id}` | 編輯 | 4 |
| PATCH | `/products/{id}/status` | 變更狀態（依 §7.4 狀態機） | 4 |
| DELETE | `/products/{id}` | 軟刪除 | 5 |
| POST | `/products/batch/analyze` | 批次建立 AI 任務 | 6 |
| GET/POST/DELETE | `/products/{id}/images` | 圖片 | 4 |
| PUT | `/products/{id}/festival-affinity` | 品項×節慶關聯度 | 21 |

`GET /products` 查詢參數：
`keyword`、`categoryId`、`supplierId`、**`trackType`**（`A`／`B`）、**`sourcingStatus`**、`grade`、`status`、`minScore`、`maxScore`、`hasRisk`、`page`、`size`、`sort`

> `trackType` 與 `sourcingStatus` 為 v3.0 補入。§FR-03-1 明列軌別為必要篩選條件，但 v2.0 的參數清單沒有。

### 評分

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/products/{id}/scores?period=&scene=` | 分數快照（含九項因子明細）。省略 `scene` 回傳主情境 | 2 |
| GET | `/products/{id}/scores/history` | 歷史分數趨勢 | 2 |
| POST | `/products/{id}/scores/recalculate` | 重算單一品項 | 4 |
| GET | `/scores/ranking?period=&scene=&categoryId=&page=` | 排行清單（依榜查詢） | 2 |
| GET | `/scores/{id}/deductions` | 扣分明細 | 2 |
| POST | `/scores/simulate` | 以指定權重版本試算（不寫入） | 2 |
| PUT | `/products/{id}/scene-override` | 人工覆寫情境權重組（理由必填） | 10 |
| GET | `/products/{id}/scene-log` | 情境判定與覆寫紀錄 | 2 |

`POST /scores/simulate` 請求（v3.0 改為版本級）：
```json
{
  "weightVersionId": 3,
  "overrides": {
    "VIRAL": { "TREND": 0.50, "MARGIN": 0.10, "CVR": 0.08,
               "PRICE_FIT": 0.07, "FESTIVAL": 0.15, "CLIMATE": 0.10 }
  },
  "thresholdOverrides": {
    "VIRAL": { "gradeAMin": 85, "gradeBMin": 70 }
  },
  "scene": "VIRAL",
  "categoryId": null,
  "limit": 20
}
```

> v2.0 的 simulate 只接受單一 `sceneType` 加五個權重，但 §FR-04 要求「以其他**權重版本**試算」——版本是四組權重加四榜門檻的集合。v3.0 以 `weightVersionId` 為基底，`overrides` 為選擇性的局部覆寫，兩種用法都支援。請求中任一組權重加總不為 1.000 時回 `409 WEIGHT_SUM_INVALID`。

### 權重版本與情境權重組

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/weight-versions` | 清單 | 2 |
| GET | `/weight-versions/active` | 生效中版本（`is_current = true`） | 2 |
| GET | `/weight-versions/{id}/profiles` | 該版本的四組權重與四榜門檻 | 2 |
| POST | `/weight-versions` | 建立草稿版本 | 15 |
| PUT | `/weight-versions/{id}` | 編輯草稿版本（含權重與門檻） | 15 |
| POST | `/weight-versions/{id}/approve` | 核准生效並觸發全量重算 | 16 |

### AI

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/products/{id}/insights` | 當前 AI 洞察（賣點／風險／建議） | 2 |
| GET | `/products/{id}/insights/history?type=` | 歷史版本 | 2 |
| POST | `/products/{id}/insights/regenerate?type=` | 重新生成（計入重試池） | 6 |
| POST | `/ai/insights/{id}/feedback` | 標記不準確 | 6 |
| POST | `/ai/tasks` | 建立任務 | 6 |
| GET | `/ai/tasks?status=&page=` | 任務清單 | 2 |
| GET | `/ai/tasks/{id}` | 任務詳情 | 2 |
| GET | `/ai/tasks/{id}/items` | 子項目與錯誤 | 2 |
| GET | `/ai/tasks/{id}/stream` | SSE 進度推播 | 2 |
| POST | `/ai/tasks/{id}/cancel` | 取消 | 6 |
| POST | `/ai/tasks/{id}/retry-failed` | 重跑失敗項 | 6 |
| **GET** | **`/ai/budgets`** | **三個預算池的即時用量與重置時間** | 2 |
| GET | `/ai/usage?month=2026-08` | 月度請求數與 token 統計 | 2 |

> `/ai/budgets` 為 v3.0 新增。§FR-07 要顯示三個預算池的即時用量與超額狀態，但 v2.0 只有月度彙總的 `/ai/usage`。

`POST /ai/tasks` 請求：
```json
{
  "taskType": "FULL_ANALYSIS",
  "productIds": [12, 45, 78],
  "options": { "forceRefresh": false }
}
```

`GET /ai/budgets` 回應：
```json
{
  "success": true,
  "data": {
    "dailyQuota": 1000,
    "resetAt": "2026-08-13T08:00:00+08:00",
    "resetSource": "HEADER",
    "pools": [
      { "pool": "TRACK_A", "share": 0.7, "limit": 700, "used": 604, "cacheHits": 96, "status": "WARNING" },
      { "pool": "TRACK_B", "share": 0.2, "limit": 200, "used": 12,  "cacheHits": 3,  "status": "OK" },
      { "pool": "RETRY",   "share": 0.1, "limit": 100, "used": 31,  "cacheHits": 0,  "status": "OK" }
    ]
  }
}
```

### 趨勢與熱度

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/trends/keywords` | 關鍵字清單 | 2 |
| POST | `/trends/keywords` | 新增 | 4 |
| PATCH | `/trends/keywords/{id}` | 啟用/停用 | 4 |
| GET | `/trends/series?keywordIds=1,2,3&range=90d` | 合成熱度數列（含各來源明細與實際合成權重） | 2 |
| GET | `/trends/movers?range=7d` | 熱度變化榜 | 2 |
| POST | `/trends/keywords/{id}/refresh` | 手動更新 | 6 |
| POST | `/trends/keywords/{id}/interpret` | AI 解讀（計入重試池） | 6 |
| GET | `/heat-sources` | 來源清單與可用性 | 2 |
| PUT | `/heat-sources/{id}` | 調整合成權重／啟用狀態 | 19 |
| POST | `/heat-sources/{id}/test` | 測試連線 | 19 |
| POST | `/heat-tags` | 建立人工熱度標記 | 7 |
| GET | `/heat-tags?scope=&days=` | 標記清單 | 2 |
| PUT | `/heat-tags/{id}` | 編輯自己建立的標記 | 7 |
| DELETE | `/heat-tags/{id}` | 刪除自己建立的標記 | 7 |
| POST | `/heat-tags/resolve-platform` | 由 URL 解析平台別 | 7 |

### 風險

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/risks?status=&severity=&type=&page=` | 風險清單 | 2 |
| PATCH | `/risks/{id}/acknowledge` | 確認 | 14 |
| PATCH | `/risks/{id}/ignore` | 忽略（理由必填） | 14 |
| GET | `/risks/rules` | 風險規則與門檻 | 2 |
| PUT | `/risks/rules/{code}` | 調整門檻，觸發背景全量扣分重算 | 18 |

### 決策與回饋

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| POST | `/products/{id}/decisions` | 建立決策 | 11 |
| GET | `/decisions?page=&decidedBy=&from=&to=&pendingResult=` | 決策清單 | 2 |
| GET | `/decisions/{id}` | 詳情 | 2 |
| GET | `/decisions/{id}/snapshot` | 決策快照（join `product_score` 與 `score_factor`） | 2 |
| POST | `/decisions/{id}/review` | 覆核 | 13 |
| POST | `/decisions/{id}/close` | **標記結案**（寫入 `campaign_end_date`） | 12 |
| POST | `/decisions/{id}/result` | 結案回填 | 12 |
| GET | `/decisions/accuracy?from=&to=&categoryId=&decidedBy=` | 準確度分析（含樣本數與效度警示旗標） | 2 |

> `PUT /decisions/{id}/feedback`（v2.0）**已廢除**，與 `POST /decisions/{id}/result` 功能重複。對應的 `decision_feedback` 表亦廢除（§7.5）。

### 尋源（B 軌）

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| POST | `/sourcing/scout` | 發起探索（回傳 `ai_task` id，非同步） | 8 |
| GET | `/sourcing/candidates?status=&page=` | 尋源優先序清單（依時效落差升冪） | 2 |
| GET | `/sourcing/candidates/{productId}` | 單筆候選詳情（含探索報告與時效落差） | 2 |
| POST | `/sourcing/candidates` | 加入清單 | 9 |
| PUT | `/sourcing/candidates/{productId}/lead-time` | 覆寫前置期，重算時效落差 | 9 |
| POST | `/sourcing/candidates/{productId}/promote` | 成案轉入 A 軌（需同時提供成本與售價） | 9 |

> **路徑參數統一為 `{productId}`**（v3.0）。v2.0 的 `GET /sourcing/{id}/time-gap` 的 `{id}` 沒有說明是探索任務、候選還是關鍵字。時效落差不另設端點，直接包含在候選詳情的回應中。

### 校準

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/calibration/reports?page=` | 歷次校準報告 | 2 |
| GET | `/calibration/reports/latest` | 最新校準建議 | 2 |
| POST | `/calibration/reports/{id}/approve` | 核准／部分採納／駁回 | 17 |
| POST | `/calibration/backtest` | 回測比較兩個權重版本 | 2 |

### 節慶、氣候與主檔

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET | `/festivals?year=2026` | 年度檔期（含農曆換算結果） | 2 |
| POST/PUT/DELETE | `/festivals` | 維護檔期 | 20 |
| GET | `/categories` | 品類樹 | 2 |
| PUT | `/categories/{id}/lead-time` | 品類前置天數 | 20 |
| PUT | `/categories/{id}/climate-profile` | 品類預設適溫區間 | 20 |
| GET | `/suppliers` | 供應商清單 | 2 |
| POST/PUT | `/suppliers` | 維護 | 4 |
| GET | `/audiences` | 客群區隔與價格帶 | 2 |
| GET | `/climate-normals?region=TW_TPE` | 歷史氣候基準 | 2 |

### 匯入與報表

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| POST | `/imports/upload` | 上傳檔案（multipart），回傳欄位對應建議 | 22 |
| POST | `/imports/{batchId}/preview` | 預覽與驗證 | 22 |
| POST | `/imports/{batchId}/confirm` | 確認匯入 | 22 |
| GET | `/imports?page=` | 批次清單 | 2 |
| GET | `/imports/{batchId}` | 批次狀態（非同步匯入的進度） | 2 |
| GET | `/imports/{batchId}/errors/download` | 下載錯誤檔 | 2 |
| POST | `/reports/generate` | 建立報表產生任務 | 3 |
| GET | `/reports?page=` | 已產生報表清單 | 3 |
| GET | `/reports/{id}/download` | 下載 | 3 |

### 系統與稽核

| Method | Path | 說明 | 權限 |
|---|---|---|---|
| GET/POST/PUT/PATCH | `/admin/users` | 使用者管理 | 23 |
| GET/PUT | `/admin/ai-config` | AI 模型別名對應、預算池上限、快取天數 | 24 |
| GET/PUT | `/admin/schedules` | 排程設定 | 24 |
| GET | `/audit-logs?page=&from=&to=&userId=&action=&entityType=` | 稽核紀錄 | 25 |
| GET | `/audit-logs/export` | 稽核紀錄匯出（本動作亦寫入稽核） | 25 |

> `/audit-logs` 已從 `/admin` 路徑下移出（§FR-18）。v2.0 把它放在 `/admin/audit-logs`，而 `/admin` 的路由守衛是 SYS_ADMIN 專屬，與 §2.1 給 BUYER_LEAD 的檢視權互相矛盾。

## 8.3 回應範例：品項詳情

`GET /api/v1/products/12`

**本範例的所有數字與 §5.5 的黃金案例、§FR-05 的區塊 C／D／E 完全一致。**

```json
{
  "success": true,
  "data": {
    "id": 12,
    "name": "日式抹茶夾心餅乾",
    "trackType": "A",
    "sourcingStatus": null,
    "category": { "id": 3, "name": "零食類" },
    "supplier": { "id": 8, "name": "京都食品" },
    "cost": 92.38,
    "suggestedPrice": 149,
    "marginRate": 0.38,
    "logisticsCondition": ["NORMAL", "MELTABLE"],
    "shelfLifeDays": 180,
    "moq": 200,
    "idealTempMin": 18.0,
    "idealTempMax": 26.0,
    "status": "EVALUATING",

    "scene": {
      "sceneType": "VIRAL",
      "sceneLabel": "話題爆款型",
      "aiConfidence": 0.82,
      "reasoning": "近 7 日 Threads 討論量成長 340%，無明確節慶對應，歷史開團 2 次",
      "alternativeScene": "SEASONAL",
      "signals": ["heat_slope_7d: +340%", "history_records: 2", "festival_match: MID_AUTUMN(0.60)"],
      "overriddenBy": null,
      "overrideReason": null
    },

    "score": {
      "id": 5521,
      "period": "2026W30",
      "sceneType": "VIRAL",
      "isPrimary": true,
      "bonusSubtotal": 86.89,
      "penaltySubtotal": 4.00,
      "finalScore": 82.89,
      "grade": "B",
      "confidence": 86,
      "weightVersion": { "id": 3, "versionNo": "v3", "name": "2026 夏季｜基準" },
      "appliedThresholds": { "gradeAMin": 85, "gradeBMin": 70 },
      "bonusFactors": [
        { "code": "TREND",     "label": "社群熱度斜率",   "rawValue": 3.40,  "normalized": 96, "weight": 0.50, "contribution": 48.00, "dataAvailable": true,  "imputed": false },
        { "code": "MARGIN",    "label": "毛利率",         "rawValue": 0.38,  "normalized": 88, "weight": 0.10, "contribution": 8.80,  "dataAvailable": true,  "imputed": false },
        { "code": "CVR",       "label": "歷史轉換率",     "rawValue": 0.042, "normalized": 90, "weight": 0.08, "contribution": 7.20,  "dataAvailable": true,  "imputed": true,  "note": "以零食類中位數填補" },
        { "code": "PRICE_FIT", "label": "價格帶適配度",   "rawValue": 0.68,  "normalized": 82, "weight": 0.07, "contribution": 5.74,  "dataAvailable": true,  "imputed": false },
        { "code": "FESTIVAL",  "label": "節慶時間窗",     "rawValue": 0.60,  "normalized": 85, "weight": 0.15, "contribution": 12.75, "dataAvailable": true,  "imputed": false, "note": "中秋前 60 天，窗權重 1.0" },
        { "code": "CLIMATE",   "label": "季節氣候適配",   "rawValue": 0.717, "normalized": 44, "weight": 0.10, "contribution": 4.40,  "dataAvailable": true,  "imputed": false, "note": "8 月均溫 29.4°C，適溫 18–26°C" }
      ],
      "penaltyFactors": [
        { "code": "REVIEW_RISK",     "label": "評論風險", "penaltyValue": 0.0, "note": "負評率 10%，未達零食類門檻 15%" },
        { "code": "LOGISTICS_RISK",  "label": "物流風險", "penaltyValue": 4.0, "note": "夏季高溫、易融化" },
        { "code": "INVENTORY_RISK",  "label": "庫存風險", "penaltyValue": 0.0, "note": "效期 180 天、MOQ 200，皆未觸發" }
      ],
      "confidenceDeductions": [
        { "reason": "SOURCE_DEGRADED", "detail": "Instagram 當日降級", "value": -10 },
        { "reason": "FACTOR_IMPUTED",  "detail": "CVR 使用同品類中位數", "value": -4 }
      ],
      "calculatedAt": "2026-07-27T03:31:12+08:00"
    },

    "sentimentDistribution": {
      "positive": 0.68, "neutral": 0.22, "negative": 0.10,
      "reviewCount": 1240,
      "negativeTopics": { "QUALITY": 0.21, "FOOD_SAFETY": 0.00, "SHIPPING_DAMAGE": 0.44, "PRICE": 0.28, "OTHER": 0.07 },
      "categoryThreshold": 0.15,
      "thresholdBreached": false
    },

    "insights": {
      "sellingPoints": [
        { "text": "抹茶風味評價極高，回購率佳", "supportCount": 312, "aspect": "口味" },
        { "text": "主打伴手禮情境，客單價提升", "supportCount": 118, "aspect": "情境" }
      ],
      "risks": [
        { "text": "夏季高溫易融化，需注意物流", "type": "LOGISTICS", "severity": "MEDIUM", "countedInPenalty": true },
        { "text": "單價偏高，對價格敏感客群較不友善", "type": "PRICE", "severity": "LOW", "countedInPenalty": false }
      ],
      "recommendation": {
        "action": "ADOPT",
        "qtyMin": 200,
        "qtyMax": 300,
        "quantityText": "首批 200–300 件",
        "reasoning": "熱度斜率為零食類前 4%，建議列入本週主推；因夏季物流扣分壓在 B 級，建議首批小量試單並指定冷鏈配送，觀察轉換後再追加。",
        "model": "nemotron-3.5-lightning:free",
        "modelAlias": "MODEL_SHORT_GEN",
        "promptVersion": "rec-v3.1",
        "requestCount": 1,
        "fromCache": false,
        "generatedAt": "2026-07-27T04:02:55+08:00",
        "sourceReviewCount": 1240
      }
    },

    "activeRisks": [
      { "id": 91, "riskType": "LOGISTICS_RISK", "severity": "MEDIUM", "triggerValue": "夏季高溫 / 易融化", "status": "OPEN" }
    ],

    "alternativeScores": [
      { "id": 5522, "sceneType": "SEASONAL", "finalScore": 74.15, "grade": "B", "isPrimary": false }
    ]
  }
}
```

**與 v2.0 範例的差異**：v2.0 的 factors 權重加總只有 0.90（違反 AC-08-1）、只有五個因子、`finalScore` 與加權結果對不上（回推為 68.2 而非 90）、`action` 用了與決策列舉不對齊的 `TRIAL`、數量為自由字串。以上全部修正。

---

# 9. 前端設計規範

## 9.1 專案結構

`ssds-frontend` 為獨立 repository，不置於後端專案內。

```
ssds-frontend/src/app/
├── core/
│   ├── auth/            AuthService、JwtInterceptor、RefreshInterceptor、AuthGuard、RoleGuard
│   ├── http/            ErrorInterceptor、LoadingInterceptor
│   └── models/          共用 TypeScript 介面
├── shared/
│   ├── components/      ScoreBar、GradeChip、RiskDot、FactorBar、AiPanel、EmptyState、
│   │                    ConfidenceBadge、PenaltyCard、SourceStatusStrip、PageHeader
│   ├── pipes/           PercentPipe(zh)、ScoreGradePipe、RelativeTimePipe、TimeGapPipe
│   └── directives/      HasPermissionDirective
├── features/
│   ├── auth/            login
│   ├── dashboard/
│   ├── products/        list / form / detail
│   ├── ranking/         四榜分頁
│   ├── trends/
│   ├── ai-tasks/
│   ├── weights/         權重組設定 + 校準審核
│   ├── heat-tags/       人工熱度標記 + 來源狀態
│   ├── sourcing/        B 軌尋源探索 + 優先序
│   ├── festivals/       節慶檔期維護
│   ├── imports/
│   ├── risks/
│   ├── decisions/
│   ├── reports/
│   ├── audit/           稽核紀錄（v3.0 自 admin 拆出）
│   └── admin/
├── api/                 由 openapi.json 產生的 client 與型別（不手改）
└── layout/              MainLayout（頂列 + 側邊選單）
```

技術約定：

- 一律使用 **standalone components**，不使用 NgModule
- 狀態以 **signals** 管理，`computed()` 處理衍生值
- 所有 feature 採 **lazy loading**（`loadComponent` / `loadChildren`）
- HTTP 呼叫集中在各 feature 的 `*.service.ts`，component 不直接使用 `HttpClient`
- `api/` 目錄為 openapi-generator 產出，**不得手動修改**；契約變更時重新產生
- API base URL 一律由 `environment.ts` 提供，不得硬編碼
- `RefreshInterceptor` 攔截 `401 TOKEN_EXPIRED`，自動換發後重送原請求；換發失敗才導回登入（§FR-01）

## 9.2 路由表

守衛一律明列角色代碼，不使用 `+` 記法。權限欄的數字為 §2.1 的列號。

| 路徑 | 畫面 | 守衛 | §2.1 列 |
|---|---|---|---|
| `/login` | S-01 登入 | — | — |
| `/dashboard` | S-02 儀表板 | Auth | 1 |
| `/products` | S-03 品項清單 | Auth | 2 |
| `/products/new`、`/products/:id/edit` | S-04 品項表單 | Auth + Role(`BUYER`,`BUYER_LEAD`,`DATA_ADMIN`,`SYS_ADMIN`) | 4 |
| `/products/:id` | S-06 品項詳情 | Auth | 2 |
| `/ranking` | S-05 分數排行（四榜） | Auth | 2 |
| `/trends` | S-07 趨勢分析 | Auth | 2 |
| `/ai-tasks` | S-08 AI 任務中心 | Auth | 2 |
| `/weights` | S-09 權重設定 | Auth + Role(`BUYER_LEAD`) | 15、16 |
| `/weights/calibration` | S-19 權重校準審核 | Auth + Role(`BUYER_LEAD`) | 17 |
| `/imports` | S-10 資料匯入 | Auth + Role(`BUYER_LEAD`,`DATA_ADMIN`,`SYS_ADMIN`) | 22 |
| `/risks` | S-11 風險示警 | Auth | 2 |
| `/decisions` | S-12 決策紀錄 | Auth | 2 |
| `/reports` | S-13 報表中心 | Auth | 3 |
| `/admin` | S-14 系統設定 | Auth + Role(`SYS_ADMIN`) | 23、24 |
| `/heat-tags` | S-15 人工熱度標記 | Auth + Role(`BUYER`,`BUYER_LEAD`,`DATA_ADMIN`,`SYS_ADMIN`) | 7 |
| `/heat-tags/sources` | S-16 熱度來源狀態 | Auth + Role(`SYS_ADMIN`) | 19 |
| `/sourcing` | S-17 尋源探索 | Auth + Role(`BUYER`,`BUYER_LEAD`,`SYS_ADMIN`) | 8 |
| `/sourcing/queue` | S-18 尋源優先序 | Auth | 2 |
| `/festivals` | S-20 節慶檔期維護 | Auth + Role(`BUYER_LEAD`,`DATA_ADMIN`,`SYS_ADMIN`) | 20 |
| `/audit-logs` | S-21 稽核紀錄 | Auth + Role(`BUYER_LEAD`,`SYS_ADMIN`) | 25 |

**頁面級與動作級守衛分開**：路由守衛控制「能不能進這一頁」，頁內的寫入動作另由 `HasPermissionDirective` 依 §2.1 控制。例如 `/products/:id` 對所有已登入者開放，但「建立決策」按鈕只對第 11 列的角色顯示。前端的顯示控制**不是安全邊界**，後端 `@PreAuthorize` 才是。

## 9.3 共用元件規格

| 元件 | Input | 說明 |
|---|---|---|
| `<ssds-score-bar>` | `value`、`max`、`showValue` | 分數水平長條，依分數區間變色 |
| `<ssds-grade-chip>` | `grade`、`scene` | 分級徽章；tooltip 顯示該榜的門檻值 |
| `<ssds-factor-bar>` | `factor` | 加分因子長條；`dataAvailable = false` 顯示灰底空條，`imputed = true` 顯示斜紋 |
| `<ssds-penalty-card>` | `penaltyFactors` | 扣分卡片，紅色系；**扣分為 0 的項目仍顯示**（AC-05-4） |
| `<ssds-risk-dot>` | `severity`、`tooltip` | 風險紅點，hover 顯示風險摘要 |
| `<ssds-ai-panel>` | `insight`、`type` | AI 產出的統一容器，含模型/時間/來源標註與「AI 生成」標籤 |
| `<ssds-confidence-badge>` | `confidence`、`deductions` | 信心度標籤，**< 50 顯示警示**；hover 顯示扣分明細 |
| `<ssds-source-status-strip>` | `sources` | 熱度來源狀態列，三態燈號與額度用量 |
| `<ssds-time-gap-chip>` | `gapDays` | B 軌時效落差，依三段變色 |
| `<ssds-empty-state>` | `icon`、`title`、`description`、`action` | 空狀態 |

> **信心度警示門檻統一為 50**（v3.0）。v2.0 的 §5.9 寫 < 50、§9.3 的元件寫 < 60，兩處不一致。

## 9.4 UI 規範

| 項目 | 規範 |
|---|---|
| 主色 | `#123A56`（深藍，取自簡報） |
| 強調色 | `#1E7A9E`（藍綠，分數條、連結） |
| 分級色（圖形／填色） | A `#1E7A4D`、B `#B8791A`、C `#8A9299` |
| 分級色（文字／徽章） | A `#16653F`、B `#8A5A0F`、C `#61696F` |
| 風險色（圖形／填色） | 高 `#C0392B`、中 `#D68910`、低 `#7F8C8D` |
| 風險色（文字） | 高 `#C0392B`、中 `#9A6208`、低 `#61696F` |
| 背景 | 頁面 `#F4F6F8`，卡片 `#FFFFFF` |
| 字體 | 標題 Noto Serif TC；內文與數字 Noto Sans TC；數字欄位加 `font-variant-numeric: tabular-nums` |
| 圓角 | 卡片 12px、按鈕 8px、徽章 4px |
| 間距 | 8px 基準網格 |
| 表格 | Angular Material Table，斑馬紋、可排序、sticky header |
| 載入 | 骨架屏（skeleton）優先於 spinner |
| 通知 | MatSnackBar，成功 3 秒、錯誤需手動關閉 |
| 破壞性操作 | 一律二次確認對話框 |
| 最小解析度 | 1366×768（採購人員以桌機為主，不做手機版，但需可在平板橫向瀏覽） |
| 無障礙 | 所有互動元素可鍵盤操作，focus 樣式清晰，**文字與背景對比 ≥ 4.5:1**，圖形與 UI 元件對比 ≥ 3:1 |

> **色彩對比修正（v3.0）**。v2.0 用同一組顏色同時做文字與圖形，但其中 B 級 `#B8791A` 於白底的對比約 3.2:1、風險中 `#D68910` 約 2.4:1、C 級 `#8A9299` 約 2.9:1，皆未達 §9.4 自訂的 4.5:1 標準。v3.0 拆為兩組：填色維持原值（僅需 3:1），文字改用加深版。對比值須於 UAT 前以自動化工具驗證，結果記入 `docs/a11y-check.md`。

## 9.5 AI 內容的視覺區隔

所有 LLM 產出必須：

1. 置於淺色底的 `<ssds-ai-panel>` 內，與系統計算的數字明顯區隔
2. 面板右上角顯示 `✦ AI 生成` 標籤與生成時間
3. 底部顯示來源說明（如「分析自 1,240 則評論 · nemotron-3.5-lightning · prompt rec-v3.1」）
4. 命中快取時額外顯示「沿用 x 日前的分析結果」
5. 提供「重新生成」與「回報不準確」兩個動作
6. 固定標示「AI 產出僅供參考，最終決策由採購人員負責」

**唯一的例外是情境判定區塊**（§FR-05 區塊 A）：它會影響分數，因此不放在 AI 面板內，而是以獨立區塊呈現，並強制顯示信心值、理由、依據訊號與覆寫入口。

---

# 10. 非功能需求

| 類別 | 需求 |
|---|---|
| **前端效能** | 一般頁面首屏 < 2 秒（500 筆品項資料量）；清單 API P95 < 500ms |
| **評分計算效能** | 單品項因子計算與扣分 < 200ms；**批次評分 500 品項 < 60 秒（純計算，不含 LLM 呼叫）** |
| **AI 分析效能** | 單品項完整 AI 分析（4 次呼叫）< 90 秒；**批次 AI 分析 150 品項（單輪上限）< 40 分鐘**；使用者不需等待（非同步任務 + 通知） |
| **容量** | 品項 10,000、評論 500,000、銷售紀錄 1,000,000 筆下仍符合上述效能。**此為儲存與查詢容量，非單輪 AI 分析量**（後者上限見 §6.7.3） |
| **可用性** | 展示環境目標 99%；AI 服務不可用時系統核心功能（評分、清單、決策、排行）仍可運作 |
| **安全** | HTTPS；密碼 BCrypt cost 10；JWT 簽章 HS256（金鑰 ≥ 256 bits）；refresh token 以 SHA-256 雜湊存放且可撤銷；所有輸入經 Jakarta Validation；JPA 參數化查詢防 SQL Injection；Angular 預設 XSS 防護，禁用 `bypassSecurityTrust*` |
| **金鑰管理** | `OPENROUTER_API_KEY` 等外部服務憑證**以環境變數為單一來源**，不存資料庫；`/admin/ai-config` 只能設定模型別名對應、預算與快取，不能設定或讀取金鑰 |
| **資料保護** | 會員資料僅接受去識別化匯入（`audience_segment` 只存統計）；送往 LLM 的內容經 `PromptSanitizer` 白名單過濾與評論去識別化（§6.8）；所有匯出檔案記錄稽核 |
| **日誌** | 使用 SLF4J + Logback，JSON 格式；每個請求帶 traceId；AI 呼叫記錄 prompt 版本、模型別名、實際 model ID、token、請求數、耗時、是否命中快取。**LLM 原始回應只入日誌，不入資料庫** |
| **稽核** | §FR-18 所列動作全數寫入 `audit_log`，保留 1 年；備份策略須涵蓋該期間（§3.4） |
| **瀏覽器支援** | Chrome / Edge 最新兩版 |
| **語言** | 介面全繁體中文（台灣用語）；程式碼註解與 commit message 中英皆可 |
| **可維護性** | 後端測試覆蓋率 ≥ 60%（核心評分引擎 ≥ 85%）；所有 API 有 OpenAPI 文件 |

> **效能指標拆分（v3.0）**。v2.0 同時寫「批次評分 500 品項 < 60 秒」與「512 次 LLM 請求 ÷ 20 次/分 ≈ 26 分鐘」，兩者只有在「評分不含 LLM」的前提下才能並存，但 SceneClassifier 明顯在評分路徑上。v3.0 拆為「評分計算」與「AI 分析」兩條指標，並明確說明評分不因 AI 未完成而缺席（沿用上次情境判定）。
>
> **金鑰來源統一（v3.0）**。v2.0 的 §10 說「API Key 以 AES-256 加密存於資料庫」，附錄 B 又列為環境變數，兩處衝突。v3.0 統一為環境變數——資料庫存放需要額外的金鑰管理（加密金鑰本身還是得放環境變數），在專題規模下只增加複雜度不增加安全性。

---

# 11. 測試計畫

| 層級 | 範圍 | 工具 | 標準 |
|---|---|---|---|
| 單元測試 | 評分引擎、六因子計算、三扣分規則、正規化、權重分攤、時間窗函數、狀態機、`PromptSanitizer` | JUnit 5 + Mockito | 核心邏輯覆蓋率 ≥ 85% |
| 整合測試 | Repository、API 端點、Flyway migration | Testcontainers (MySQL) + MockMvc | 每個 API 至少 1 正例 1 反例 |
| AI 測試 | Agent 輸出解析與降級 | 固定測試資料集 + Schema 驗證；LLM 呼叫以 WireMock 假回應 | **假回應下 Schema 解析通過率 100%** |
| 模型基準測試 | 真實免費模型的能力 | §6.7.5 的六個測項 | **真實模型 Schema 通過率 ≥ 95%** |
| 前端單元 | 服務、pipe、共用元件、守衛 | **Vitest**（或 Web Test Runner） | — |
| 端對端 | 六條主要流程 | Playwright | 全數通過 |
| 效能測試 | 清單、排行 API 與批次評分 | JMeter，50 併發 | P95 < 500ms；500 品項評分 < 60 秒 |
| 無障礙 | 色彩對比、鍵盤操作 | axe-core 或同等工具 | 文字對比 ≥ 4.5:1，無 critical 問題 |
| 使用者驗收 | 由客戶採購人員實際操作 | — | 完成 §14 的 UAT 情境 |

> **兩個 Schema 通過率標準量測的對象不同**（v3.0 註明）。「100%」測的是 WireMock 固定假回應下，本系統的解析與降級邏輯是否正確——這是我們自己的程式碼，必須全對。「≥ 95%」測的是真實免費模型的格式遵循度——這是外部模型的行為，不可能要求 100%，所以才需要重試、備援與降級四層防護。v2.0 兩處並列但未說明，容易被讀為互相衝突。

## 11.1 評分引擎的黃金案例測試

§5.5 的計算範例作為迴歸測試的固定案例，任何評分邏輯變更都必須通過：

| 測試 | 斷言 |
|---|---|
| `bonusSubtotal` | 86.89（±0.01） |
| `penaltySubtotal` | 4.00 |
| `finalScore` | 82.89 |
| `grade` | `B`（VIRAL 榜 A 門檻 85） |
| `confidence` | 86 |
| 六因子 `contribution` 加總 | 等於 `bonusSubtotal` |
| 六因子 `weight` 加總 | 1.000 |
| 11 月變體 | `finalScore` 90.69、`grade` `A` |

同一組數字亦用於 §FR-05 的畫面與 §8.3 的 API 回應，三者由同一份測試 fixture 產生，避免文件與實作再度分歧。

## 11.2 必要的專項測試

| 項目 | 測試內容 | 對應 |
|---|---|---|
| 資料外送 | `PromptSanitizer` 對每個 Agent 的輸入組裝結果，斷言不含 `cost`、`suggestedPrice`、`marginRate` 原始值、`actualQty`、供應商名稱、客群價格帶 | §6.8、§14 第 6 項 |
| 評論去識別化 | 含帳號、電話、Email、地址、訂單編號的評論樣本，斷言送出內容已遮罩 | §6.8.2 第 2 條 |
| MCP 白名單 | 白名單外的工具呼叫被拒絕並寫入 `audit_log` | §FR-16-1、AC-16-7 |
| 權重加總 | 任一組權重加總不為 1.000 時儲存被拒（`409 WEIGHT_SUM_INVALID`） | AC-08-1 |
| 降級鏈 | 主模型 5xx → 備援 → 降級的三段路徑，斷言分數仍產出 | §6.7.4 |
| 配額耗盡 | A 軌配額用盡時，分數仍產出且沿用上次 AI 結果 | §6.7.3 |
| 多情境評分 | 同品項同期產生兩筆 score，唯一鍵不衝突，KPI 去重正確 | AC-04-6、AC-02-6 |
| 軟刪除 | 已刪除品項不出現在清單、排行、評分批次與報表 | AC-03-5 |
| 狀態機 | §7.4 表中的每一條允許轉換皆通過，表外轉換回 409 | §7.4 |
| 時間窗邊界 | `d = L`、`d = L+30`、`d = 0`、`d = -1` 四個邊界點的窗權重 | §FR-17-1 |

## 11.3 E2E 主要流程

1. 登入 → 新增品項 → 觸發 AI 分析 → 查看詳情（六因子 + 三扣分展開）→ 建立決策
2. 匯入銷售 CSV → 驗證 → 確認匯入 → 重算因子 → 排行變動（且未消耗 LLM 配額）
3. 建立權重版本草稿 → 模擬試算 → 核准生效 → 全量重算 → 分級改變
4. 風險示警產生 → 確認/忽略 → 稽核紀錄可查
5. 決策 → 標記結案 → 回填實際銷售 → 準確度報表更新
6. B 軌：人工熱度標記 → 發起尋源探索 → 時效落差判定 → 成案轉軌 → 熱度資料完整保留

> 第 6 條為 v3.0 新增。v2.0 的五條 E2E 完全沒有涵蓋 B 軌，而 B 軌是 v2.0 新增的兩大模組之一。

---

# 12. 開發時程

以專題製作的期中／期末為節點，共 16 週。

## 12.1 範圍分層

功能模組共 18 個、資料表約 40 張。16 週無法涵蓋全部範圍，因此區分**必做**與**次要**兩層：

### 必做（驗證核心價值）

| 優先序 | 項目 | 對應 FR |
|---|---|---|
| 1 | Threads + Google Trends 熱度採集與合成 | FR-06、FR-14-2 |
| 2 | 加分／扣分分離的評分引擎（六因子 + 三扣分） | FR-04、§5 |
| 3 | 情境權重組 + AI 選組 + 版本管理 | FR-08、FR-05 |
| 4 | 人工熱度標記介面 | FR-14-1 |
| 5 | 回饋閉環：決策、結案、回填、決策快照 | FR-11 |

### 次要（時程允許再做）

| 優先序 | 項目 | 對應 FR | 降級方案 |
|---|---|---|---|
| 6 | B 軌尋源探索 | FR-16 | 僅做清單與時效落差計算，AI 探索改為手動填寫探索報告 |
| 7 | 節慶／氣候時間窗 | FR-17 | 節慶先做（時間窗函數為純計算，成本低）；**氣候因子若 `category_climate_profile` 資料不可得，則該因子整體停用**，權重由其餘五因子分攤 |
| 8 | 價格帶適配度 | §5.2.4 | 若客戶無法提供客群價格帶，該因子標為無資料，權重分攤；**須同步在對客戶說明中揭露 P-3 未被評分引擎涵蓋** |
| 9 | 權重校準模組 | FR-15 | **僅做歷史回測，不做線上校準** |

> 第 8 項為 v3.0 新增的分層。v2.0 把 `PRICE_FIT` 定義為因子卻沒有納入權重也沒有資料表，等於默默不做——但它是唯一對應痛點 P-3 的因子，不做就必須明講。

### 關於 FR-15 的建議做法

專題期間將「權重校準」實作為**歷史回測**：以客戶提供的過往開團紀錄，比較「校準後權重 vs 平權」的預測準確度。

這是在一學期內誠實展示「AI 會越來越準」的唯一可行方式——線上校準需要至少 200 筆已回填的開團案例（§FR-15 的算術），專題期間不可能達成。此項成立與否取決於附錄 A 第 4 項。

## 12.2 時程表

| 階段 | 週次 | 工作項目 | 交付 |
|---|---|---|---|
| **P0 準備** | W1 | 需求確認、環境建置、DB schema 設計、UI 原型確認、**Spring Boot 4.1／JDK 相容性驗證**、農曆函式庫選型、前端測試框架選型 | 本規格書、畫面示意圖、Flyway V7 |
| **P0.1 repo 初始化** | W1 | 建立 `ssds-backend` 與 `ssds-frontend` 兩個 repo、CI 骨架、OpenAPI 契約流程 | 兩 repo 可各自建置 |
| **P0.2 資料源驗證** | W2 | Threads API 審核申請、關鍵字資料量測試、Trends 穩定性驗證 | 資料源可行性報告 |
| **P0.3 模型基準測試** | W2 | 免費模型能力測試（§6.7.5）：Schema 遵循度、中文品質、長輸入、工具呼叫、延遲、可用性、**實際配額上限與重置標頭** | `model-benchmark.md`、定案的模型分派表與配額前提 |
| **P0.4 API 契約定稿** | W2 | 依 §8 產出 `openapi.json` 骨架，前端可產生 client | 契約 v1 |
| **P1 基礎** | W3–W5 | 登入與 token 換發、品項 CRUD（含軌別與軟刪除）、狀態機、類別／供應商／客群主檔、清單與篩選、Angular 版面骨架 | 可登入、可管理品項 |
| **P2 評分引擎** | W6–W8 | 六因子計算、同品類正規化、加分／扣分分離、權重分攤、情境權重組、版本管理與門檻、四榜分組排行 | 分數可算出，四榜可看，黃金案例測試通過 |
| **期中展示** | W8 | 完整展示：登入→建品項→評分→四榜排行→詳情（加扣分透明化） | 期中報告 |
| **P3 AI 整合** | W9–W11 | OpenRouter 串接、**Agent 1–5**（SceneClassifier／ReviewRisk／ProductInsight／Recommendation／TrendInterpreter）、任務中心、預算池、快取、Schema 驗證與四層降級、`PromptSanitizer` | 情境判定與 AI 分析可產出 |
| **P4 熱度資料層** | W12–W13 | Adapter 架構、Threads／Trends／IG 串接、合成與斜率、**人工熱度標記（FR-14-1）**、來源狀態與健康檢查、趨勢分析頁、CSV 匯入、風險示警 | 多來源熱度合成可用 |
| **P5 閉環與報表** | W14 | 決策快照、結案與回填、準確度分析、**歷史回測**、報表匯出、稽核紀錄頁 | 完整閉環 |
| **P6 收尾** | W15–W16 | 次要項目補做（依 §12.1 優先序取捨，含 **Agent 6 SourcingScout 與 Agent 7 WeightCalibration**）、測試補齊、無障礙檢查、效能調校、UAT 修正、文件與部署、簡報製作 | 期末展示 |

> **階段編號依週次排序**（v3.0）。v2.0 把 W1 的 repo 初始化編為 P0.7 卻排在 W2 的 P0.5／P0.6 之後，閱讀時容易誤判順序。
>
> **七個 Agent 全部排入時程**（v3.0）。v2.0 的 P3 只列五個 Agent，SourcingScoutAgent 與 WeightCalibrationAgent 未出現在任何階段。

**里程碑檢核**

| 里程碑 | 週次 | 檢核點 |
|---|---|---|
| M0-A | W2 | Threads API 可取得台灣關鍵字資料，且資料量足以計算斜率 |
| M0-B | W2 | 免費模型基準測試完成；至少一組主／備模型的 Schema 通過率 ≥ 95%；**實際每分鐘／每日配額已量得，§6.7.3 的單輪上限已依實測值調整** |
| M0-C | W2 | 前後端 repo 各自可建置，OpenAPI 契約可產出並被前端消費 |
| M0-D | W1 | Spring Boot 4.1 與目標 JDK 版本相容性確認 |
| M1 | W5 | 品項 CRUD 全通過整合測試；軌別、軟刪除與狀態機正常運作 |
| M2 | W8（期中） | **500 筆品項純計算評分 < 60 秒**；黃金案例測試通過；加分與扣分可分別展開查看原始值；四榜可切換 |
| M3 | W11 | WireMock 假回應下 Schema 解析通過率 100%；SceneClassifier 失敗時可正確退回預設權重組；`PromptSanitizer` 專項測試通過 |
| M4 | W13 | 任一熱度來源停用時，系統仍產出完整排行並正確標示缺漏，且 `applied_weights` 記錄實際比例 |
| M5 | W14 | 六條 E2E 流程全通過；回測可比較兩個權重版本 |
| M6 | W16（期末） | UAT 驗收簽署 |

**M0 的重要性**：熱度資料源與模型配額是整個系統的關鍵路徑。若 W2 無法通過，需在 P1 開始前重新調整資料源配置與 AI 分析規模，而非等到 P4 才發現。

---

# 13. 風險與對策

**單一編號序列 R-01 ～ R-21。** v2.0 的 §13 用 `R-1`…`R-8`、§13.1 用 `R-08`…`R-19`，`R-8` 與 `R-08` 是兩個不同風險卻共用一個編號，在追蹤表與會議紀錄中必然混淆。v3.0 合併為單一表並統一為兩位數。

| 編號 | 風險 | 影響 | 對策 |
|---|---|---|---|
| R-01 | 客戶無法提供歷史銷售/評論資料 | 高－`CVR` 因子與評論風險扣分失效 | 準備公開資料集作為展示備案；缺值走 §5.2.3 的三段退路並標示低信心度；列為附錄 A 第 1 項 |
| R-02 | Google Trends 無官方 API，非官方套件不穩定 | 中－趨勢因子無法更新 | Adapter 架構；任一來源降級時其餘自動重新正規化（§5.3.2） |
| R-03 | 電商平台反爬，評論無法自動取得 | 中－評論資料來源受限 | 第一階段以 CSV 匯入為主要路徑；規格中不承諾自動爬取（附錄 C） |
| R-04 | LLM 輸出不穩定或格式錯誤 | 中－AI 功能不可靠 | 四層防護：Schema 驗證 + 重試 + 備援模型 + Agent 降級（§6.5、§6.7.4） |
| R-05 | 評分結果不被採購人員信任 | 高－系統不被使用 | 因子透明化（九項全列）、原始值可查、加減分分離、權重可調、回饋閉環證明準確度；定位為輔助而非取代 |
| R-06 | 期中前功能做不完 | 中－展示不完整 | 評分引擎優先於 AI（M2 不含 AI），確保期中有可展示的完整閉環 |
| R-07 | 團隊對 Angular signals / opencode 不熟 | 中－進度延誤 | W1–W2 安排技術驗證（spike），先跑通最小可行呼叫鏈 |
| R-08 | Threads API 審核未通過或資料量不足 | 高－主力熱度來源失效 | W2 設 M0-A 檢核點；備案為提高人工標記合成權重、加購第三方資料 |
| R-09 | 客戶無過往開團紀錄可供回測 | 高－「越用越準」無法展示 | 附錄 A 第 4 項列為最高優先確認；若不可得，改以機制展示替代成效展示 |
| R-10 | 結案回填執行率過低 | 高－FR-15 完全失效 | 欄位壓在 5 個以內、`campaign_end_date` 起算的儀表板逾期提醒；於合約明列為客戶責任 |
| R-11 | 客戶要求納入爬蟲 | 中－法律風險 | 附錄 C 已載明評估；MCP 工具白名單同樣排除爬蟲型工具；若客戶堅持，須由客戶法務評估並於合約明訂責任歸屬 |
| R-12 | 範圍擴大導致時程不足 | 中 | §12.1 已分必做／次要兩層，次要項目定義降級方案 |
| R-13 | 情境判定覆寫率過高 | 中－AI 選組價值受質疑 | 覆寫率納入 §FR-11-3 指標；持續偏高時檢討權重組定義而非增加 AI 自由度 |
| R-14 | 非官方 Google Trends API 失效 | 中 | 同 R-02；`heat_composite_daily.applied_weights` 完整記錄降級歷程 |
| R-15 | 免費模型下架或改為付費 | 高－AI 功能全面失效 | 邏輯別名 + 環境變數對應，換模型不改程式碼；每個別名配置備援；`free-models-router` 作為最後手段 |
| R-16 | 免費模型日配額不足以完成全量評分 | 高 | §6.7.3 的單輪上限 `LLM_BATCH_ITEM_CAP`；W2 量得實際配額後校正；快取分箱、失敗項隔日續跑；**分數不因 AI 未完成而缺席** |
| R-17 | 免費端點的資料政策導致客戶營運資料外流 | 高－信任與合約風險 | §6.8 資料外送原則；`PromptSanitizer` 白名單 + 評論去識別化 + 單元測試把關；於 UAT 文件明確揭露 |
| R-18 | 免費模型 JSON 遵循度不足 | 中 | 同 R-04；W2 基準測試設 95% 通過率門檻 |
| R-19 | 前後端 repo 分離導致契約不同步 | 中 | OpenAPI 為單一事實來源，後端 CI 產出、前端 CI 消費；破壞性變更須先部署後端並保留舊欄位 |
| R-20 | **客戶無法提供客群價格帶資料** | 中－`PRICE_FIT` 因子失效，痛點 P-3 未被評分引擎涵蓋 | 附錄 A 第 12 項；若不可得，依 §12.1 第 8 項降級並於對客戶說明中明確揭露 |
| R-21 | **Spring Boot 4.1 要求的 JDK 版本高於部署環境** | 中－需調整部署環境或降版 | W1 設 M0-D 檢核點；備案為降至 Spring Boot 3.x LTS，本規格的功能設計不受影響 |

> R-20 與 R-21 為 v3.0 新增，對應本次修訂發現的兩個未揭露前提。

---

# 14. 驗收標準

專案通過驗收需同時滿足：

1. §4 所有 `AC-xx-x` 驗收準則全數通過
2. §11.3 的**六條** E2E 流程全數通過
3. §11.2 的**十項專項測試**全數通過
4. §10 非功能需求的效能指標達標（評分計算與 AI 分析兩條指標分別驗收）
5. §11.1 的**黃金案例測試**通過，且 §5.5、§FR-05、§8.3 三處的數字與測試 fixture 完全一致
6. **資料外送驗證**：以單元測試證明 `PromptSanitizer` 不會將機敏欄位（成本、售價、毛利率原始值、實際銷量、實現毛利、客群價格帶、供應商名稱）送往外部 LLM，且評論已去識別化
7. **MCP 白名單驗證**：以設定檔檢查與單元測試證明白名單外的工具呼叫被拒絕
8. **必做項目**（§12.1）全數交付；次要項目若採降級方案，須於交付文件中明確載明實際交付範圍與未涵蓋的痛點
9. **雙 repo 交付**：`ssds-backend` 與 `ssds-frontend` 各自可獨立建置與部署；OpenAPI 契約可正確產出並產生前端 client
10. 客戶採購人員完成以下 UAT 情境並簽署：
    - 能在 3 分鐘內完成一個新品項從建檔到看到分數與 AI 建議
    - 能說出某個 A 級品項「為什麼是 A 級」，並分別指出六個加分因子與三個扣分項的來源
    - 能說出某個高熱度品項為何被壓成 B 級（扣分硬規則有效）
    - 能說出同一個品項在不同月份為何分數不同（節慶時間窗與氣候因子有效）
    - 能在 30 秒內完成一筆人工熱度標記
    - 能自行切換情境權重組並看到排行變化，且理由被記錄
    - 能完成一筆決策 → 結案 → 回填，並看到準確度指標更新
    - 能匯入一份自有銷售 CSV 並看到分數更新
11. 交付文件齊全：本規格書、API 文件（Swagger／OpenAPI）、模型基準測試報告（含實測配額）、資料庫 schema 說明與 migration 腳本、兩個 repo 各自的部署手冊、使用者操作手冊、資料來源說明（含附錄 C 之法律評估）、資料外送原則說明（§6.8）、無障礙檢查報告

---

# 15. v2.0 → v3.0 修訂對照

本次修訂共處理 82 項問題，分三級。以下為裁決摘要，各項的完整說明見對應章節。

## 15.1 阻斷級（22 項）

規格內部互相矛盾，兩種讀法會導出兩種資料模型或兩套數字。

| # | 問題 | 裁決 | 章節 |
|---|---|---|---|
| B-01 | 加分小計「76.3 → 91」的二次換算未定義；§5.5／§8.3／§FR-05 三處數字互斥；§8.3 權重加總只有 0.90 | 取消二次換算；建立單一黃金案例貫穿三處與測試 | §5.5、§11.1 |
| B-02 | 加分因子定義 7 個，權重組只有 5 欄；`PRICE_FIT`（唯一對應 P-3 的因子）與 `heat_volume` 進不了評分 | `PRICE_FIT` 納入權重（四組重新分配至 100%）；`heat_volume` 改為門檻條件 | §5.2.1、§FR-08 |
| B-03 | 排行三榜 vs 權重組四組，`SEASONAL` 無歸屬榜 | 改為四榜，與四組情境一對一 | §FR-04 |
| B-04 | 「同品項可上多榜」vs `product_score` 單一 `scene_type` | 每個適用情境各一筆 score，加 `is_primary`／`is_active` 與唯一鍵 | §FR-04、§7.2.6 |
| B-05 | B 軌兩套模型：`product.track_type` vs `sourcing_candidate.keyword_id` | 一律以 `product` 為主體，`sourcing_candidate` 改綁 `product_id` | §FR-16-2、§7.2.9 |
| B-06 | `decision_feedback` 與 `campaign_result` 重複，API 也重複 | 廢除 `decision_feedback` 與 `PUT /decisions/{id}/feedback` | §7.5、§8.2 |
| B-07 | `weight_version` 在 §7.2 定義兩次，欄位互斥 | 合併為單一定義 | §7.2.5 |
| B-08 | 「移除情感欄位」vs 負評率、情緒分佈、API 三處都要用 | 保留 `sentiment`，只取消其加分用途 | §5.2.2、§7.2.4 |
| B-09 | `BUYER+`／`DATA_ADMIN+` 記法，但角色非全序 | 廢除 `+` 記法，全部明列角色；§2.1 為唯一事實來源 | §2.1、§8.2、§9.2 |
| B-10 | 稽核權限：§2.1 給 BUYER_LEAD vs AC-13-3 僅 SYS_ADMIN | 拆出獨立模組 FR-18 與 `/audit-logs` 路由 | §FR-18、§9.2 |
| B-11 | 500 品項 × 4 Agent = 2,000 次 > 日配額 1,000（A 軌 700） | 新增 `LLM_BATCH_ITEM_CAP`（預設 150）；容量目標與 AI 分析量分開表述 | §6.7.3、§10 |
| B-12 | 「500 品項 < 60 秒」vs「512 請求 ≈ 26 分鐘」 | 拆為「評分計算」與「AI 分析批次」兩條指標 | §10、§12 M2 |
| B-13 | 風險門檻生效時機三種說法 | 統一為「立即觸發背景全量扣分重算」 | §FR-10-3、§5.10 |
| B-14 | 節慶時間窗區間端點重疊；範例「62 天 → 0.5」代不進公式 | 改為明確不等式；範例改為「60 天、進口食品 L=45 → 1.0」 | §FR-17-1 |
| B-15 | 氣候因子無映射規則、`product` 無氣候偏好欄位 | 新增 `ideal_temp_min/max` 與 `category_climate_profile`，定義映射函數 | §FR-17-2、§7.2 |
| B-16 | `PRICE_FIT` 依賴「會員輪廓」但無對應資料表 | 新增 `audience_segment` 與 `category_audience_mix`，定義計算式 | §5.2.4、§7.2.2 |
| B-17 | 快取 7 日 vs 每週評分 → 情境判定永遠命中快取 | `AI_CACHE_DAYS` 改為 6；新增各 Agent 的 cache key 組成表 | §6.5 |
| B-18 | 「結案」無時間欄位承載，逾期天數算不出來 | 新增 `campaign_end_date` 與「標記結案」流程步驟 | §FR-11-2、§7.2.8 |
| B-19 | 合成熱度無儲存表，90 天曲線與斜率無資料來源 | 新增 `heat_composite_daily`；廢除 `trend_daily` | §7.2.3、§7.5 |
| B-20 | MCP「社群資料工具」資料來源未定義，可能繞開附錄 C | 建立工具白名單，明確排除爬蟲型工具 | §FR-16-1、附錄 C |
| B-21 | opencode 角色：§3.1 全流量經 opencode vs §6.2 A 軌直連 | A 軌 REST 直連、B 軌經 opencode + MCP；重畫架構圖 | §3.1、§6.2 |
| B-22 | `R-8`（§13）與 `R-08`（§13.1）撞號 | 合併為單一風險表 R-01～R-21 | §13 |

## 15.2 語意不明（35 項）

可實作但會被各自解讀，補上定義。

| # | 問題 | 裁決 | 章節 |
|---|---|---|---|
| C-01 | 扣分正負號三種寫法 | 一律存 0–40 正值，負號只在 UI | §FR-04、§5.5 |
| C-02 | 權重分攤／中位數填補／`is_imputed`／`data_available` 四者並存 | 明確分工：`data_available=false` 不加權、`is_imputed=true` 正常加權但扣信心度 | §5.7、§7.2.6 |
| C-03 | 因子代號兩套（`heat_slope` vs `TREND`） | 全文統一為大寫 `factor_code` | §5.2.1 |
| C-04 | 熱度階段三套詞彙（5 值／3 值／中文） | 統一為 `RISING`／`PLATEAU`／`DECLINING`；移除 `trend_keyword.lifecycle` | §FR-06、§7.2.2 |
| C-05 | 人工標記 1–5 → 讀值／百分位／斜率的換算未定義 | 新增五步驟換算規則，含稀疏期向前填補 | §FR-14-1-a |
| C-06 | 階梯衰減 vs 變數名暗示指數半衰 | 確定為階梯；變數更名 `HEAT_TAG_HALVE_AFTER_DAYS` | §FR-14-1-a、附錄 B |
| C-07 | 「加權平均」權重來源未定義；漏列 2 人係數 | 以時間衰減權重加權；三段係數完整列出 | §FR-14-1-a |
| C-08 | 信心度無起算值與下限 | `clamp(100 − Σ扣分, 0, 100)` | §5.9 |
| C-09 | 信心警示門檻 §5.9 為 50、§9.3 為 60 | 統一為 50 | §5.9、§9.3 |
| C-10 | 情境信心 0.5 與 0.7 兩門檻未說明關係 | 明列「採用門檻」與「計分門檻」 | §FR-05 |
| C-11 | 分級門檻依類別還是依榜？且無資料表 | 統一依榜；新增 `grade_threshold` 表並綁權重版本 | §FR-08、§7.2.5 |
| C-12 | 「A 級主推數（三榜合計）」在多榜前提下重複計數 | 改為以 `product_id` 去重 | §FR-02 |
| C-13 | 時效落差 +14 邊界重疊 | 改為 `> 14`／`0 ≤ x ≤ 14`／`< 0` | §FR-16-1、§5.8 |
| C-14 | `sourcing_status` 四值 vs 五值 | 統一為五值（含 `URGENT`），以英文 code 為準 | §FR-03-1 |
| C-15 | 風險示警無列舉與觸發規則；「供應商異常」無資料源 | 補完整 ENUM 與九條觸發規則；供應商異常移出範圍 | §FR-10-1、§7.2.8 |
| C-16 | 「潛在風險 2–4 點」無 Agent 負責 | `SellingPointAgent` 擴充為 `ProductInsightAgent`，同時產出賣點與風險 | §6.3 Agent 3 |
| C-17 | G-3「24 小時內示警」無實作定義 | 新增 §FR-10-2，含偵測排程、判定式與去重規則 | §FR-10-2 |
| C-18 | `heat_source.availability` 無探測者與排程 | 新增每 15 分鐘健康檢查與三態判定條件 | §FR-14-2 |
| C-19 | Agent 4 輸入「因子明細」違反 §6.8「只送百分位」 | 輸入改為六因子百分位，明確不含原始值 | §6.3 Agent 4 |
| C-20 | 免費模型無費用，但全篇以金額計量 | 主要度量改為請求數與配額百分比；金額降為次要 | §FR-07、§7.2.7 |
| C-21 | 有 refresh token 但 AC-01-2 直接導回登入 | 補完整換發流程與 `refresh_token` 表 | §FR-01、§7.2.1 |
| C-22 | API Key：環境變數 vs DB 加密 | 統一為環境變數；`/admin/ai-config` 不可設定金鑰 | §10、附錄 B |
| C-23 | 評論列為公開可送，但 §10 要求不含個資 | 新增五類遮罩規則於 `PromptSanitizer` | §6.8.2 |
| C-24 | 軟刪除無欄位 | 新增 `deleted_at`／`deleted_by` 與可見性規則 | §FR-03-2、§7.2.2 |
| C-25 | 狀態機未定義 | 新增 §7.4 狀態轉換表 | §7.4 |
| C-26 | AI `action` 與決策列舉無映射 | AI 直接輸出 `ADOPT`／`WATCH`／`REJECT` | §6.3 Agent 4、§FR-11-1 |
| C-27 | `quantitySuggestion` 字串 vs `first_order_qty` INT | 拆為 `qtyMin`／`qtyMax` 整數 | §6.3 Agent 4 |
| C-28 | 時區與 ISO 週界未定義 | 欄位存 UTC；ISO 週以 Asia/Taipei 判定 | §3.2 |
| C-29 | 樣本 <10 退回全品類，牴觸「同品類可比」原則 | 增加「兄弟品類合併」中間層並要求標示偏差 | §5.3.1 |
| C-30 | 匯入觸發重算是否含 LLM 未說明 | 明訂只重算因子與扣分，不消耗配額 | §FR-09 |
| C-31 | 「上限 10MB／50,000 列；超過改非同步」語意矛盾 | 拆為同步門檻／非同步門檻／硬上限三段 | §FR-09 |
| C-32 | IG「品類級」如何落入 keyword 維度 | `heat_reading` 新增 `category_id`，合成時套 0.5 粒度折扣 | §FR-06、§5.3.2 |
| C-33 | `manual_heat_tag` 兩欄皆可空，與必填規則矛盾 | 新增 CHECK 約束 | §7.2.3 |
| C-34 | 「扣分 ≥20 壓級」意味評論風險單項即觸發，是否刻意 | 明列為刻意設計並說明理由 | §FR-04、§5.6 |
| C-35 | §5.7「權重分攤」vs AC-04-3「不影響分數」 | AC-04-3 改為「不產生扣分」，並說明分攤確實改變分數 | §5.7、AC-04-3 |

## 15.3 一致性維護（25 項）

| # | 問題 | 裁決 | 章節 |
|---|---|---|---|
| D-01 | ERD 未含 v2.0 新表 | 重畫，涵蓋全部資料表 | §7.1 |
| D-02 | 資料表在「主要資料表」與「既有變更」兩處定義且不一致 | 表定義只保留 v3.0 最終狀態；migration 說明另置 §7.5 | §7.2、§7.5 |
| D-03 | §1.3 缺「越用越準」目標 | 新增 G-6 | §1.3 |
| D-04 | In Scope 未含 FR-14-1／15／16／17 | 補齊並依軌別分組 | §1.4 |
| D-05 | 端點重複列出四組 | 合併為單一完整清單，每個端點只出現一次 | §8.2 |
| D-06 | `/dashboard/top-products` 與「取消總榜」衝突 | 改為 `/dashboard/rankings?track=&scene=` | §8.2 |
| D-07 | `/products` 缺 `trackType`／`sourcingStatus` 參數 | 補入 | §8.2 |
| D-08 | `/scores/simulate` 為單組權重，與「版本試算」不符 | 改為以 `weightVersionId` 為基底，支援局部覆寫 | §8.2 |
| D-09 | 缺預算池即時查詢端點 | 新增 `GET /ai/budgets` | §8.2 |
| D-10 | `/reports` 有端點無資料表 | 新增 `report_job` | §7.2.11 |
| D-11 | 時程只列 5 個 Agent（共 7 個） | Agent 6／7 排入 P6 | §12.2 |
| D-12 | P0.7（W1）排在 P0.5／P0.6（W2）之後 | 依週次重排為 P0～P0.4 | §12.2 |
| D-13 | 附錄 B 缺多項必需變數 | 補齊並標註可否執行期覆寫 | 附錄 B |
| D-14 | Karma 已 deprecated | 改為 Vitest（或 Web Test Runner），W1 定案 | §3.2、§11 |
| D-15 | Angular「19 或 21」跳過 20 無理由 | 改為「≥ 19，建議 21」 | §3.2 |
| D-16 | Spring Boot 4.1 與 Java 17 相容性未驗證 | 列入 M0-D 與附錄 A 第 11 項；備案 R-21 | §3.2、§12.2 |
| D-17 | 模型分派表讀起來像已定案 | 加「候選」與「驗證狀態」欄 | §6.7.2 |
| D-18 | 分級色與風險色未達 4.5:1 對比 | 拆為填色組與文字組，文字改用加深版 | §9.4 |
| D-19 | 備份 14 份 vs 稽核保留 1 年 | 增加週／月保留層級 | §3.4 |
| D-20 | 「200 筆需一至兩年」與月開團數十團算術不符 | 改以回填執行率說明落差 | §FR-15 |
| D-21 | Schema 100% vs 95% 看似衝突 | 註明兩者量測對象不同（假回應 vs 真實模型） | §11 |
| D-22 | 權限矩陣未含 v2.0 新功能，且權重與風險門檻混為一列 | 重製為 25 列，涵蓋所有寫入型動作 | §2.1 |
| D-23 | `period CHAR(8)` 但值為 7 字元 | 改為 `CHAR(7)` | §7.2.6 |
| D-24 | `return_rate` 精度不一致；`realized_margin` 語意不明 | 統一為 `DECIMAL(5,4)`；更名 `realized_margin_rate` | §7.2.8 |
| D-25 | 「同編號畫面」但 FR-03 對應 S-03/S-04 | 改為「下表所列畫面編號」 | §4 |

---

# 附錄 A：待客戶確認事項

## 承接自 v1.0／v2.0（尚未解決）

| # | 項目 | 優先度 | 影響 |
|---|---|---|---|
| 1 | 歷史銷售資料的可得性與涵蓋範圍（是否含曝光數 `impression`） | 高 | 決定 `CVR` 因子能否使用，以及是否需走 §5.2.3 的替代算法 |
| 2 | 非官方 Google Trends API 的穩定性與存取方式 | 中 | 影響熱度來源配置 |
| 3 | 四榜 A／B 分級門檻校準（暫定值見 §5.6） | 中 | 各榜的分數分佈不同，需與採購團隊共同校準 |
| 4 | **過往開團紀錄是否可得？** | **最高** | 決定能否執行回測。若無，「AI 越用越準」的承諾須改變說明口徑（R-09） |
| 5 | 月開團數與平均決策週期、預期回填執行率 | 高 | 決定回饋閉環多久才具統計意義（§FR-15 的算術），直接影響期望管理 |
| 6 | Threads API 審核能否通過、台灣關鍵字資料量是否足夠 | 高 | 建議先執行 2 週技術驗證 spike 再定案規格（M0-A） |
| 7 | 四組情境權重的初始數值（六因子版） | 中 | 需與採購團隊共同校準 |
| 8 | 各品類尋源前置天數 | 中 | 由採購提供，同時影響 A 軌節慶因子與 B 軌時效落差 |
| 9 | 結案回填的營運責任歸屬與執行方式 | 中 | 無回填則 FR-15 完全無法運作（R-10） |
| 10 | 是否採用付費第三方社群資料商 | 低 | 若採用可補足 IG／TikTok／小紅書覆蓋，並可納入 MCP 白名單 |

## v3.0 新增

| # | 項目 | 優先度 | 影響 |
|---|---|---|---|
| 11 | **Spring Boot 4.1 的 GA 時程與 JDK 最低版本要求** | 高 | 決定部署環境的 Java 版本；不相容時的備案見 R-21。W1 內須確認（M0-D） |
| 12 | **主力客群的價格帶資料是否可得？** | 高 | 決定 `PRICE_FIT` 因子能否使用。此為唯一對應痛點 P-3 的因子，不可得時須明確揭露 P-3 未被評分引擎涵蓋（R-20、§12.1 第 8 項） |
| 13 | **各品類（或代表性品項）的適溫區間** | 中 | 決定 `CLIMATE` 因子能否計算。不可得時該因子整體停用，權重由其餘五因子分攤（§12.1 第 7 項） |
| 14 | **各品類的負評率門檻** | 中 | 預設 15%，但生鮮與 3C 的合理負評率差異大，需分品類設定（`risk_rule.category_id`） |
| 15 | **B 軌 MCP 工具的資料來源** | 中 | 若客戶期待涵蓋 FB／小紅書，須說明白名單限制與附錄 C 的法律評估；是否採購第三方資料商由客戶決定 |
| 16 | **OpenRouter 帳戶的實際配額** | 高 | §6.7.3 的「每分鐘 20 次、每日 1,000 次」目前是假設值，整個單輪上限與完成天數都建立在它之上。W2 須量得實際值（M0-B） |
| 17 | 是否接受「同一品項同時出現在多張榜」的呈現方式 | 低 | 影響 §FR-04 的多情境評分設計；若客戶偏好單一歸屬，可簡化為僅主情境 |
| 18 | **`LOGISTICS_RISK`／`INVENTORY_RISK` 各觸發條件的扣分點數（初始值見 §5.2.2）** | 中 | 目前是由單一黃金案例反推、能自洽但無客戶依據的預設值；需與採購團隊共同校準，同第 8 項品類前置天數的性質 |

---

# 附錄 B：環境變數

「執行期可覆寫」欄標示該值是否可由 SYS_ADMIN 於 `/admin/ai-config` 等畫面調整；標 ✗ 者變更後需重啟服務。

## 資料庫與認證

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `DB_URL` / `DB_USER` / `DB_PASSWORD` | — | MySQL 連線；URL 須含 `serverTimezone=UTC` | ✗ |
| `DB_POOL_MAX_SIZE` | 20 | HikariCP | ✗ |
| `JWT_SECRET` | — | ≥ 256 bits | ✗ |
| `JWT_ACCESS_TTL` | `2h` | access token 存活期 | ✗ |
| `JWT_REFRESH_TTL` | `7d` | refresh token 存活期 | ✗ |
| `BCRYPT_COST` | 10 | 密碼雜湊成本 | ✗ |
| `LOGIN_MAX_FAILED_ATTEMPTS` | 5 | 鎖定門檻 | ✓ |
| `LOGIN_LOCK_DURATION` | `15m` | 鎖定時長 | ✓ |
| `CORS_ALLOWED_ORIGINS` | `http://localhost:4200` | 前端來源清單 | ✗ |
| `API_BASE_PATH` | `/api/v1` | API 路徑前綴 | ✗ |
| `SPRING_PROFILES_ACTIVE` | `dev` | `dev` / `demo` | ✗ |

## LLM 與 AI

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `OPENROUTER_API_KEY` | — | **金鑰唯一來源，不存資料庫**（§10） | ✗ |
| `OPENROUTER_BASE_URL` | — | LLM 閘道端點 | ✗ |
| `MODEL_CLASSIFY_PRIMARY` / `_FALLBACK` | 見 §6.7.2 | 情境判定模型 | ✓ |
| `MODEL_LONG_TEXT_PRIMARY` / `_FALLBACK` | 見 §6.7.2 | 評論風險與賣點／風險萃取模型 | ✓ |
| `MODEL_SHORT_GEN_PRIMARY` / `_FALLBACK` | 見 §6.7.2 | 進貨建議模型 | ✓ |
| `MODEL_NUMERIC_PRIMARY` / `_FALLBACK` | 見 §6.7.2 | 趨勢解讀模型 | ✓ |
| `MODEL_REASONING_PRIMARY` / `_FALLBACK` | 見 §6.7.2 | 尋源探索與權重校準模型 | ✓ |
| `LLM_RATE_LIMIT_PER_MINUTE` | 20 | 每分鐘請求上限（W2 依實測調整） | ✓ |
| `LLM_QUOTA_DAILY` | 1000 | 每日請求上限（W2 依實測調整） | ✓ |
| `LLM_QUOTA_SHARE_TRACK_A` | 0.7 | A 軌批次池比例 | ✓ |
| `LLM_QUOTA_SHARE_TRACK_B` | 0.2 | B 軌探索池比例 | ✓ |
| `LLM_QUOTA_SHARE_RETRY` | 0.1 | 重試與臨時任務池（含季度校準） | ✓ |
| `LLM_BATCH_ITEM_CAP` | 150 | **單輪 AI 分析的品項上限**（§6.7.3） | ✓ |
| `LLM_RETRY_MAX` | 3 | 429 退避重試次數 | ✓ |
| `LLM_TIMEOUT_SECONDS` | 30 | 一般呼叫逾時 | ✓ |
| `LLM_TIMEOUT_SCOUT_SECONDS` | 90 | 尋源探索逾時 | ✓ |
| `AI_CACHE_DAYS` | 6 | 一般 Agent 快取天數（須短於評分週期 7 日） | ✓ |
| `AI_CACHE_DAYS_TREND` | 3 | 趨勢與尋源類快取天數 | ✓ |
| `MCP_SOURCING_ENDPOINT` | — | opencode / MCP server 端點 | ✗ |
| `MCP_ALLOWED_TOOLS` | 見 §FR-16-1 | **MCP 工具白名單**，逗號分隔 | ✗ |

## 熱度來源

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `THREADS_APP_ID` / `THREADS_APP_SECRET` / `THREADS_ACCESS_TOKEN` | — | Threads API | ✗ |
| `THREADS_DAILY_QUOTA` | 2200 | — | ✓ |
| `IG_ACCESS_TOKEN` / `IG_BUSINESS_ACCOUNT_ID` | — | Instagram Graph API | ✗ |
| `IG_WEEKLY_HASHTAG_QUOTA` | 30 | — | ✓ |
| `TRENDS_ENDPOINT` | — | Google Trends 存取設定 | ✗ |
| `HEAT_SOURCE_PROBE_INTERVAL` | `15m` | 健康檢查間隔（§FR-14-2） | ✓ |
| `HEAT_SOURCE_PROBE_FAIL_THRESHOLD` | 2 | 連續失敗幾次判為 UNAVAILABLE | ✓ |
| `HEAT_TAG_HALVE_AFTER_DAYS` | 14 | 人工標記權重減半天數（階梯式，非指數） | ✓ |
| `HEAT_TAG_EXPIRE_DAYS` | 30 | 人工標記失效天數 | ✓ |
| `HEAT_VOLUME_FLOOR_RATIO` | 0.2 | 熱度量級下限（品類近 90 日中位數的比例，§5.2.1-a） | ✓ |
| `HEAT_CATEGORY_GRANULARITY_DISCOUNT` | 0.5 | 品類級來源的合成權重折扣 | ✓ |

## 評分與校準

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `SCORING_MIN_CATEGORY_SAMPLE` | 10 | 同品類百分位的最低樣本數（§5.3.1） | ✓ |
| `SCORING_CONFIDENCE_WARN_THRESHOLD` | 50 | 信心度警示門檻（§5.9、§9.3） | ✓ |
| `SCENE_ADOPT_CONFIDENCE` | 0.5 | 情境判定的採用門檻 | ✓ |
| `SCENE_SCORING_CONFIDENCE` | 0.7 | 情境判定的信心度計分門檻 | ✓ |
| `CLIMATE_TOLERANCE` | 12.0 | 氣候適配度的衰減容忍範圍（°C，§FR-17-2） | ✓ |
| `CALIBRATION_MIN_SAMPLE` | 200 | 校準最低樣本數 | ✓ |
| `CAMPAIGN_RESULT_REMINDER_DAYS` | 7 | 結案後幾日未回填即提醒（§FR-11-2） | ✓ |

## 排程

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `SCHEDULE_HEAT_COLLECT` | `0 0 6 * * *` | 每日熱度採集與合成 | ✓ |
| `SCHEDULE_HEAT_ALERT` | `0 30 6 * * *` | 熱度異常示警偵測（§FR-10-2） | ✓ |
| `SCHEDULE_FULL_SCORING` | `0 0 7 * * MON` | 每週全量評分 | ✓ |
| `SCHEDULE_CALIBRATION` | `0 0 8 1 1,4,7,10 *` | 每季校準 | ✓ |
| `SCHEDULE_FESTIVAL_SYNC` | `0 0 2 1 1 *` | 每年農曆節慶換算 | ✓ |
| `SHEDLOCK_DEFAULT_LOCK_AT_MOST_FOR` | `PT2H` | ShedLock 最長持鎖時間 | ✗ |
| `SHEDLOCK_TABLE` | `shedlock` | ShedLock 鎖表名稱 | ✗ |

## 檔案與報表

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `IMPORT_SYNC_MAX_BYTES` | `2MB` | 同步匯入門檻（§FR-09） | ✓ |
| `IMPORT_SYNC_MAX_ROWS` | 5000 | 同步匯入列數門檻 | ✓ |
| `IMPORT_HARD_MAX_BYTES` | `50MB` | 硬上限，超過拒收 | ✓ |
| `IMPORT_HARD_MAX_ROWS` | 200000 | 硬上限，超過拒收 | ✓ |
| `REPORT_STORAGE_PATH` | `/var/ssds/reports` | 報表產出目錄 | ✗ |
| `REPORT_ASYNC_ROW_THRESHOLD` | 5000 | 超過此列數改非同步（AC-12-3） | ✓ |
| `REPORT_RETENTION_DAYS` | 90 | 報表檔案保留天數 | ✓ |
| `PDF_FONT_PATH` | — | Noto Sans TC 字型路徑（AC-12-1） | ✗ |

## 其他

| 變數 | 預設 | 說明 | 執行期可覆寫 |
|---|---|---|:---:|
| `LUNAR_CALENDAR_PROVIDER` | — | 農曆換算函式庫識別（W1 選型，§3.2） | ✗ |
| `CLIMATE_REGION_DEFAULT` | `TW_TPE` | 預設氣候區域 | ✓ |
| `AUDIT_RETENTION_DAYS` | 365 | 稽核紀錄保留天數 | ✓ |

---

# 附錄 C：資料取得方式的法律評估

## 結論

**系統不建置網路爬蟲。** FB／TikTok／小紅書改由 FR-14-1 人工熱度標記涵蓋。

**此結論同樣適用於 B 軌的 MCP 工具**（v3.0 補充）。MCP 只是換一種呼叫方式，不改變資料取得行為的性質——若透過 MCP 工具去爬取受服務條款保護的社群內容，法律風險與自建爬蟲相同，責任歸屬還更模糊（工具由第三方提供，但使用決定與商業利益在本系統）。因此 §FR-16-1 建立工具白名單，只允許官方 API、系統自有資料與有合約的付費資料商。

## 理由

### 一、台灣已有刑事判決前例

新北地方法院於七法（Lawsnote）案中認定：以爬蟲擷取資料時，即使取得的資料本身不受著作權法保護，違反網站使用規範的擷取行為本身仍可能構成刑法第 359 條無故取得他人電磁紀錄罪。該案負責人與工程師均遭判有期徒刑，公司另負高額民事賠償。

此與美國 hiQ v. LinkedIn 所確立的「爬取公開資料不違反 CFAA」有實質差異，不可援引美國先例評估本案風險。

### 二、本專案情境的風險更高

| 因素 | 說明 |
|---|---|
| 擷取對象 | 使用者貼文，著作權屬於不特定多數發文者 |
| 個資 | 貼文含帳號、暱稱等資訊 |
| 服務條款 | FB／IG／TikTok／小紅書均明文禁止自動化擷取 |
| 使用目的 | 交付營利公司的商業用途（商業使用正是該案認定不構成合理使用的關鍵） |

### 三、維運不可行

此類爬蟲的存活週期常以週計，平台改版即失效。交付後客戶端無工程能力維護，將成為長期負債。

## 替代方案

| 優先序 | 方案 | 說明 |
|---|---|---|
| 1 | 官方 API | Threads（額度充足）、Instagram（額度有限，品類級使用） |
| 2 | 人工標記 | FR-14-1，零法律風險、零維運成本，且累積為自有資料資產 |
| 3 | 付費第三方資料商 | 責任在供應商，合約可寫，成本可預估。須注意部分服務條款禁止轉供第三方；納入 MCP 白名單前須先確認合約允許本用途 |

## MCP 工具白名單（對應 §FR-16-1）

| 允許 | 資料來源 | 法律依據 |
|---|---|---|
| `heat.query`、`heat.manual_tags`、`catalog.lookup` | 系統自有資料庫 | 客戶自有資料 |
| `trends.official`、`threads.search` | 官方 API，與 A 軌共用憑證 | 平台授權 |
| `vendor.dataset` | 付費第三方 | 合約 |

**禁止**：任何以 HTML 抓取、模擬瀏覽器、無官方 API 授權、或違反平台服務條款方式取得資料的工具。白名單以 `MCP_ALLOWED_TOOLS` 環境變數強制，違規呼叫於 client 層被拒並寫入 `audit_log`（AC-16-7）。

## 免責說明

本節非法律意見。若客戶仍希望納入自動化擷取，建議先由客戶法務評估，並於合約明訂責任歸屬（風險 R-11、附錄 A 第 15 項）。

---

*開發規格書 v3.0　·　莊敬生活百貨 × 團購產業 AI 採購選品策略系統　·　搭配《畫面功能示意圖 v3.0》使用*

